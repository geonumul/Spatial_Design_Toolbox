window.GNN_PRACTICE=window.GNN_PRACTICE||{};window.GNN_PRACTICE["w1-walk"]={"id":"w1-walk","track":"1","title":"1주차 랜덤워크와 node2vec 워크","intro":"그래프 위를 주사위 굴리듯 걸어 다니는 코드를 직접 짜요. 기본 랜덤워크와 랜덤워크 커널, 그리고 과제 1 의 node2vec 워크(p, q 로 걷는 성격 조절)까지 차례로 만들어요.","source":"PracticeCode_1 (3).ipynb 셀 114 / Solution_1.ipynb 셀 11, 12","exercises":[{"id":"w1-walk-01","title":"무작위로 한 걸음 걷기","level":"type","run":"browser","goal":"아무 노드에서 시작해서, 이웃 중 하나를 무작위로 골라 한 걸음 옮겨요. 랜덤워크의 가장 작은 조각이에요.","explain":["그래프 위를 주사위 굴리듯 이웃으로 옮겨 다니는 것, 이걸 랜덤워크(Random Walk)라고 해요. 지나간 노드 목록이 워크예요.","`random.seed(0)` 은 무작위 순서를 고정해서 매번 같은 결과가 나오게 해요 (c04).","`random.choice(리스트)` 는 리스트에서 하나를 무작위로 골라요. 그래서 `G.nodes()` 를 `list(...)` 로 바꿔서 넣어요.","`walk.append(node)` 는 워크 리스트 끝에 새 노드를 붙여요 (c02)."],"show":"import random\nimport networkx as nx\n\nrandom.seed(0)\ngraph = nx.Graph([(1, 2), (2, 3), (3, 4), (4, 1)])\nnode = random.choice(list(graph.nodes()))\nwalk = [node]\nneighbors = list(graph.neighbors(node))\nnode = random.choice(neighbors)\nwalk.append(node)\nprint(walk)","given":"","solution":"import random\nimport networkx as nx\n\nrandom.seed(0)\ngraph = nx.Graph([(1, 2), (2, 3), (3, 4), (4, 1)])\nnode = random.choice(list(graph.nodes()))\nwalk = [node]\nneighbors = list(graph.neighbors(node))\nnode = random.choice(neighbors)\nwalk.append(node)\nprint(walk)","tests":"assert 'walk' in globals() and isinstance(walk, list), \"walk 는 리스트여야 해요\"\nassert len(walk) == 2, f\"시작 노드와 한 걸음, 노드 2개가 있어야 해요. 지금은 {len(walk)}개예요\"\nassert graph.has_edge(walk[0], walk[1]), f\"{walk[0]} 와 {walk[1]} 은 이웃이 아니에요. 두 번째 random.choice 에는 neighbors 를 넣어요\"\nassert str(walk) in __out__, \"print(walk) 로 출력해 주세요\"\n","hints":["import random 이 먼저","시작 노드는 random.choice(list(graph.nodes()))","다음 노드는 random.choice(neighbors)"],"after":"node2vec, DeepWalk 는 이 한 걸음을 수십 번 반복한 워크를 문장처럼 써서 노드 임베딩을 배워요.","source":"PracticeCode_1 (3).ipynb 셀 114"},{"id":"w1-walk-02","title":"랜덤워크 함수 완성","level":"blank","run":"browser","goal":"시작 노드를 무작위로 고르고, 이웃이 있으면 한 걸음씩 옮기고, 없으면 멈추는 random_walk 함수를 완성해요.","explain":["`for _ in range(walk_length - 1):` : 시작 노드가 이미 1개라서 걸음은 walk_length - 1 번이에요.","`if neighbors:` 는 리스트가 비어 있지 않으면 참이에요. 빈 리스트는 거짓처럼 다뤄져요.","`break` 는 반복을 바로 끝내요 (c03). 이웃 없는 노드에 갇히면 멈추는 거예요.","`tuple(walk)` 은 리스트를 튜플로 바꿔요 (c02). 튜플끼리는 `==` 로 통째로 비교하기 좋아요."],"show":"","given":"import random\nimport networkx as nx\n\ndef random_walk(graph, walk_length):\n    # Start the walk at a random node\n    node = random.choice(list(graph.___()))\n\n    # Perform the random walk\n    walk = [___]\n    for _ in range(walk_length - 1):\n        neighbors = list(graph.neighbors(___))\n        if neighbors:\n            node = random.choice(___)\n            walk.append(node)\n        else:\n            break\n\n    return tuple(walk)\n\nrandom.seed(0)\nG1 = nx.Graph([(1, 2), (2, 3), (3, 4), (4, 1)])\nw = random_walk(G1, 5)\nprint(w)","solution":"import random\nimport networkx as nx\n\ndef random_walk(graph, walk_length):\n    # Start the walk at a random node\n    node = random.choice(list(graph.nodes()))\n\n    # Perform the random walk\n    walk = [node]\n    for _ in range(walk_length - 1):\n        neighbors = list(graph.neighbors(node))\n        if neighbors:\n            node = random.choice(neighbors)\n            walk.append(node)\n        else:\n            break\n\n    return tuple(walk)\n\nrandom.seed(0)\nG1 = nx.Graph([(1, 2), (2, 3), (3, 4), (4, 1)])\nw = random_walk(G1, 5)\nprint(w)","tests":"assert 'random_walk' in globals(), \"random_walk 함수가 있어야 해요\"\nG_t = nx.Graph([(1, 2), (2, 3), (3, 4), (4, 1), (4, 5)])\nstarts = set()\nfor seed in range(40):\n    random.seed(seed)\n    t = random_walk(G_t, 6)\n    assert t is not None, \"return tuple(walk) 로 돌려줘야 해요\"\n    assert isinstance(t, tuple), \"워크는 tuple(walk) 로 바꿔서 돌려줘요\"\n    assert len(t) == 6, f\"walk_length 가 6 이면 노드 6개예요. 지금은 {len(t)}개예요. range(walk_length - 1) 을 확인해요\"\n    for a, b in zip(t, t[1:]):\n        assert G_t.has_edge(a, b), f\"{a} 에서 {b} 로는 선이 없어요. 이웃 중에서 골라야 해요\"\n    starts.add(t[0])\nassert len(starts) >= 3, \"시작 노드가 매번 같아요. 시작은 random.choice(list(graph.nodes())) 로 골라요\"\nlone = nx.Graph()\nlone.add_node(7)\nassert random_walk(lone, 5) == (7,), \"이웃이 없으면 break 로 멈춰서 (7,) 만 돌려줘야 해요\"\n","hints":["시작 노드는 graph.nodes() 에서","walk 는 시작 노드 하나로 시작","이웃은 지금 node 의 이웃, 고를 때는 neighbors 에서"],"after":"워크 길이와 개수(walk_length, num_walks)는 node2vec 과제에서 직접 정하는 설정값이에요.","source":"PracticeCode_1 (3).ipynb 셀 114"},{"id":"w1-walk-03","title":"워크가 한 칸 길어요","level":"fix","run":"browser","goal":"walk_length=5 로 불렀는데 노드가 6개 나와요. 한 곳을 고쳐요.","explain":["`range(n)` 은 n 번 돌아요. 시작 노드는 반복 전에 이미 들어 있어요.","노드 수 = 시작 1개 + 걸음 수 예요. 그러니 걸음은 walk_length - 1 번이어야 해요."],"show":"","given":"import random\nimport networkx as nx\n\ndef random_walk(graph, walk_length):\n    # Start the walk at a random node\n    node = random.choice(list(graph.nodes()))\n\n    # Perform the random walk\n    walk = [node]\n    for _ in range(walk_length):\n        neighbors = list(graph.neighbors(node))\n        if neighbors:\n            node = random.choice(neighbors)\n            walk.append(node)\n        else:\n            break\n\n    return tuple(walk)\n\nrandom.seed(0)\nG1 = nx.Graph([(1, 2), (2, 3), (3, 4), (4, 1)])\nw = random_walk(G1, 5)\nprint(w)","solution":"import random\nimport networkx as nx\n\ndef random_walk(graph, walk_length):\n    # Start the walk at a random node\n    node = random.choice(list(graph.nodes()))\n\n    # Perform the random walk\n    walk = [node]\n    for _ in range(walk_length - 1):\n        neighbors = list(graph.neighbors(node))\n        if neighbors:\n            node = random.choice(neighbors)\n            walk.append(node)\n        else:\n            break\n\n    return tuple(walk)\n\nrandom.seed(0)\nG1 = nx.Graph([(1, 2), (2, 3), (3, 4), (4, 1)])\nw = random_walk(G1, 5)\nprint(w)","tests":"assert 'random_walk' in globals(), \"random_walk 함수가 있어야 해요\"\nG_t = nx.Graph([(1, 2), (2, 3), (3, 4), (4, 1), (4, 5)])\nstarts = set()\nfor seed in range(40):\n    random.seed(seed)\n    t = random_walk(G_t, 6)\n    assert t is not None, \"return tuple(walk) 로 돌려줘야 해요\"\n    assert isinstance(t, tuple), \"워크는 tuple(walk) 로 바꿔서 돌려줘요\"\n    assert len(t) == 6, f\"walk_length 가 6 이면 노드 6개예요. 지금은 {len(t)}개예요. range(walk_length - 1) 을 확인해요\"\n    for a, b in zip(t, t[1:]):\n        assert G_t.has_edge(a, b), f\"{a} 에서 {b} 로는 선이 없어요. 이웃 중에서 골라야 해요\"\n    starts.add(t[0])\nassert len(starts) >= 3, \"시작 노드가 매번 같아요. 시작은 random.choice(list(graph.nodes())) 로 골라요\"\nlone = nx.Graph()\nlone.add_node(7)\nassert random_walk(lone, 5) == (7,), \"이웃이 없으면 break 로 멈춰서 (7,) 만 돌려줘야 해요\"\n","hints":["반복 횟수를 정하는 줄을 보세요","시작 노드까지 세면 몇 개가 되나요?","range(walk_length - 1)"],"after":"한 칸 차이 실수(off-by-one)는 워크 길이, 슬라이싱, range 에서 가장 흔한 버그예요.","source":"PracticeCode_1 (3).ipynb 셀 114"},{"id":"w1-walk-04","title":"랜덤워크 커널","level":"blank","run":"browser","goal":"두 그래프에서 워크를 여러 번 뽑아 똑같은 워크가 나온 비율을 구해요. 두 그래프가 얼마나 닮았는지 보는 숫자예요.","explain":["두 그래프가 얼마나 닮았는지 숫자 하나로 재는 함수, 이걸 그래프 커널(Graph Kernel)이라고 해요. 여기서는 같은 워크가 나온 비율을 써요.","`kernel += 1` 은 같을 때마다 1 씩 세고, `kernel /= num_walks` 는 전체 횟수로 나눠 비율로 만들어요. `a /= b` 는 `a = a / b` 예요.","`walk1 == walk2` 는 튜플 두 개가 칸마다 모두 같은지 비교해요. `=` (이름표 붙이기)와 `==` (같은지 묻기)를 헷갈리지 마세요 (c03).","random_walk 함수는 앞 실습에서 완성한 그대로 들어 있어요."],"show":"","given":"import random\nimport networkx as nx\n\ndef random_walk(graph, walk_length):\n    # Start the walk at a random node\n    node = random.choice(list(graph.nodes()))\n\n    # Perform the random walk\n    walk = [node]\n    for _ in range(walk_length - 1):\n        neighbors = list(graph.neighbors(node))\n        if neighbors:\n            node = random.choice(neighbors)\n            walk.append(node)\n        else:\n            break\n\n    return tuple(walk)\n\ndef random_walk_kernel(graph1, graph2, walk_length=2, num_walks=20):\n    # Initialize the kernel to zero\n    kernel = ___\n\n    # Perform random walks on both graphs\n    for _ in range(___):\n        # Perform a random walk on the first graph\n        walk1 = random_walk(graph1, walk_length)\n\n        # Perform a random walk on the second graph\n        walk2 = random_walk(___, walk_length)\n\n        # Update the kernel if the walks have common subsequences\n        if walk1 ___ walk2:\n            kernel += 1\n\n    # Normalize the kernel\n    kernel /= ___\n\n    return kernel\n\n# Define two simple graphs\nG1 = nx.Graph([(1, 2), (2, 3), (3, 4), (4, 1)])\nG2 = nx.Graph([(1, 2), (2, 3), (3, 4)])\n\nwalk_length = 2\nnum_walks = 20\nkernel_value = random_walk_kernel(G1, G2, walk_length, num_walks)\nprint(\"Random Walk Kernel Value:\", kernel_value)","solution":"import random\nimport networkx as nx\n\ndef random_walk(graph, walk_length):\n    # Start the walk at a random node\n    node = random.choice(list(graph.nodes()))\n\n    # Perform the random walk\n    walk = [node]\n    for _ in range(walk_length - 1):\n        neighbors = list(graph.neighbors(node))\n        if neighbors:\n            node = random.choice(neighbors)\n            walk.append(node)\n        else:\n            break\n\n    return tuple(walk)\n\ndef random_walk_kernel(graph1, graph2, walk_length=2, num_walks=20):\n    # Initialize the kernel to zero\n    kernel = 0\n\n    # Perform random walks on both graphs\n    for _ in range(num_walks):\n        # Perform a random walk on the first graph\n        walk1 = random_walk(graph1, walk_length)\n\n        # Perform a random walk on the second graph\n        walk2 = random_walk(graph2, walk_length)\n\n        # Update the kernel if the walks have common subsequences\n        if walk1 == walk2:\n            kernel += 1\n\n    # Normalize the kernel\n    kernel /= num_walks\n\n    return kernel\n\n# Define two simple graphs\nG1 = nx.Graph([(1, 2), (2, 3), (3, 4), (4, 1)])\nG2 = nx.Graph([(1, 2), (2, 3), (3, 4)])\n\nwalk_length = 2\nnum_walks = 20\nkernel_value = random_walk_kernel(G1, G2, walk_length, num_walks)\nprint(\"Random Walk Kernel Value:\", kernel_value)","tests":"assert 'random_walk_kernel' in globals(), \"random_walk_kernel 함수가 있어야 해요\"\none_a = nx.Graph(); one_a.add_node(5)\none_b = nx.Graph(); one_b.add_node(5)\nr = random_walk_kernel(one_a, one_b, 3, 10)\nassert r is not None and abs(r - 1.0) < 1e-9, f\"노드 5 하나짜리 그래프 둘은 워크가 늘 같아서 1.0 이어야 해요. 지금은 {r} 이에요\"\nr = random_walk_kernel(nx.Graph([(1, 2)]), nx.Graph([(\"a\", \"b\")]), 2, 10)\nassert abs(r) < 1e-9, \"노드 이름이 전혀 다른 그래프는 0.0 이어야 해요\"\nrandom.seed(1)\nr = random_walk_kernel(nx.Graph([(1, 2)]), nx.Graph([(1, 2)]), 2, 4000)\nassert 0.45 < r < 0.55, f\"엣지 하나짜리 같은 그래프는 약 0.5 가 나와야 해요. 지금은 {r} 이에요. num_walks 로 나눴는지 확인해요\"\nassert 0.0 <= kernel_value <= 1.0, \"kernel_value 는 0 과 1 사이 비율이어야 해요\"\n","hints":["처음 kernel 은 0","반복 횟수는 num_walks, 두 번째 워크는 graph2 에서","같은지 비교는 ==, 마지막에 num_walks 로 나눠요"],"after":"노트북에서 결과가 0.0 이 나오는 건 이상한 게 아니에요. 원 모양과 줄 모양은 워크 20번으로는 잘 겹치지 않아요.","source":"PracticeCode_1 (3).ipynb 셀 114"},{"id":"w1-walk-05","title":"node2vec 이웃 무게 세 가지","level":"type","run":"browser","goal":"방금 온 노드(previous)와 지금 노드(current)를 보고, 다음 이웃마다 고를 무게를 1/p, 1, 1/q 중 하나로 정해요.","explain":["node2vec 은 걷는 성격을 조절하는 랜덤워크예요. 되돌아가기 무게는 1/p, 방금 노드와도 이웃인 곳은 1, 더 멀어지는 곳은 1/q 예요.","q 가 작으면(여기 0.2) 1/q = 5 가 커져서 멀리 뻗어 나가요. p 가 작으면 되돌아가기를 좋아해요.","`if ... elif ... else` 는 위에서부터 처음 참인 한 곳만 실행해요 (c03).","`G.has_edge(a, b)` 는 a 와 b 사이에 선이 있으면 True 예요.","노드 2 의 이웃 순서는 엣지를 넣은 순서라서 [1, 0, 3] 이에요."],"show":"import networkx as nx\nG = nx.Graph([(0, 1), (1, 2), (0, 2), (2, 3)])\nprevious_node, current_node = 0, 2\np, q = 0.8, 0.2\nunnormalized_weights = []\nfor neighbor in G.neighbors(current_node):\n    if neighbor == previous_node:\n        unnormalized_weights.append(1.0 / p)\n    elif G.has_edge(previous_node, neighbor):\n        unnormalized_weights.append(1.0)\n    else:\n        unnormalized_weights.append(1.0 / q)","given":"","solution":"import networkx as nx\nG = nx.Graph([(0, 1), (1, 2), (0, 2), (2, 3)])\nprevious_node, current_node = 0, 2\np, q = 0.8, 0.2\nunnormalized_weights = []\nfor neighbor in G.neighbors(current_node):\n    if neighbor == previous_node:\n        unnormalized_weights.append(1.0 / p)\n    elif G.has_edge(previous_node, neighbor):\n        unnormalized_weights.append(1.0)\n    else:\n        unnormalized_weights.append(1.0 / q)","tests":"assert 'unnormalized_weights' in globals(), \"unnormalized_weights 리스트가 있어야 해요\"\nassert len(unnormalized_weights) == 3, f\"노드 2 의 이웃은 3개라 무게도 3개예요. 지금은 {len(unnormalized_weights)}개예요\"\nwant = [1.0, 1.25, 5.0]\nassert all(abs(a - b) < 1e-9 for a, b in zip(unnormalized_weights, want)), f\"무게는 [1.0, 1.25, 5.0] 이어야 해요 (이웃 1: 0 과도 이웃, 이웃 0: 되돌아가기, 이웃 3: 멀어지기). 지금은 {unnormalized_weights} 이에요\"\n","hints":["for neighbor in G.neighbors(current_node):","되돌아가기 조건은 neighbor == previous_node","elif 는 G.has_edge(previous_node, neighbor)"],"after":"과제 1 Solution 셀 11 의 node2vec_weighted_choice 가 바로 이 반복으로 시작해요.","source":"Solution_1.ipynb 셀 11"},{"id":"w1-walk-06","title":"무게를 비율로 바꿔 이웃 고르기","level":"blank","run":"browser","goal":"무게를 합이 1 인 비율로 바꾸고, 그 비율대로 이웃 하나를 뽑는 node2vec_weighted_choice 를 완성해요.","explain":["`[w / sum(무게들) for w in 무게들]` 은 무게마다 전체 합으로 나눠 비율로 만들어요 (c03 리스트 컴프리헨션).","`random.choices(후보, weights=비율)` 는 비율에 맞춰 무작위로 뽑아요. 결과가 **리스트**라서 `[0]` 으로 하나를 꺼내요.","`random.choice` (s 없음)는 똑같은 확률로 하나, `random.choices` (s 있음)는 무게를 줄 수 있어요."],"show":"","given":"import random\nimport networkx as nx\n\ndef node2vec_weighted_choice(G, current_node, previous_node, p, q):\n    neighbors = list(G.neighbors(current_node))\n    unnormalized_weights = []\n\n    for neighbor in neighbors:\n        if neighbor == ___:\n            unnormalized_weights.append(1.0 / p)\n        elif G.has_edge(___, neighbor):\n            unnormalized_weights.append(1.0)\n        else:\n            unnormalized_weights.append(1.0 / ___)\n\n    norm_weights = [w / sum(___) for w in unnormalized_weights]\n    return random.choices(neighbors, weights=___)[0]","solution":"import random\nimport networkx as nx\n\ndef node2vec_weighted_choice(G, current_node, previous_node, p, q):\n    neighbors = list(G.neighbors(current_node))\n    unnormalized_weights = []\n\n    for neighbor in neighbors:\n        if neighbor == previous_node:\n            unnormalized_weights.append(1.0 / p)\n        elif G.has_edge(previous_node, neighbor):\n            unnormalized_weights.append(1.0)\n        else:\n            unnormalized_weights.append(1.0 / q)\n\n    norm_weights = [w / sum(unnormalized_weights) for w in unnormalized_weights]\n    return random.choices(neighbors, weights=norm_weights)[0]","tests":"from collections import Counter\nassert 'node2vec_weighted_choice' in globals(), \"node2vec_weighted_choice 함수가 있어야 해요\"\nG_t = nx.Graph([(0, 1), (1, 2), (0, 2), (2, 3)])\nrandom.seed(0)\npicks = [node2vec_weighted_choice(G_t, 2, 0, 0.8, 0.2) for _ in range(20000)]\nassert None not in picks, \"고른 이웃 하나를 return 해야 해요\"\nc = Counter(picks)\nfor node, prob in [(1, 1.0 / 7.25), (0, 1.25 / 7.25), (3, 5.0 / 7.25)]:\n    got = c[node] / 20000\n    assert abs(got - prob) < 0.02, f\"p=0.8, q=0.2 에서 이웃 {node} 가 뽑히는 비율은 약 {prob:.3f} 이어야 해요. 지금은 {got:.3f} 이에요. 무게 규칙과 weights= 를 확인해요\"\nrandom.seed(1)\nc = Counter(node2vec_weighted_choice(G_t, 2, 0, 1.0, 1.0) for _ in range(9000))\nassert all(abs(c[n] / 9000 - 1 / 3) < 0.03 for n in [0, 1, 3]), \"p = q = 1 이면 세 이웃이 똑같이 약 1/3 씩 뽑혀야 해요\"\nrandom.seed(2)\nc = Counter(node2vec_weighted_choice(G_t, 2, 1, 1.0, 0.5) for _ in range(12000))\nassert abs(c[3] / 12000 - 0.5) < 0.03, \"previous 가 1 일 때 이웃 3 은 무게 2 라서 약 0.5 로 뽑혀야 해요\"\n","hints":["되돌아가기는 previous_node 와 같을 때","has_edge 첫 칸은 previous_node, 멀어지기는 1.0 / q","sum 안에는 unnormalized_weights, weights= 에는 norm_weights"],"after":"비율로 뽑기는 LINE 의 음성 샘플링(Vose alias)과 GNN 이웃 샘플링에도 똑같이 쓰여요.","source":"Solution_1.ipynb 셀 11"},{"id":"w1-walk-07","title":"방금 온 노드를 잘못 넘겼어요","level":"fix","run":"browser","goal":"node2vec 워크인데 p, q 를 바꿔도 걷는 성격이 안 바뀌어요. previous_node 로 넘기는 값 하나를 고쳐요.","explain":["`walk[-1]` 은 리스트 맨 끝(지금 노드), `walk[-2]` 는 끝에서 두 번째(방금 전 노드)예요 (c02 의 음수 인덱스).","`len(walk) == 1` 이면 아직 방금 전 노드가 없어서 첫 걸음은 그냥 `random.choice` 로 골라요.","previous 에 지금 노드를 넣으면 모든 이웃이 지금 노드와 이어져 있어서 무게가 전부 1 이 돼요. 그래서 p, q 가 효과가 없어요.","node2vec_weighted_choice 는 앞 실습에서 완성한 그대로 들어 있어요."],"show":"","given":"import random\nimport networkx as nx\n\ndef node2vec_weighted_choice(G, current_node, previous_node, p, q):\n    neighbors = list(G.neighbors(current_node))\n    unnormalized_weights = []\n\n    for neighbor in neighbors:\n        if neighbor == previous_node:\n            unnormalized_weights.append(1.0 / p)\n        elif G.has_edge(previous_node, neighbor):\n            unnormalized_weights.append(1.0)\n        else:\n            unnormalized_weights.append(1.0 / q)\n\n    norm_weights = [w / sum(unnormalized_weights) for w in unnormalized_weights]\n    return random.choices(neighbors, weights=norm_weights)[0]\n\ndef node2vec_walk(G, start_node, num_steps, p, q):\n    walk = [start_node]\n    for _ in range(num_steps - 1):\n        current_node = walk[-1]\n        neighbors = list(G.neighbors(current_node))\n\n        if len(neighbors) == 0:\n            break\n\n        if len(walk) == 1:\n            next_node = random.choice(neighbors)\n        else:\n            next_node = node2vec_weighted_choice(G, current_node, walk[-1], p, q)\n\n        walk.append(next_node)\n    return walk","solution":"import random\nimport networkx as nx\n\ndef node2vec_weighted_choice(G, current_node, previous_node, p, q):\n    neighbors = list(G.neighbors(current_node))\n    unnormalized_weights = []\n\n    for neighbor in neighbors:\n        if neighbor == previous_node:\n            unnormalized_weights.append(1.0 / p)\n        elif G.has_edge(previous_node, neighbor):\n            unnormalized_weights.append(1.0)\n        else:\n            unnormalized_weights.append(1.0 / q)\n\n    norm_weights = [w / sum(unnormalized_weights) for w in unnormalized_weights]\n    return random.choices(neighbors, weights=norm_weights)[0]\n\ndef node2vec_walk(G, start_node, num_steps, p, q):\n    walk = [start_node]\n    for _ in range(num_steps - 1):\n        current_node = walk[-1]\n        neighbors = list(G.neighbors(current_node))\n\n        if len(neighbors) == 0:\n            break\n\n        if len(walk) == 1:\n            next_node = random.choice(neighbors)\n        else:\n            next_node = node2vec_weighted_choice(G, current_node, walk[-2], p, q)\n\n        walk.append(next_node)\n    return walk","tests":"assert 'node2vec_walk' in globals(), \"node2vec_walk 함수가 있어야 해요\"\nP = nx.path_graph(4)\nfor seed in range(10):\n    random.seed(seed)\n    w = node2vec_walk(P, 0, 7, 1e9, 1.0)\n    assert w is not None, \"walk 리스트를 return 해야 해요\"\n    assert w == [0, 1, 2, 3, 2, 1, 0], f\"p 가 아주 크면 되돌아가지 않아서 0-1-2-3 길에서 [0, 1, 2, 3, 2, 1, 0] 이 나와야 해요. 지금은 {w} 이에요. previous 로 넘기는 값이 방금 전 노드 walk[-2] 인지 확인해요\"\nK = nx.karate_club_graph()\nrandom.seed(3)\nw = node2vec_walk(K, 5, 10, 0.8, 0.2)\nassert len(w) == 10 and w[0] == 5, f\"가라테 그래프에서 5번에서 시작하는 10칸 워크여야 해요. 지금은 길이 {len(w)}, 시작 {w[0]} 이에요\"\nassert all(K.has_edge(a, b) for a, b in zip(w, w[1:])), \"워크의 이웃한 두 노드는 선으로 이어져 있어야 해요\"\nlone = nx.Graph(); lone.add_node(9)\nassert node2vec_walk(lone, 9, 5, 1, 1) == [9], \"이웃이 없으면 [시작 노드] 만 돌려줘야 해요\"\n","hints":["node2vec_weighted_choice 를 부르는 줄을 보세요","지금 노드는 walk[-1], 방금 전 노드는?","walk[-2] 로 고쳐요"],"after":"과제 결과 그림이 이상할 때 이런 인덱스 한 칸 실수가 원인인 경우가 많아요. 에러가 안 나서 더 찾기 어려워요.","source":"Solution_1.ipynb 셀 11"},{"id":"w1-walk-08","title":"워크를 문자열 문장으로","level":"type","run":"browser","goal":"숫자 노드로 된 워크들을 문자열 리스트로 바꿔요. Word2Vec 은 단어(문자열) 문장을 받기 때문이에요.","explain":["Word2Vec 은 문장 속 단어의 뜻을 숫자 벡터로 배우는 모델이에요. node2vec 은 워크를 문장, 노드를 단어로 보고 이걸 그대로 써요.","`[[str(node) for node in walk] for walk in walks_int]` 은 컴프리헨션 안에 컴프리헨션이에요. 바깥은 워크마다, 안쪽은 노드마다 돌아요 (c03).","`str(3)` 은 숫자 3 을 글자 \"3\" 으로 바꿔요 (c01).","위 네 줄(num_walks ~ q)은 과제 셀 12 에서 정하는 설정값이에요."],"show":"num_walks = 100\nwalk_length = 10\np = 0.8\nq = 0.2\nwalks_int = [[0, 1, 2, 1], [2, 3, 2, 0], [1, 0, 2, 3]]\nwalks = [[str(node) for node in walk] for walk in walks_int]\nprint(walks[0])\nprint(type(walks[0][0]).__name__)","given":"","solution":"num_walks = 100\nwalk_length = 10\np = 0.8\nq = 0.2\nwalks_int = [[0, 1, 2, 1], [2, 3, 2, 0], [1, 0, 2, 3]]\nwalks = [[str(node) for node in walk] for walk in walks_int]\nprint(walks[0])\nprint(type(walks[0][0]).__name__)","tests":"assert 'walks' in globals(), \"walks 리스트가 있어야 해요\"\nassert walks == [[\"0\", \"1\", \"2\", \"1\"], [\"2\", \"3\", \"2\", \"0\"], [\"1\", \"0\", \"2\", \"3\"]], f\"워크가 문자열로 바뀌어야 해요. 지금은 {walks} 이에요\"\nassert (num_walks, walk_length, p, q) == (100, 10, 0.8, 0.2), \"설정값 네 개를 과제와 같게 적어요\"\nassert \"str\" in __out__, \"print(type(walks[0][0]).__name__) 로 str 이 출력되어야 해요\"\n","hints":["설정값 네 줄부터","안쪽 컴프리헨션은 [str(node) for node in walk]","바깥은 for walk in walks_int"],"after":"과제 셀 12 는 이 변환을 node2vec_walk 호출과 한 번에 써요. 다음 실습이에요.","source":"Solution_1.ipynb 셀 12"},{"id":"w1-walk-09","title":"가라테 그래프에서 워크 100개 만들기","level":"blank","run":"browser","goal":"시작 노드를 무작위로 골라 node2vec 워크를 100개 만들고, 각 워크를 문자열 리스트로 바꿔요.","explain":["괄호 안이 여러 줄로 나뉘어 있어도 괄호가 닫힐 때까지 한 문장이에요. 길어서 보기 좋게 줄을 나눈 거예요.","`start_node=...`, `num_steps=...` 처럼 이름을 붙여 값을 넘길 수 있어요. 순서를 헷갈리지 않아요 (c04 의 매개변수).","원본은 PyG 의 KarateClub 을 networkx 로 바꾼 그래프예요. 브라우저에서는 PyG 를 못 써서 `nx.karate_club_graph()` 로 바꿨어요. 노드와 엣지는 같아요."],"show":"","given":"import random\nimport networkx as nx\n\ndef node2vec_weighted_choice(G, current_node, previous_node, p, q):\n    neighbors = list(G.neighbors(current_node))\n    unnormalized_weights = []\n\n    for neighbor in neighbors:\n        if neighbor == previous_node:\n            unnormalized_weights.append(1.0 / p)\n        elif G.has_edge(previous_node, neighbor):\n            unnormalized_weights.append(1.0)\n        else:\n            unnormalized_weights.append(1.0 / q)\n\n    norm_weights = [w / sum(unnormalized_weights) for w in unnormalized_weights]\n    return random.choices(neighbors, weights=norm_weights)[0]\n\ndef node2vec_walk(G, start_node, num_steps, p, q):\n    walk = [start_node]\n    for _ in range(num_steps - 1):\n        current_node = walk[-1]\n        neighbors = list(G.neighbors(current_node))\n\n        if len(neighbors) == 0:\n            break\n\n        if len(walk) == 1:\n            next_node = random.choice(neighbors)\n        else:\n            next_node = node2vec_weighted_choice(G, current_node, walk[-2], p, q)\n\n        walk.append(next_node)\n    return walk\n\nkarate_graph = nx.karate_club_graph()\n\n# Generate random walks\nnum_walks = 100\nwalk_length = 10\np = 0.8\nq = 0.2\n\nwalks = [\n    [str(node) for node in node2vec_walk(\n        karate_graph,\n        start_node=random.choice(list(karate_graph.___())),\n        num_steps=___,\n        p=p,\n        q=q\n    )]\n    for _ in range(___)\n]\nprint(len(walks), walks[0])","solution":"import random\nimport networkx as nx\n\ndef node2vec_weighted_choice(G, current_node, previous_node, p, q):\n    neighbors = list(G.neighbors(current_node))\n    unnormalized_weights = []\n\n    for neighbor in neighbors:\n        if neighbor == previous_node:\n            unnormalized_weights.append(1.0 / p)\n        elif G.has_edge(previous_node, neighbor):\n            unnormalized_weights.append(1.0)\n        else:\n            unnormalized_weights.append(1.0 / q)\n\n    norm_weights = [w / sum(unnormalized_weights) for w in unnormalized_weights]\n    return random.choices(neighbors, weights=norm_weights)[0]\n\ndef node2vec_walk(G, start_node, num_steps, p, q):\n    walk = [start_node]\n    for _ in range(num_steps - 1):\n        current_node = walk[-1]\n        neighbors = list(G.neighbors(current_node))\n\n        if len(neighbors) == 0:\n            break\n\n        if len(walk) == 1:\n            next_node = random.choice(neighbors)\n        else:\n            next_node = node2vec_weighted_choice(G, current_node, walk[-2], p, q)\n\n        walk.append(next_node)\n    return walk\n\nkarate_graph = nx.karate_club_graph()\n\n# Generate random walks\nnum_walks = 100\nwalk_length = 10\np = 0.8\nq = 0.2\n\nwalks = [\n    [str(node) for node in node2vec_walk(\n        karate_graph,\n        start_node=random.choice(list(karate_graph.nodes())),\n        num_steps=walk_length,\n        p=p,\n        q=q\n    )]\n    for _ in range(num_walks)\n]\nprint(len(walks), walks[0])","tests":"assert 'walks' in globals() and isinstance(walks, list), \"walks 리스트가 있어야 해요\"\nassert len(walks) == 100, f\"워크는 num_walks = 100 개예요. 지금은 {len(walks)}개예요\"\nassert all(len(w) == 10 for w in walks), \"워크마다 노드 10개여야 해요. num_steps=walk_length 를 확인해요\"\nassert all(isinstance(x, str) for w in walks for x in w), \"워크 안 노드는 모두 str 이어야 해요\"\nassert all(karate_graph.has_edge(int(a), int(b)) for w in walks for a, b in zip(w, w[1:])), \"워크의 이웃한 두 노드는 선으로 이어져 있어야 해요\"\nassert len({w[0] for w in walks}) > 5, \"시작 노드가 너무 적어요. random.choice(list(karate_graph.nodes())) 로 골라요\"\n","hints":["시작 노드 후보는 karate_graph.nodes()","num_steps 에는 walk_length","바깥 반복은 range(num_walks)"],"after":"이 walks 가 그대로 Word2Vec(sentences=walks, ...) 에 들어가요 (과제 셀 12 뒷부분, w1-assign).","source":"Solution_1.ipynb 셀 10, 12"},{"id":"w1-walk-10","title":"node2vec 이웃 고르기 직접 짜기","level":"write","run":"browser","goal":"node2vec_weighted_choice(G, current_node, previous_node, p, q) 를 주석만 보고 짜요.","explain":["05 번(무게 세 가지)과 06 번(비율로 뽑기)을 한 함수로 합치는 거예요.","채점은 2만 번 뽑아서 비율이 이론값과 가까운지 봐요. 무작위라도 규칙이 맞으면 통과해요."],"show":"","given":"import random\nimport networkx as nx\n\n# 1. neighbors = current_node 의 이웃 리스트, unnormalized_weights = 빈 리스트\n# 2. 이웃 neighbor 마다 무게를 붙여요\n#    - neighbor 가 previous_node 이면 1.0 / p\n#    - previous_node 와 neighbor 사이에 선이 있으면 1.0\n#    - 나머지는 1.0 / q\n# 3. norm_weights = 무게마다 전체 합으로 나눈 리스트\n# 4. random.choices(neighbors, weights=norm_weights) 에서 하나를 꺼내 return\ndef node2vec_weighted_choice(G, current_node, previous_node, p, q):\n    ...","solution":"import random\nimport networkx as nx\n\ndef node2vec_weighted_choice(G, current_node, previous_node, p, q):\n    neighbors = list(G.neighbors(current_node))\n    unnormalized_weights = []\n\n    for neighbor in neighbors:\n        if neighbor == previous_node:\n            unnormalized_weights.append(1.0 / p)\n        elif G.has_edge(previous_node, neighbor):\n            unnormalized_weights.append(1.0)\n        else:\n            unnormalized_weights.append(1.0 / q)\n\n    norm_weights = [w / sum(unnormalized_weights) for w in unnormalized_weights]\n    return random.choices(neighbors, weights=norm_weights)[0]","tests":"from collections import Counter\nassert 'node2vec_weighted_choice' in globals(), \"node2vec_weighted_choice 함수가 있어야 해요\"\nG_t = nx.Graph([(0, 1), (1, 2), (0, 2), (2, 3)])\nrandom.seed(0)\npicks = [node2vec_weighted_choice(G_t, 2, 0, 0.8, 0.2) for _ in range(20000)]\nassert None not in picks, \"고른 이웃 하나를 return 해야 해요\"\nc = Counter(picks)\nfor node, prob in [(1, 1.0 / 7.25), (0, 1.25 / 7.25), (3, 5.0 / 7.25)]:\n    got = c[node] / 20000\n    assert abs(got - prob) < 0.02, f\"p=0.8, q=0.2 에서 이웃 {node} 가 뽑히는 비율은 약 {prob:.3f} 이어야 해요. 지금은 {got:.3f} 이에요. 무게 규칙과 weights= 를 확인해요\"\nrandom.seed(1)\nc = Counter(node2vec_weighted_choice(G_t, 2, 0, 1.0, 1.0) for _ in range(9000))\nassert all(abs(c[n] / 9000 - 1 / 3) < 0.03 for n in [0, 1, 3]), \"p = q = 1 이면 세 이웃이 똑같이 약 1/3 씩 뽑혀야 해요\"\nrandom.seed(2)\nc = Counter(node2vec_weighted_choice(G_t, 2, 1, 1.0, 0.5) for _ in range(12000))\nassert abs(c[3] / 12000 - 0.5) < 0.03, \"previous 가 1 일 때 이웃 3 은 무게 2 라서 약 0.5 로 뽑혀야 해요\"\n","hints":["neighbors = list(G.neighbors(current_node))","if 되돌아가기 1/p, elif has_edge 1, else 1/q","비율로 나누고 random.choices(neighbors, weights=...)[0]"],"after":"p, q 를 바꿔 과제를 다시 돌려 보면 t-SNE 그림에서 무리가 뭉치는 모양이 달라져요.","source":"Solution_1.ipynb 셀 11"},{"id":"w1-walk-11","title":"node2vec 워크 직접 짜기","level":"write","run":"browser","goal":"node2vec_walk(G, start_node, num_steps, p, q) 를 짜요. 이웃 고르기 함수는 이미 들어 있어요.","explain":["첫 걸음은 방금 전 노드가 없어서 `random.choice`, 두 번째 걸음부터 `node2vec_weighted_choice(G, 지금, 방금 전, p, q)` 예요.","지금 노드는 `walk[-1]`, 방금 전 노드는 `walk[-2]` 예요 (07 번 실습)."],"show":"","given":"import random\nimport networkx as nx\n\ndef node2vec_weighted_choice(G, current_node, previous_node, p, q):\n    neighbors = list(G.neighbors(current_node))\n    unnormalized_weights = []\n\n    for neighbor in neighbors:\n        if neighbor == previous_node:\n            unnormalized_weights.append(1.0 / p)\n        elif G.has_edge(previous_node, neighbor):\n            unnormalized_weights.append(1.0)\n        else:\n            unnormalized_weights.append(1.0 / q)\n\n    norm_weights = [w / sum(unnormalized_weights) for w in unnormalized_weights]\n    return random.choices(neighbors, weights=norm_weights)[0]\n\n# 1. walk = [start_node]\n# 2. num_steps - 1 번 반복:\n#    2-1. current_node = walk 의 마지막, neighbors = 그 이웃 리스트\n#    2-2. 이웃이 없으면 break\n#    2-3. walk 에 노드가 하나뿐이면 random.choice(neighbors),\n#         아니면 node2vec_weighted_choice(G, current_node, 방금 전 노드, p, q)\n#    2-4. 고른 노드를 walk 에 붙여요\n# 3. walk 를 return\ndef node2vec_walk(G, start_node, num_steps, p, q):\n    ...","solution":"import random\nimport networkx as nx\n\ndef node2vec_weighted_choice(G, current_node, previous_node, p, q):\n    neighbors = list(G.neighbors(current_node))\n    unnormalized_weights = []\n\n    for neighbor in neighbors:\n        if neighbor == previous_node:\n            unnormalized_weights.append(1.0 / p)\n        elif G.has_edge(previous_node, neighbor):\n            unnormalized_weights.append(1.0)\n        else:\n            unnormalized_weights.append(1.0 / q)\n\n    norm_weights = [w / sum(unnormalized_weights) for w in unnormalized_weights]\n    return random.choices(neighbors, weights=norm_weights)[0]\n\ndef node2vec_walk(G, start_node, num_steps, p, q):\n    walk = [start_node]\n    for _ in range(num_steps - 1):\n        current_node = walk[-1]\n        neighbors = list(G.neighbors(current_node))\n\n        if len(neighbors) == 0:\n            break\n\n        if len(walk) == 1:\n            next_node = random.choice(neighbors)\n        else:\n            next_node = node2vec_weighted_choice(G, current_node, walk[-2], p, q)\n\n        walk.append(next_node)\n    return walk","tests":"assert 'node2vec_walk' in globals(), \"node2vec_walk 함수가 있어야 해요\"\nP = nx.path_graph(4)\nfor seed in range(10):\n    random.seed(seed)\n    w = node2vec_walk(P, 0, 7, 1e9, 1.0)\n    assert w is not None, \"walk 리스트를 return 해야 해요\"\n    assert w == [0, 1, 2, 3, 2, 1, 0], f\"p 가 아주 크면 되돌아가지 않아서 0-1-2-3 길에서 [0, 1, 2, 3, 2, 1, 0] 이 나와야 해요. 지금은 {w} 이에요. previous 로 넘기는 값이 방금 전 노드 walk[-2] 인지 확인해요\"\nK = nx.karate_club_graph()\nrandom.seed(3)\nw = node2vec_walk(K, 5, 10, 0.8, 0.2)\nassert len(w) == 10 and w[0] == 5, f\"가라테 그래프에서 5번에서 시작하는 10칸 워크여야 해요. 지금은 길이 {len(w)}, 시작 {w[0]} 이에요\"\nassert all(K.has_edge(a, b) for a, b in zip(w, w[1:])), \"워크의 이웃한 두 노드는 선으로 이어져 있어야 해요\"\nlone = nx.Graph(); lone.add_node(9)\nassert node2vec_walk(lone, 9, 5, 1, 1) == [9], \"이웃이 없으면 [시작 노드] 만 돌려줘야 해요\"\n","hints":["walk = [start_node] 로 시작, num_steps - 1 번 반복","이웃이 없으면 break","len(walk) == 1 이면 random.choice, 아니면 weighted_choice 에 walk[-2]","walk.append(next_node), 반복 뒤 return walk"],"after":"DeepWalk 는 p = q = 1 인 node2vec 과 같아요. 이 함수 하나로 두 방법을 다 돌릴 수 있어요.","source":"Solution_1.ipynb 셀 11"},{"id":"w1-walk-12","title":"워크 모음 함수 직접 짜기","level":"write","run":"browser","goal":"generate_walks(G, num_walks, walk_length, p, q) 가 무작위 시작 노드로 워크 num_walks 개를 만들어 문자열 리스트의 리스트로 돌려주게 짜요.","explain":["09 번 실습의 walks = [...] 를 함수로 옮기는 거예요. 한 줄 컴프리헨션이 어려우면 빈 리스트에 for 문으로 append 해도 똑같아요.","노드 번호를 `str(node)` 로 바꾸는 걸 잊지 마세요. Word2Vec 에 넣을 문장이에요."],"show":"","given":"import random\nimport networkx as nx\n\ndef node2vec_weighted_choice(G, current_node, previous_node, p, q):\n    neighbors = list(G.neighbors(current_node))\n    unnormalized_weights = []\n\n    for neighbor in neighbors:\n        if neighbor == previous_node:\n            unnormalized_weights.append(1.0 / p)\n        elif G.has_edge(previous_node, neighbor):\n            unnormalized_weights.append(1.0)\n        else:\n            unnormalized_weights.append(1.0 / q)\n\n    norm_weights = [w / sum(unnormalized_weights) for w in unnormalized_weights]\n    return random.choices(neighbors, weights=norm_weights)[0]\n\ndef node2vec_walk(G, start_node, num_steps, p, q):\n    walk = [start_node]\n    for _ in range(num_steps - 1):\n        current_node = walk[-1]\n        neighbors = list(G.neighbors(current_node))\n\n        if len(neighbors) == 0:\n            break\n\n        if len(walk) == 1:\n            next_node = random.choice(neighbors)\n        else:\n            next_node = node2vec_weighted_choice(G, current_node, walk[-2], p, q)\n\n        walk.append(next_node)\n    return walk\n\n# 1. 빈 리스트 walks 를 만들어요\n# 2. num_walks 번 반복:\n#    2-1. 시작 노드를 G 의 노드 중에서 무작위로 골라요\n#    2-2. node2vec_walk(G, 시작, walk_length, p, q) 로 워크를 만들어요\n#    2-3. 워크 속 노드를 모두 str 로 바꾼 리스트를 walks 에 붙여요\n# 3. walks 를 return\ndef generate_walks(G, num_walks, walk_length, p, q):\n    ...","solution":"import random\nimport networkx as nx\n\ndef node2vec_weighted_choice(G, current_node, previous_node, p, q):\n    neighbors = list(G.neighbors(current_node))\n    unnormalized_weights = []\n\n    for neighbor in neighbors:\n        if neighbor == previous_node:\n            unnormalized_weights.append(1.0 / p)\n        elif G.has_edge(previous_node, neighbor):\n            unnormalized_weights.append(1.0)\n        else:\n            unnormalized_weights.append(1.0 / q)\n\n    norm_weights = [w / sum(unnormalized_weights) for w in unnormalized_weights]\n    return random.choices(neighbors, weights=norm_weights)[0]\n\ndef node2vec_walk(G, start_node, num_steps, p, q):\n    walk = [start_node]\n    for _ in range(num_steps - 1):\n        current_node = walk[-1]\n        neighbors = list(G.neighbors(current_node))\n\n        if len(neighbors) == 0:\n            break\n\n        if len(walk) == 1:\n            next_node = random.choice(neighbors)\n        else:\n            next_node = node2vec_weighted_choice(G, current_node, walk[-2], p, q)\n\n        walk.append(next_node)\n    return walk\n\ndef generate_walks(G, num_walks, walk_length, p, q):\n    walks = [\n        [str(node) for node in node2vec_walk(\n            G,\n            start_node=random.choice(list(G.nodes())),\n            num_steps=walk_length,\n            p=p,\n            q=q\n        )]\n        for _ in range(num_walks)\n    ]\n    return walks","tests":"assert 'generate_walks' in globals(), \"generate_walks 함수를 만들어야 해요\"\nK = nx.karate_club_graph()\nrandom.seed(0)\nws = generate_walks(K, 50, 8, 0.8, 0.2)\nassert ws is not None, \"walks 를 return 해야 해요\"\nassert len(ws) == 50 and all(len(w) == 8 for w in ws), \"워크 50개, 각 8칸이어야 해요. num_walks 와 walk_length 를 확인해요\"\nassert all(isinstance(x, str) for w in ws for x in w), \"노드는 str(node) 로 바꿔야 해요\"\nassert all(K.has_edge(int(a), int(b)) for w in ws for a, b in zip(w, w[1:])), \"워크의 이웃한 두 노드는 선으로 이어져 있어야 해요\"\nassert len({w[0] for w in ws}) > 5, \"시작 노드를 워크마다 무작위로 골라야 해요\"\nC = nx.cycle_graph(5)\nws = generate_walks(C, 7, 3, 1.0, 1.0)\nassert len(ws) == 7 and all(len(w) == 3 for w in ws), \"다른 그래프, 다른 개수에서도 맞아야 해요\"\n","hints":["빈 walks 리스트를 만들고 num_walks 번 반복","시작 노드는 random.choice(list(G.nodes()))","node2vec_walk 결과를 [str(node) for node in ...] 로 바꿔 append","return walks"],"after":"이제 과제 Part 2 의 앞 절반(워크 만들기)을 혼자 쓸 수 있어요. 나머지 Word2Vec 과 t-SNE 는 w1-assign 에서 해요.","source":"Solution_1.ipynb 셀 12"}]};
