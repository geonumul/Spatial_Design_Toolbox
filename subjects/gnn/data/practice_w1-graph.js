window.GNN_PRACTICE=window.GNN_PRACTICE||{};window.GNN_PRACTICE["w1-graph"]={"id":"w1-graph","track":"1","title":"1주차 networkx 기본과 그래프 통계","intro":"PracticeCode_1 앞부분과 과제 1 Part 1 을 직접 다시 짜요. 노드와 엣지 넣기, 차수, 인접행렬, 연결 요소, 최단 경로를 손으로 쳐 보고 함수로 만들어요.","source":"PracticeCode_1 (3).ipynb 셀 12~55, 88 / Solution_1.ipynb 셀 6, 8","exercises":[{"id":"w1-graph-01","title":"빈 그래프에 노드 6개 넣기","level":"type","run":"browser","goal":"빈 무방향 그래프를 만들고 노드 1~6 을 하나씩 넣어요. 노트북 4.1 절의 첫 두 칸이에요.","explain":["`nx.Graph()` 는 점(노드)과 선(엣지)을 담을 빈 상자를 만들어요. 방향이 없는 그래프예요. c08 에서 본 그대로예요.","`G.add_node(1)` 은 상자에 1번 점 하나를 넣어요. 점(노드, Node)은 사람, 선(엣지, Edge)은 친구 관계라고 생각하면 돼요.","`G.nodes()` 는 지금 들어 있는 노드 목록을 보여 줘요."],"show":"import networkx as nx\n\nG = nx.Graph()\nG.add_node(1)\nG.add_node(2)\nG.add_node(3)\nG.add_node(4)\nG.add_node(5)\nG.add_node(6)\nprint(\"Nodes:\", G.nodes())","given":"","solution":"import networkx as nx\n\nG = nx.Graph()\nG.add_node(1)\nG.add_node(2)\nG.add_node(3)\nG.add_node(4)\nG.add_node(5)\nG.add_node(6)\nprint(\"Nodes:\", G.nodes())","tests":"assert 'G' in globals(), \"G 라는 이름으로 그래프를 만들어야 해요\"\nassert isinstance(G, nx.Graph) and not G.is_directed(), \"G 는 nx.Graph() 로 만든 무방향 그래프여야 해요\"\nassert sorted(G.nodes()) == [1, 2, 3, 4, 5, 6], f\"노드는 1~6 이어야 해요. 지금은 {sorted(G.nodes())} 이에요\"\nassert G.number_of_edges() == 0, \"이번에는 엣지를 넣지 않아요\"\nassert \"Nodes:\" in __out__, \"print(\\\"Nodes:\\\", G.nodes()) 로 출력해 주세요\"\n","hints":["import networkx as nx 로 시작해요","G = nx.Graph() 로 빈 그래프","G.add_node(숫자) 를 6번"],"after":"노트북에서 그래프를 만드는 첫 줄은 거의 항상 G = nx.Graph() 예요.","source":"PracticeCode_1 (3).ipynb 셀 12, 14","think":["이 코드가 만드는 것: 노드 1~6 만 있고 엣지는 없는 무방향 그래프 `G` 예요.","순서대로 읽어요. networkx 를 `nx` 로 불러오고, `nx.Graph()` 로 빈 그래프를 만들어요.","`G.add_node(숫자)` 줄이 숫자만 바뀌며 6번 나와요. 한 줄에 노드 하나씩 들어가요.","틀리기 쉬운 곳: `nx.Graph` 의 G 는 대문자예요. 끝의 `()` 를 빼면 빈 그래프가 아니라 틀 자체가 담겨요.","확인: `Nodes: [1, 2, 3, 4, 5, 6]` 이 찍히면 맞아요."],"pseudo":"networkx 를 nx 라는 이름으로 불러온다\n빈 무방향 그래프 G 를 만든다\n숫자 1 부터 6 까지 하나씩 노드로 넣는다\nG 의 노드 목록을 출력한다"},{"id":"w1-graph-02","title":"엣지 넣고 개수 세기","level":"blank","run":"browser","goal":"노드 6개가 있는 그래프에 엣지 7개를 넣고, 노드 목록, 엣지 목록, 노드 수, 엣지 수를 출력해요.","explain":["`G.add_edge(1,2)` 는 1번과 2번 사이에 선을 하나 그어요. 무방향이라 (2,1) 과 같은 선이에요.","`G.edges()` 는 엣지 목록, `G.number_of_nodes()` 는 노드 수, `G.number_of_edges()` 는 엣지 수예요.","`print(\"Nodes:\",node_list)` 처럼 쉼표로 여러 값을 한 줄에 찍어요 (c01)."],"show":"","given":"import networkx as nx\n\nG = nx.Graph()\nG.add_node(1)\nG.add_node(2)\nG.add_node(3)\nG.add_node(4)\nG.add_node(5)\nG.add_node(6)\n\nG.add_edge(1,2)\nG.add_edge(3,1)\nG.___(2,4)\nG.add_edge(4,1)\nG.add_edge(5,1)\nG.add_edge(1,6)\nG.add_edge(___)\n\nnode_list = G.nodes()\nprint(\"Nodes:\",node_list)\nedge_list = G.___()\nprint(\"Edges:\",edge_list)\n\nn = G.___()\nprint(\"Number of nodes:\",n)\ne = G.number_of_edges()\nprint(\"Number of edges:\",e)","solution":"import networkx as nx\n\nG = nx.Graph()\nG.add_node(1)\nG.add_node(2)\nG.add_node(3)\nG.add_node(4)\nG.add_node(5)\nG.add_node(6)\n\nG.add_edge(1,2)\nG.add_edge(3,1)\nG.add_edge(2,4)\nG.add_edge(4,1)\nG.add_edge(5,1)\nG.add_edge(1,6)\nG.add_edge(6,2)\n\nnode_list = G.nodes()\nprint(\"Nodes:\",node_list)\nedge_list = G.edges()\nprint(\"Edges:\",edge_list)\n\nn = G.number_of_nodes()\nprint(\"Number of nodes:\",n)\ne = G.number_of_edges()\nprint(\"Number of edges:\",e)","tests":"assert 'G' in globals(), \"G 그래프가 있어야 해요\"\nwant = {(1, 2), (1, 3), (2, 4), (1, 4), (1, 5), (1, 6), (2, 6)}\nhave = {tuple(sorted(x)) for x in G.edges()}\nassert have == want, f\"엣지 7개가 모두 있어야 해요. 빠진 엣지: {sorted(want - have)}, 남는 엣지: {sorted(have - want)}\"\nassert 'n' in globals() and n == 6, \"n 은 G.number_of_nodes() 로 구한 6 이어야 해요\"\nassert 'e' in globals() and e == 7, \"e 는 G.number_of_edges() 로 구한 7 이어야 해요\"\nassert \"Number of edges: 7\" in __out__, \"Number of edges: 7 이 출력되어야 해요\"\n","hints":["엣지를 긋는 메서드 이름은 add_edge","마지막 엣지는 6번과 2번 사이","엣지 목록은 G.edges(), 노드 수는 G.number_of_nodes()"],"after":"노드 수와 엣지 수는 모든 과제에서 가장 먼저 출력하는 두 숫자예요 (Solution_1 셀 6).","source":"PracticeCode_1 (3).ipynb 셀 16, 22, 26","think":["입력은 노드 1~6 이 든 그래프예요. 출력은 노드 목록, 엣지 목록, 노드 수, 엣지 수 네 줄이에요.","빈칸마다 필요한 이름을 나눠요. 엣지 넣기는 `add_edge`, 엣지 목록은 `edges()`, 노드 수는 `number_of_nodes()` 예요.","마지막 엣지 빈칸에는 두 노드를 쉼표로 적어요. 노트북 그래프는 6번과 2번을 이어요.","틀리기 쉬운 곳: `G.number_of_nodes` 처럼 괄호를 빼면 숫자 대신 `<bound method ...>` 가 찍혀요.","확인: `Number of nodes: 6` 과 `Number of edges: 7` 이 나오면 맞아요."],"pseudo":"빈 그래프 G 에 노드 1~6 을 넣는다\n정해진 두 노드 쌍마다 엣지를 하나씩 넣는다 (모두 7개)\n노드 목록과 엣지 목록을 꺼내 출력한다\n노드 수 n 과 엣지 수 e 를 세어 출력한다"},{"id":"w1-graph-03","title":"노드, 엣지 지우고 이웃과 차수 보기","level":"blank","run":"browser","goal":"앞 실습 그래프에서 4번 노드와 1-2 엣지를 지운 뒤, 1번의 이웃 목록과 차수를 구해요.","explain":["`G.remove_node(4)` 는 4번 점을 지워요. 4번에 붙은 선도 함께 사라져요.","`G.remove_edge(1,2)` 는 1번과 2번 사이 선만 지워요.","`G.adj[1]` 은 1번과 선으로 이어진 이웃들이에요. `list(...)` 로 감싸면 리스트가 돼요 (c02).","`G.degree[1]` 은 1번에 붙은 선의 개수, 즉 차수(Degree)예요. 노트북에서는 칸 마지막 줄이라 값이 바로 보였는데, 채점하려고 변수에 담았어요."],"show":"","given":"import networkx as nx\n\nG = nx.Graph()\nG.add_node(1)\nG.add_node(2)\nG.add_node(3)\nG.add_node(4)\nG.add_node(5)\nG.add_node(6)\nG.add_edge(1,2)\nG.add_edge(3,1)\nG.add_edge(2,4)\nG.add_edge(4,1)\nG.add_edge(5,1)\nG.add_edge(1,6)\nG.add_edge(6,2)\n\n# Remove node 4\nG.___(4)\nnode_list = G.nodes()\nprint(\"Node:\",node_list)\n\n# Remove edge between node 1 and 2\nG.remove_edge(___, ___)\nedge_list = G.edges()\nprint(\"Edge:\",edge_list)\n\n# Find neighbors of node 1\nnbrs = list(G.adj[___])\n# The node degree is the number of edges adjacent to the node\ndeg1 = G.___[1]\nprint(nbrs, deg1)","solution":"import networkx as nx\n\nG = nx.Graph()\nG.add_node(1)\nG.add_node(2)\nG.add_node(3)\nG.add_node(4)\nG.add_node(5)\nG.add_node(6)\nG.add_edge(1,2)\nG.add_edge(3,1)\nG.add_edge(2,4)\nG.add_edge(4,1)\nG.add_edge(5,1)\nG.add_edge(1,6)\nG.add_edge(6,2)\n\n# Remove node 4\nG.remove_node(4)\nnode_list = G.nodes()\nprint(\"Node:\",node_list)\n\n# Remove edge between node 1 and 2\nG.remove_edge(1,2)\nedge_list = G.edges()\nprint(\"Edge:\",edge_list)\n\n# Find neighbors of node 1\nnbrs = list(G.adj[1])\n# The node degree is the number of edges adjacent to the node\ndeg1 = G.degree[1]\nprint(nbrs, deg1)","tests":"assert 4 not in G, \"4번 노드를 G.remove_node(4) 로 지워야 해요\"\nassert not G.has_edge(1, 2), \"1번과 2번 사이 엣지를 G.remove_edge(1,2) 로 지워야 해요\"\nassert G.number_of_nodes() == 5 and G.number_of_edges() == 4, f\"노드 5개, 엣지 4개가 남아야 해요. 지금은 {G.number_of_nodes()}개, {G.number_of_edges()}개예요\"\nassert 'nbrs' in globals() and sorted(nbrs) == [3, 5, 6], \"nbrs 는 list(G.adj[1]) 로 구한 [3, 5, 6] 이어야 해요\"\nassert 'deg1' in globals() and deg1 == 3, \"deg1 은 G.degree[1] 로 구한 3 이어야 해요\"\n","hints":["지우는 메서드는 remove_ 로 시작해요","remove_edge 에는 양 끝 노드 두 개를 넣어요","이웃은 G.adj[1], 차수는 G.degree[1]"],"after":"이웃 목록 G.neighbors(u) 와 차수는 중심성, 랜덤워크, GCN 정규화까지 계속 나와요.","source":"PracticeCode_1 (3).ipynb 셀 24, 28, 30","think":["입력은 02 의 그래프(노드 6개, 엣지 7개)예요. 출력은 1번의 이웃 리스트 `nbrs` 와 차수 `deg1` 이에요.","손으로 먼저 풀어요. 1번의 이웃은 처음에 2, 3, 4, 5, 6 이에요.","4번 노드를 지우면 이웃에서 4 가 빠지고, 1-2 엣지를 지우면 2 도 빠져요. 남는 이웃은 [3, 5, 6], 차수는 3 이에요.","틀리기 쉬운 곳: 노드를 지우면 붙어 있던 엣지도 같이 사라져요. 1-4, 2-4 엣지를 따로 지울 필요가 없어요.","확인: 마지막 줄에 `[3, 5, 6] 3` 이 찍히면 맞아요."],"pseudo":"노드 1~6 과 엣지 7개로 그래프 G 를 만든다\n노드 4 를 지운다 (4 에 붙은 엣지도 사라진다)\n노드 1 과 2 사이 엣지를 지운다\n1번의 이웃을 리스트로 모아 nbrs 에 담는다\n1번의 차수를 deg1 에 담는다\nnbrs 와 deg1 을 출력한다"},{"id":"w1-graph-04","title":"방향 그래프의 들어오는 차수, 나가는 차수","level":"type","run":"browser","goal":"화살표가 있는 그래프를 만들고 노드마다 들어오는 선 수와 나가는 선 수를 출력해요.","explain":["`nx.DiGraph()` 는 방향 그래프예요. (1, 2) 는 1 에서 2 로 가는 화살표 하나예요.","`G.add_edges_from([...])` 는 엣지 리스트를 한 번에 넣어요. 괄호 안이 여러 줄이어도 괄호가 닫힐 때까지 한 문장이에요.","`G.in_degree()` 는 들어오는 화살표 수, `G.out_degree` 는 나가는 화살표 수예요. `dict(...)` 로 {노드: 수} 딕셔너리로 바꿔요 (c02).","노트북의 그림 줄 `nx.draw_networkx(...)` 는 브라우저에서 그림 모듈을 못 써서 뺐어요. 노트북에서는 그대로 두면 돼요."],"show":"import networkx as nx\n\nG = nx.DiGraph()\nG.add_edges_from([(1, 2), (1, 4),\n                  (2, 3), (2, 4),\n                  (3, 5),\n                  (4, 3),\n                  (5, 4), (5, 1)])\nprint(\"Number of edges: \", int(G.number_of_edges()))\nprint(\"List of nodes: \", list(G.nodes()))\nprint(\"In degree for nodes: \", dict(G.in_degree()))\nprint(\"Out degree for nodes: \", dict(G.out_degree))","given":"","solution":"import networkx as nx\n\nG = nx.DiGraph()\nG.add_edges_from([(1, 2), (1, 4),\n                  (2, 3), (2, 4),\n                  (3, 5),\n                  (4, 3),\n                  (5, 4), (5, 1)])\nprint(\"Number of edges: \", int(G.number_of_edges()))\nprint(\"List of nodes: \", list(G.nodes()))\nprint(\"In degree for nodes: \", dict(G.in_degree()))\nprint(\"Out degree for nodes: \", dict(G.out_degree))","tests":"assert 'G' in globals() and G.is_directed(), \"G 는 nx.DiGraph() 로 만든 방향 그래프여야 해요\"\nassert G.number_of_edges() == 8, f\"화살표는 8개여야 해요. 지금은 {G.number_of_edges()}개예요\"\nassert dict(G.in_degree()) == {1: 1, 2: 1, 4: 3, 3: 2, 5: 1}, \"엣지 리스트를 노트북과 똑같이 넣었는지 확인해요\"\nassert \"In degree for nodes:  {1: 1, 2: 1, 4: 3, 3: 2, 5: 1}\" in __out__, \"In degree 줄이 노트북과 같게 출력되어야 해요\"\nassert \"Out degree for nodes:  {1: 2, 2: 2, 4: 1, 3: 1, 5: 2}\" in __out__, \"Out degree 줄이 노트북과 같게 출력되어야 해요\"\n","hints":["방향 그래프는 nx.DiGraph()","add_edges_from 괄호 안에 리스트를 통째로","dict(G.in_degree()) 와 dict(G.out_degree)"],"after":"PageRank 는 방향 그래프에서 들어오는 선을 따라 점수를 모아요 (Sample_Code_1 셀 19).","source":"PracticeCode_1 (3).ipynb 셀 34","think":["이 코드가 만드는 것: 화살표 8개짜리 방향 그래프와, 노드마다 들어오는 수, 나가는 수 딕셔너리예요.","순서대로 읽어요. `nx.DiGraph()` 로 방향 그래프를 만들고, 엣지 리스트를 한 번에 넣고, 개수와 차수를 찍어요.","하나만 손으로 세 봐요. 4번으로 들어오는 화살표는 (1, 4), (2, 4), (5, 4) 세 개라서 들어오는 차수가 3 이에요.","틀리기 쉬운 곳: 방향 그래프에서 `(1, 2)` 와 `(2, 1)` 은 서로 다른 화살표예요. 앞이 출발, 뒤가 도착이에요.","확인: `In degree for nodes:` 줄에서 4 옆에 3 이 보이면 맞아요."],"pseudo":"빈 방향 그래프 G 를 만든다\n(출발, 도착) 화살표 8개를 한 번에 넣는다\n엣지 수와 노드 목록을 출력한다\n노드마다 들어오는 화살표 수를 딕셔너리로 출력한다\n노드마다 나가는 화살표 수를 딕셔너리로 출력한다"},{"id":"w1-graph-05","title":"인접행렬과 차수, 엣지 수","level":"type","run":"browser","goal":"엣지 리스트로 그래프를 만들고 인접행렬 A 를 꺼내요. A 의 열 합과 행 합이 차수이고, 차수 합의 절반이 엣지 수예요.","explain":["`nx.adjacency_matrix(G).todense()` 는 인접행렬을 numpy 표로 꺼내요. A[i, j] 가 1 이면 i 번째와 j 번째 노드가 이어져 있어요 (c07, c08).","행과 열 순서는 **노드가 처음 등장한 순서** [1, 2, 5, 3, 4] 예요. 그래서 첫 줄 [0 1 1 0 0] 은 1번이 2번, 5번과 이어졌다는 뜻이에요.","`A.sum(axis=0)` 은 열마다 더하기, `axis=1` 은 행마다 더하기예요. 둘 다 차수 $k_i$ 가 나와요.","엣지 하나가 차수를 두 번 올리니까 $L = \\frac{1}{2}\\sum_i k_i$ 예요. 그 식이 `L = 0.5 * ki.sum()` 이에요.","지금 networkx 에서는 ki 가 `[2 3 2 2 3]` 로 찍혀요. 노트북 출력의 `[[2 3 2 2 3]]` 은 옛 버전 모양이고 값은 같아요. 그림 줄은 브라우저라 뺐어요."],"show":"import networkx as nx\n\nedge_list = [(1, 2), (1, 5), (2, 3), (4, 3), (4,5), (2,4)]\nG = nx.Graph(edge_list)\nA = nx.adjacency_matrix(G).todense()\nki = A.sum(axis=0).flatten()\nkj = A.sum(axis=1).flatten()\nL = 0.5 * ki.sum()\nprint(f\"\\nA:\\n{A}\")\nprint(f\"\\nki: {ki}\")\nprint(f\"kj: {kj}\")\nprint(f\"# of edges (L): {L}\")","given":"","solution":"import networkx as nx\n\nedge_list = [(1, 2), (1, 5), (2, 3), (4, 3), (4,5), (2,4)]\nG = nx.Graph(edge_list)\nA = nx.adjacency_matrix(G).todense()\nki = A.sum(axis=0).flatten()\nkj = A.sum(axis=1).flatten()\nL = 0.5 * ki.sum()\nprint(f\"\\nA:\\n{A}\")\nprint(f\"\\nki: {ki}\")\nprint(f\"kj: {kj}\")\nprint(f\"# of edges (L): {L}\")","tests":"import numpy as np\nassert 'A' in globals() and np.asarray(A).shape == (5, 5), \"A 는 5x5 인접행렬이어야 해요\"\nassert np.allclose(np.asarray(ki).ravel(), [2, 3, 2, 2, 3]), f\"ki 는 [2 3 2 2 3] 이어야 해요. 지금은 {ki} 이에요\"\nassert np.allclose(np.asarray(kj).ravel(), [2, 3, 2, 2, 3]), \"kj 는 A.sum(axis=1).flatten() 이어야 해요\"\nassert abs(float(L) - 6.0) < 1e-9, f\"L 은 6.0 이어야 해요. 지금은 {L} 이에요\"\nassert \"# of edges (L): 6.0\" in __out__, \"마지막 줄에 # of edges (L): 6.0 이 출력되어야 해요\"\n","hints":["nx.Graph(edge_list) 로 한 번에 만들어요","A.sum(axis=0) 은 열 합","L = 0.5 * ki.sum()"],"after":"GCN 의 차수 행렬 D 는 바로 이 A.sum(axis=1) 로 만들어요 (2주차).","source":"PracticeCode_1 (3).ipynb 셀 38","think":["입력은 엣지 6개 리스트예요. 출력은 5x5 인접행렬 `A`, 차수 `ki`, `kj`, 엣지 수 `L` 이에요.","행과 열 순서는 노드가 처음 나온 순서 [1, 2, 5, 3, 4] 예요. 그래서 A 의 둘째 줄이 2번 노드예요.","손으로 풀어요. 2번은 1, 3, 4 와 이어져서 차수 3 이에요. 모든 차수를 더하면 2 + 3 + 2 + 2 + 3 = 12 예요.","엣지 하나는 양 끝 두 노드의 차수를 하나씩 올려요. 그래서 12 의 절반인 6 이 엣지 수예요.","확인: `ki: [2 3 2 2 3]` 과 `# of edges (L): 6.0` 이 나오면 맞아요."],"pseudo":"엣지 리스트로 무방향 그래프 G 를 만든다\nG 의 인접행렬 A 를 numpy 표로 꺼낸다\nA 를 열마다 더해 ki, 행마다 더해 kj 를 만든다\nki 의 합을 반으로 나눠 엣지 수 L 을 구한다\nA, ki, kj, L 을 출력한다"},{"id":"w1-graph-06","title":"가장 큰 덩어리와 외톨이 노드","level":"blank","run":"browser","goal":"선으로 이어진 덩어리(연결 요소)를 크기 순으로 줄 세우고, 가장 큰 덩어리와 혼자 떨어진 노드를 찾아요.","explain":["서로 선을 따라 오갈 수 있는 노드 무리, 이걸 연결 요소(Connected Component)라고 해요. `nx.connected_components(G)` 가 무리마다 집합(set)을 하나씩 줘요 (c02 의 set).","`sorted(..., key=len, reverse=True)` 는 길이(len)를 기준으로 큰 것부터 줄 세워요. `key=len` 은 함수 이름을 값처럼 넘기는 거예요 (c04).","가장 큰 무리(Giant Component)는 줄의 맨 앞, 인덱스 0 이에요.","`[cc for cc in ... if len(cc) == 1]` 은 조건 붙은 리스트 컴프리헨션이에요 (c03). 크기 1 인 무리가 외톨이 노드예요."],"show":"","given":"import networkx as nx\n\nedge_list = [(\"A\", \"B\"), (\"A\", \"C\"), (\"A\", \"D\"), (\"B\", \"C\"), (\"F\", \"G\")]\nG = nx.Graph(edge_list)\nG.add_node(___)\nconnected_components = sorted(nx.connected_components(G), key=___, reverse=___)\ngiant_component = connected_components[___]\nisolated_nodes = [cc for cc in connected_components if len(cc) == ___]\n\nprint(f\"Giant Component: {giant_component}\")\nprint(f\"Isolated nodes: {isolated_nodes}\")","solution":"import networkx as nx\n\nedge_list = [(\"A\", \"B\"), (\"A\", \"C\"), (\"A\", \"D\"), (\"B\", \"C\"), (\"F\", \"G\")]\nG = nx.Graph(edge_list)\nG.add_node(\"H\")\nconnected_components = sorted(nx.connected_components(G), key=len, reverse=True)\ngiant_component = connected_components[0]\nisolated_nodes = [cc for cc in connected_components if len(cc) == 1]\n\nprint(f\"Giant Component: {giant_component}\")\nprint(f\"Isolated nodes: {isolated_nodes}\")","tests":"assert 'G' in globals() and \"H\" in G, \"G.add_node(\\\"H\\\") 로 외톨이 노드 H 를 넣어야 해요\"\nassert 'giant_component' in globals() and giant_component == {\"A\", \"B\", \"C\", \"D\"}, f\"가장 큰 덩어리는 A, B, C, D 예요. 지금은 {giant_component} 이에요. reverse=True 와 인덱스 0 을 확인해요\"\nassert 'isolated_nodes' in globals() and isolated_nodes == [{\"H\"}], f\"외톨이는 [{{'H'}}] 여야 해요. 지금은 {isolated_nodes} 이에요\"\n","hints":["외톨이 노드 이름은 \"H\" (따옴표 필수)","크기 기준은 key=len, 큰 것부터는 reverse=True","맨 앞은 [0], 외톨이는 len(cc) == 1"],"after":"실제 데이터셋은 덩어리가 여러 개라서, 가장 큰 덩어리만 남기고 학습하는 경우가 많아요.","source":"PracticeCode_1 (3).ipynb 셀 44","think":["입력은 엣지 5개와 따로 넣을 노드 \"H\" 예요. 출력은 가장 큰 덩어리(집합 하나)와 외톨이 덩어리 리스트예요.","손으로 풀어요. A, B, C, D 가 한 덩어리, F, G 가 한 덩어리, H 는 혼자예요. 크기는 4, 2, 1 이에요.","크기 순으로 줄 세우면 맨 앞(인덱스 0)이 가장 큰 덩어리예요. 크기가 1 인 것만 골라내면 외톨이예요.","틀리기 쉬운 곳: `key=len` 에는 괄호를 붙이지 않고 함수 이름만 넘겨요. `reverse=True` 를 빼면 작은 것부터 서요.","확인: `Giant Component` 에 A, B, C, D 네 개, `Isolated nodes` 에 `[{'H'}]` 가 나오면 맞아요."],"pseudo":"엣지 리스트로 그래프 G 를 만든다\n엣지가 없는 노드 H 를 따로 넣는다\n연결 요소들을 크기가 큰 것부터 줄 세운다\n맨 앞 덩어리를 giant_component 에 담는다\n크기가 1 인 덩어리만 모아 isolated_nodes 에 담는다\n두 결과를 출력한다"},{"id":"w1-graph-07","title":"가중치 있는 최단 경로","level":"blank","run":"browser","goal":"엣지마다 거리(가중치)가 있는 방향 그래프에서 0번에서 9번까지 거리 합이 가장 짧은 길을 찾아요.","explain":["`G.add_weighted_edges_from(edges)` 는 (출발, 도착, 가중치) 세 칸짜리 튜플 리스트를 넣어요. 가중치는 `'weight'` 라는 이름으로 저장돼요.","`nx.shortest_path(..., weight='weight', method=\"dijkstra\")` 는 가중치 합이 가장 작은 경로를 노드 리스트로 줘요.","`\"->\".join(map(str, path))` : `map(str, path)` 가 숫자를 하나씩 문자열로 바꾸고, `join` 이 사이에 -> 를 넣어 붙여요.","`zip(path, path[1:])` 는 리스트를 한 칸 밀린 자기 자신과 짝지어요 (c03 의 zip, c02 의 슬라이싱). 경로 위 엣지 목록이 돼요.","노트북의 그림 줄(pos, draw_networkx ...)은 브라우저라 뺐어요."],"show":"","given":"import networkx as nx\n\nG = nx.DiGraph()\nedges = [(0, 1, 2.1), (0, 2, 1.5), (0, 3, 2), (0, 5, 10), (1, 3, 1.6), (1, 6, 1.4), (2, 3, 4), (3, 4, 2.5), (4, 5, 1.8), (4, 7, 2.3), (5, 8, 1.5), (8, 9, 1.9)]\nG.add_weighted_edges_from(___)\n\nsource = 0\ntarget = 9\npath = nx.shortest_path(G,source=source,target=target, weight=___, method=\"dijkstra\")\n\nprint(f\"Shortest path from {source} -> {target}: \")\nprint(\"->\".join(map(str, ___)))\n\npath_edges = list(zip(path,path[___:]))\nlabels = nx.get_edge_attributes(G,'weight')","solution":"import networkx as nx\n\nG = nx.DiGraph()\nedges = [(0, 1, 2.1), (0, 2, 1.5), (0, 3, 2), (0, 5, 10), (1, 3, 1.6), (1, 6, 1.4), (2, 3, 4), (3, 4, 2.5), (4, 5, 1.8), (4, 7, 2.3), (5, 8, 1.5), (8, 9, 1.9)]\nG.add_weighted_edges_from(edges)\n\nsource = 0\ntarget = 9\npath = nx.shortest_path(G,source=source,target=target, weight='weight', method=\"dijkstra\")\n\nprint(f\"Shortest path from {source} -> {target}: \")\nprint(\"->\".join(map(str, path)))\n\npath_edges = list(zip(path,path[1:]))\nlabels = nx.get_edge_attributes(G,'weight')","tests":"assert 'G' in globals() and G.number_of_edges() == 12, \"엣지 12개를 add_weighted_edges_from(edges) 로 넣어야 해요\"\nassert 'path' in globals() and path == [0, 3, 4, 5, 8, 9], f\"최단 경로는 [0, 3, 4, 5, 8, 9] 예요. 지금은 {path} 이에요. weight='weight' 를 넣었는지 확인해요\"\nassert \"0->3->4->5->8->9\" in __out__, \"\\\"->\\\".join(map(str, path)) 로 0->3->4->5->8->9 를 출력해야 해요\"\nassert 'path_edges' in globals() and path_edges == [(0, 3), (3, 4), (4, 5), (5, 8), (8, 9)], f\"path_edges 는 이웃한 두 노드 짝이에요. 지금은 {path_edges} 이에요. path[1:] 을 확인해요\"\nassert 'labels' in globals() and labels[(0, 5)] == 10, \"labels 는 nx.get_edge_attributes(G,'weight') 예요\"\n","hints":["가중치 엣지는 add_weighted_edges_from","weight 에는 문자열 'weight'","join 에 넣을 것은 path, 한 칸 밀기는 path[1:]"],"after":"가중치가 없으면 weight 를 빼요. 그러면 0->5->8->9 처럼 선 개수가 가장 적은 길이 나와요.","source":"PracticeCode_1 (3).ipynb 셀 88","think":["입력은 (출발, 도착, 거리) 12개와 출발 0, 도착 9 예요. 출력은 거리 합이 가장 작은 경로의 노드 리스트 `path` 예요.","뒤에서부터 봐요. 9 로 오는 화살표는 8 에서만, 8 로 오는 화살표는 5 에서만 와요. 그래서 경로는 5, 8, 9 로 끝나요.","0 에서 5 까지는 바로 가면 10, 0-3-4-5 로 돌아가면 2 + 2.5 + 1.8 = 6.3 이에요. 돌아가는 쪽이 더 짧아요.","틀리기 쉬운 곳: `weight='weight'` 를 빼면 거리 대신 엣지 개수로 세서 0-5-8-9 가 나와요.","확인: `0->3->4->5->8->9` 가 찍히면 맞아요."],"pseudo":"빈 방향 그래프 G 에 가중치 엣지를 한꺼번에 넣는다\n출발 source 는 0, 도착 target 은 9 로 정한다\n가중치 합이 가장 작은 경로 path 를 다익스트라로 구한다\npath 의 숫자를 문자열로 바꿔 -> 로 이어 출력한다\npath 와 한 칸 밀린 path 를 짝지어 경로 위 엣지 목록을 만든다\n엣지마다 가중치를 꺼내 labels 에 담는다"},{"id":"w1-graph-08","title":"평균 차수가 두 배로 나와요","level":"fix","run":"browser","goal":"평균 차수는 2.00 이 나와야 하는데 4.00 이 나와요. 한 곳을 고쳐요.","explain":["무방향 그래프의 인접행렬에서 엣지 하나는 A[i, j] 와 A[j, i] 두 칸에 1 로 들어가요.","그래서 `A.sum()` 은 엣지 수의 **두 배**예요. 엣지 수 $E$ 는 그 절반이에요.","평균 차수는 $\\frac{2E}{n}$ 이에요. n 은 노드 수예요."],"show":"","given":"import networkx as nx\n\nedge_list = [(1, 2), (1, 3), (2, 3), (2, 4)]\nG = nx.Graph(edge_list)\nA = nx.adjacency_matrix(G).todense()\nE = A.sum()\navg_degree = 2 * E / G.number_of_nodes()\n\nprint(f\"\\nA:\\n{A}\")\nprint(f\"\\nAvg. degree: {avg_degree:.2f}\")","solution":"import networkx as nx\n\nedge_list = [(1, 2), (1, 3), (2, 3), (2, 4)]\nG = nx.Graph(edge_list)\nA = nx.adjacency_matrix(G).todense()\nE = A.sum() / 2\navg_degree = 2 * E / G.number_of_nodes()\n\nprint(f\"\\nA:\\n{A}\")\nprint(f\"\\nAvg. degree: {avg_degree:.2f}\")","tests":"assert 'E' in globals() and abs(float(E) - 4) < 1e-9, f\"엣지 수 E 는 4 여야 해요. 지금은 {E} 이에요. 인접행렬 합은 엣지를 두 번 세요\"\nassert abs(float(avg_degree) - 2.0) < 1e-9, f\"평균 차수는 2.0 이어야 해요. 지금은 {avg_degree} 이에요\"\nassert \"Avg. degree: 2.00\" in __out__, \"Avg. degree: 2.00 이 출력되어야 해요\"\n","hints":["E 를 구하는 줄을 보세요","A.sum() 은 엣지를 몇 번 세나요?","E = A.sum() / 2"],"after":"가중치 그래프(셀 55)에서는 같은 식이 가중치 합의 평균, 3.75 가 돼요.","source":"PracticeCode_1 (3).ipynb 셀 53","think":["증상부터 나눠요. 에러는 없고 값만 틀려요. 2.00 이어야 하는데 4.00, 딱 두 배예요.","손으로 풀어요. 엣지 4개, 노드 4개라서 평균 차수는 2 x 4 / 4 = 2 예요.","중간값을 print 해 봐요. `print(E)` 를 넣으면 4 가 아니라 8 이 나와요. E 를 만드는 줄이 틀렸어요.","이유: 무방향 인접행렬은 엣지 하나를 A[i, j] 와 A[j, i] 두 칸에 적어요. 그래서 `A.sum()` 을 2 로 나눠야 엣지 수예요.","확인: 고친 뒤 `Avg. degree: 2.00` 이 나오면 맞아요."],"pseudo":"엣지 리스트로 그래프 G 를 만든다\n인접행렬 A 를 꺼낸다\nA 의 모든 칸 합을 2 로 나눠 엣지 수 E 를 구한다\n2 x E 를 노드 수로 나눠 평균 차수를 구한다\nA 와 평균 차수를 출력한다"},{"id":"w1-graph-09","title":"과제 풀이의 출력 칸 고치기","level":"fix","run":"browser","goal":"과제 1 Exercise 2 풀이에서 엣지 목록과 차수를 출력하는 칸이에요. 초보가 자주 하는 실수 두 개가 숨어 있어요.","explain":["에러가 나는 건 정상이에요. 메시지 마지막 줄 `SyntaxError` 와 줄 번호부터 읽어요.","`for ... in ...` 줄 끝에는 콜론(:)이 있어야 하고, 반복할 줄은 스페이스 4칸 들여써요 (c03).","`enumerate(목록, 1)` 은 번호를 0 이 아니라 1 부터 붙여요 (c03)."],"show":"","given":"import networkx as nx\n\nedges_ex2 = [(1, 2), (1, 4), (2, 3), (2, 5), (3, 6), (4, 5), (4, 7), (5, 6), (5, 8), (6, 9), (7, 8), (8, 9)]\nG2 = nx.Graph()\nG2.add_edges_from(edges_ex2)\n\n# 3. Edge List\nprint(\"\\n=== Edge List ===\")\nfor idx, edge in enumerate(sorted(list(G2.edges())), 1)\n    print(f\"Edge {idx}: {edge}\")\n\n# 4. Node Degrees\nprint(\"\\n=== Node Degrees ===\")\nfor node in sorted(G2.nodes()):\nprint(f\"Node {node}: Degree = {G2.degree[node]}\")","solution":"import networkx as nx\n\nedges_ex2 = [(1, 2), (1, 4), (2, 3), (2, 5), (3, 6), (4, 5), (4, 7), (5, 6), (5, 8), (6, 9), (7, 8), (8, 9)]\nG2 = nx.Graph()\nG2.add_edges_from(edges_ex2)\n\n# 3. Edge List\nprint(\"\\n=== Edge List ===\")\nfor idx, edge in enumerate(sorted(list(G2.edges())), 1):\n    print(f\"Edge {idx}: {edge}\")\n\n# 4. Node Degrees\nprint(\"\\n=== Node Degrees ===\")\nfor node in sorted(G2.nodes()):\n    print(f\"Node {node}: Degree = {G2.degree[node]}\")","tests":"assert \"Edge 1: (1, 2)\" in __out__ and \"Edge 12: (8, 9)\" in __out__, \"Edge 1 부터 Edge 12 까지 출력되어야 해요\"\nassert __out__.count(\"Degree =\") == 9, f\"노드 9개의 차수가 한 줄씩 나와야 해요. 지금은 {__out__.count('Degree =')}줄이에요. print 줄 들여쓰기를 확인해요\"\nassert \"Node 5: Degree = 4\" in __out__, \"Node 5: Degree = 4 가 출력되어야 해요\"\n","hints":["for 줄 두 개를 비교해 보세요","첫 번째 for 줄 끝에 무엇이 빠졌나요?","두 번째 for 아래 print 는 4칸 들여쓰기"],"after":"과제는 에러 없이 실행된 노트북만 받아요. 콜론과 들여쓰기가 가장 흔한 감점 원인이에요.","source":"Solution_1.ipynb 셀 8","think":["에러 메시지 마지막 줄부터 봐요. `SyntaxError: expected ':'` 와 줄 번호가 보여요.","그 줄로 가요. `for idx, edge in enumerate(...)` 끝에 콜론이 없어요. 콜론을 붙여요.","다시 실행하면 이번엔 `IndentationError` 가 나요. 두 번째 `for` 아래 `print` 줄이 들여쓰기가 안 돼 있어요. 스페이스 4칸을 넣어요.","에러는 한 번에 하나씩 보여요. 하나 고치고 다시 실행하는 게 정상이에요.","확인: `Edge 1: (1, 2)` 부터 `Edge 12: (8, 9)` 까지, 그리고 `Node 5: Degree = 4` 가 보이면 맞아요."],"pseudo":"엣지 12개로 그래프 G2 를 만든다\n\"=== Edge List ===\" 제목을 출력한다\n정렬한 엣지마다 1 부터 번호를 붙여\n    번호와 엣지를 출력한다\n\"=== Node Degrees ===\" 제목을 출력한다\n정렬한 노드마다\n    노드 번호와 차수를 출력한다"},{"id":"w1-graph-10","title":"평균 차수 함수 직접 짜기","level":"write","run":"browser","goal":"엣지 리스트를 받아 평균 차수를 돌려주는 함수 avg_degree(edge_list) 를 만들어요. 가중치 엣지도 그대로 받아요.","explain":["셀 53 과 55 의 세 줄(A, E, avg_degree)을 함수 안으로 옮기는 거예요. 함수는 c04 에서 배운 `def` 와 `return` 이에요.","`nx.Graph(edge_list)` 는 (1, 2) 도, (1, 2, {\"weight\": 2}) 도 받아요. 가중치가 있으면 인접행렬 칸에 가중치가 들어가요."],"show":"","given":"import networkx as nx\n\n# 1. edge_list 로 그래프 G 를 만들어요: nx.Graph(edge_list)\n# 2. A = nx.adjacency_matrix(G).todense() 로 인접행렬을 꺼내요\n# 3. E = A.sum() / 2 로 엣지 수(가중치면 가중치 합)를 구해요\n# 4. 2 * E / 노드 수 를 return 해요\ndef avg_degree(edge_list):\n    ...","solution":"import networkx as nx\n\ndef avg_degree(edge_list):\n    G = nx.Graph(edge_list)\n    A = nx.adjacency_matrix(G).todense()\n    E = A.sum() / 2\n    return 2 * E / G.number_of_nodes()","tests":"assert 'avg_degree' in globals(), \"avg_degree 함수를 만들어야 해요\"\nr = avg_degree([(1, 2), (1, 3), (2, 3), (2, 4)])\nassert r is not None, \"return 으로 값을 돌려줘야 해요\"\nassert abs(float(r) - 2.0) < 1e-9, f\"[(1, 2), (1, 3), (2, 3), (2, 4)] 의 평균 차수는 2.0 이에요. 지금은 {r} 이에요\"\nr = avg_degree([(1, 2, {\"weight\": 2}), (1, 3, {\"weight\": 0.5}), (2, 3, {\"weight\": 1}), (2, 4, {\"weight\": 4})])\nassert abs(float(r) - 3.75) < 1e-9, f\"셀 55 가중치 그래프는 3.75 예요. 지금은 {r} 이에요\"\nr = avg_degree([(1, 2), (1, 5), (2, 3), (4, 3), (4, 5), (2, 4)])\nassert abs(float(r) - 2.4) < 1e-9, f\"셀 38 그래프는 2.4 예요. 지금은 {r} 이에요\"\n","hints":["함수 첫 줄에서 G 를 만들어요","A 를 꺼내고 E = A.sum() / 2","return 2 * E / G.number_of_nodes()"],"after":"평균 차수는 그래프가 얼마나 빽빽한지 한 숫자로 보여 줘요. 데이터셋 소개 표에 늘 나와요.","source":"PracticeCode_1 (3).ipynb 셀 53, 55","think":["입력은 엣지 리스트 `edge_list`, 출력은 평균 차수 숫자 하나예요. `(1, 2, {\"weight\": 2})` 같은 가중치 엣지도 들어올 수 있어요.","손으로 풀어요. [(1, 2), (1, 3), (2, 3), (2, 4)] 면 엣지 4개, 노드 4개라서 2 x 4 / 4 = 2.0 이에요.","08 에서 고친 세 줄(그래프, 인접행렬, E)을 함수 안으로 옮기고, 마지막 계산 값을 돌려주면 돼요.","틀리기 쉬운 곳: `print` 가 아니라 `return` 이에요. `A.sum()` 을 2 로 나누는 것도 잊지 마요.","확인: `print(avg_degree([(1, 2), (1, 3), (2, 3), (2, 4)]))` 가 2.0 이면 맞아요."],"pseudo":"함수 avg_degree 가 edge_list 를 받는다\n    edge_list 로 그래프 G 를 만든다\n    인접행렬 A 를 꺼낸다\n    A 의 모든 칸 합을 2 로 나눠 E 에 담는다\n    2 x E 를 노드 수로 나눈 값을 돌려준다"},{"id":"w1-graph-11","title":"과제 Exercise 2 를 함수로","level":"write","run":"browser","goal":"엣지 리스트를 받아 (정렬된 노드 리스트, 인접행렬, 차수 딕셔너리) 세 개를 돌려주는 함수 adjacency_and_degrees(edges) 를 만들어요.","explain":["Solution_1 셀 8 의 2번(인접행렬)과 4번(차수)을 출력 대신 return 으로 바꾼 거예요.","`nx.adjacency_matrix(G, nodelist=nodes)` 처럼 nodelist 를 주면 행과 열이 그 순서로 만들어져요. 정렬해서 주면 1, 2, 3 순서가 돼요.","여러 값을 돌려줄 때는 `return a, b, c` 로 쓰고 받을 때 `x, y, z = 함수(...)` 로 풀어요 (c04).","차수 딕셔너리는 `{node: G.degree[node] for node in nodes}` 처럼 만들 수 있어요 (c03 컴프리헨션의 딕셔너리판)."],"show":"","given":"import networkx as nx\n\n# 1. G2 = nx.Graph() 를 만들고 G2.add_edges_from(edges) 로 엣지를 넣어요\n# 2. nodes = 노드를 정렬한 리스트 (sorted)\n# 3. adj_matrix = nodelist=nodes 를 준 인접행렬 (.todense())\n# 4. degrees = {노드: 차수} 딕셔너리\n# 5. nodes, adj_matrix, degrees 를 return 해요\ndef adjacency_and_degrees(edges):\n    ...","solution":"import networkx as nx\n\ndef adjacency_and_degrees(edges):\n    G2 = nx.Graph()\n    G2.add_edges_from(edges)\n    nodes = sorted(list(G2.nodes()))\n    adj_matrix = nx.adjacency_matrix(G2, nodelist=nodes).todense()\n    degrees = {node: G2.degree[node] for node in nodes}\n    return nodes, adj_matrix, degrees","tests":"import numpy as np\nassert 'adjacency_and_degrees' in globals(), \"adjacency_and_degrees 함수를 만들어야 해요\"\nedges_ex2 = [(1, 2), (1, 4), (2, 3), (2, 5), (3, 6), (4, 5), (4, 7), (5, 6), (5, 8), (6, 9), (7, 8), (8, 9)]\nr = adjacency_and_degrees(edges_ex2)\nassert r is not None and len(r) == 3, \"return nodes, adj_matrix, degrees 로 세 값을 돌려줘야 해요\"\nnodes, M, deg = r\nassert list(nodes) == list(range(1, 10)), f\"nodes 는 정렬된 [1, ..., 9] 여야 해요. 지금은 {nodes} 이에요\"\nM = np.asarray(M)\nassert M.shape == (9, 9), f\"인접행렬은 9x9 여야 해요. 지금은 {M.shape} 이에요\"\nassert M[0, 1] == 1 and M[0, 3] == 1 and M[0, 2] == 0, \"1번 줄은 2번, 4번과만 이어져야 해요. nodelist=nodes 를 넣었는지 확인해요\"\nassert deg == {1: 2, 2: 3, 3: 2, 4: 3, 5: 4, 6: 3, 7: 2, 8: 3, 9: 2}, f\"차수 딕셔너리가 달라요: {deg}\"\nnodes, M, deg = adjacency_and_degrees([(3, 1), (2, 3), (0, 3)])\nM = np.asarray(M)\nassert list(nodes) == [0, 1, 2, 3] and M[3].tolist() == [1, 1, 1, 0], \"다른 그래프에서도 정렬 순서로 행렬을 만들어야 해요\"\nassert deg == {0: 1, 1: 1, 2: 1, 3: 3}, f\"[(3, 1), (2, 3), (0, 3)] 의 차수가 달라요: {deg}\"\nassert np.allclose(M.sum(axis=1), [deg[u] for u in nodes]), \"행 합이 차수와 같아야 해요\"\n","hints":["G2 = nx.Graph() 후 add_edges_from","nodes = sorted(list(G2.nodes()))","adj_matrix 는 nodelist=nodes, degrees 는 딕셔너리 컴프리헨션","return nodes, adj_matrix, degrees"],"after":"과제에서는 이 세 값을 보기 좋게 print 만 하면 Exercise 2 가 끝나요.","source":"Solution_1.ipynb 셀 8","think":["입력은 엣지 리스트 `edges`, 출력은 세 개예요. 정렬된 노드 리스트, 인접행렬, {노드: 차수} 딕셔너리.","손으로 풀어요. [(2, 1), (1, 3)] 이면 nodes 는 [1, 2, 3] 이고, 1번 차수 2, 2번과 3번 차수 1 이에요.","행렬의 줄 순서가 nodes 와 같아야 해요. 그래서 인접행렬을 만들 때 `nodelist=nodes` 를 넘겨요.","틀리기 쉬운 곳: nodelist 를 빼면 노드가 처음 나온 순서 [2, 1, 3] 으로 줄이 서서 채점과 달라져요.","확인: `nodes, A, deg = adjacency_and_degrees([(2, 1), (1, 3)])` 로 받아 print 해요. A 첫 줄이 [0 1 1] 이면 맞아요."],"pseudo":"빈 그래프 G2 를 만들고 edges 를 넣는다\n노드를 정렬해 nodes 리스트로 만든다\nnodes 순서대로 줄을 세운 인접행렬 adj_matrix 를 꺼낸다\n빈 딕셔너리 degrees 를 만든다\nnodes 의 노드마다\n    degrees 에 그 노드의 차수를 적는다\nnodes, adj_matrix, degrees 세 개를 돌려준다"},{"id":"w1-graph-12","title":"가장 큰 덩어리와 외톨이 함수","level":"write","run":"browser","goal":"엣지 리스트와 따로 넣을 노드 리스트를 받아 (가장 큰 연결 요소, 외톨이 노드 리스트) 를 돌려주는 함수 giant_and_isolated(edge_list, extra_nodes) 를 만들어요.","explain":["셀 44 를 함수로 옮기는 과제예요. 외톨이 노드는 엣지 리스트에 없으니 `G.add_node(...)` 로 따로 넣어야 해요.","`for node in extra_nodes:` 로 하나씩 넣어도 되고, `G.add_nodes_from(extra_nodes)` 로 한 번에 넣어도 돼요."],"show":"","given":"import networkx as nx\n\n# 1. edge_list 로 그래프 G 를 만들어요\n# 2. extra_nodes 의 노드를 하나씩 G 에 넣어요\n# 3. 연결 요소를 크기가 큰 순서로 정렬해요 (sorted, key=len, reverse=True)\n# 4. giant_component = 맨 앞 덩어리, isolated_nodes = 크기 1 인 덩어리 리스트\n# 5. 두 값을 return 해요\ndef giant_and_isolated(edge_list, extra_nodes):\n    ...","solution":"import networkx as nx\n\ndef giant_and_isolated(edge_list, extra_nodes):\n    G = nx.Graph(edge_list)\n    for node in extra_nodes:\n        G.add_node(node)\n    connected_components = sorted(nx.connected_components(G), key=len, reverse=True)\n    giant_component = connected_components[0]\n    isolated_nodes = [cc for cc in connected_components if len(cc) == 1]\n    return giant_component, isolated_nodes","tests":"assert 'giant_and_isolated' in globals(), \"giant_and_isolated 함수를 만들어야 해요\"\nr = giant_and_isolated([(\"A\", \"B\"), (\"A\", \"C\"), (\"A\", \"D\"), (\"B\", \"C\"), (\"F\", \"G\")], [\"H\"])\nassert r is not None and len(r) == 2, \"return giant_component, isolated_nodes 로 두 값을 돌려줘야 해요\"\ng, iso = r\nassert set(g) == {\"A\", \"B\", \"C\", \"D\"}, f\"셀 44 그래프의 가장 큰 덩어리는 A, B, C, D 예요. 지금은 {g} 이에요\"\nassert [set(c) for c in iso] == [{\"H\"}], f\"외톨이는 [{{'H'}}] 여야 해요. 지금은 {iso} 이에요\"\ng, iso = giant_and_isolated([(1, 2), (2, 3), (4, 5)], [6, 7])\nassert set(g) == {1, 2, 3}, f\"[(1, 2), (2, 3), (4, 5)] 의 가장 큰 덩어리는 {{1, 2, 3}} 이에요. 지금은 {g} 이에요\"\nassert sorted(min(c) for c in iso) == [6, 7] and all(len(c) == 1 for c in iso), f\"외톨이는 {{6}}, {{7}} 두 개예요. 지금은 {iso} 이에요\"\ng, iso = giant_and_isolated([(1, 2), (3, 4), (4, 5), (5, 3)], [])\nassert set(g) == {3, 4, 5} and iso == [], \"외톨이가 없으면 빈 리스트여야 해요\"\n","hints":["nx.Graph(edge_list) 로 시작","extra_nodes 를 for 로 돌며 add_node","셀 44 의 sorted 줄과 두 줄을 그대로","return giant_component, isolated_nodes"],"after":"이 과정이 데이터 전처리에서 흔한 '가장 큰 연결 요소만 남기기' 예요.","source":"PracticeCode_1 (3).ipynb 셀 44","think":["입력은 엣지 리스트와 따로 넣을 노드 리스트 `extra_nodes`, 출력은 (가장 큰 덩어리 집합, 외톨이 덩어리 리스트) 예요.","06 의 예로 손으로 풀어요. extra_nodes 가 [\"H\"] 면 결과는 {A, B, C, D} 와 [{'H'}] 예요.","06 과 달라진 점은 노드 하나 대신 리스트가 온다는 거예요. 그래서 for 로 하나씩 넣어요.","틀리기 쉬운 곳: 외톨이 노드는 엣지 리스트에 없어요. 넣는 줄을 빼먹으면 isolated_nodes 가 빈 리스트가 돼요.","확인: extra_nodes 를 [\"H\", \"Z\"] 로 불러서 외톨이가 두 개 나오면 맞아요."],"pseudo":"edge_list 로 그래프 G 를 만든다\nextra_nodes 의 노드마다\n    G 에 그 노드를 넣는다\n연결 요소들을 크기가 큰 것부터 줄 세운다\n맨 앞 덩어리를 giant_component 로 둔다\n크기가 1 인 덩어리만 모아 isolated_nodes 로 둔다\n두 값을 돌려준다"},{"id":"w1-graph-13","title":"노드를 지우면 엣지는 몇 개 남을까","level":"predict","run":"browser","goal":"실행하기 전에 출력을 손으로 먼저 적어요. 작은 그래프에서 노드 하나를 지운 뒤 노드 수, 엣지 수, 차수를 맞혀요.","explain":["1, 2, 3 이 삼각형으로 이어져 있고, 4번은 3번에만 붙어 있는 그래프예요.","`G.remove_node(1)` 은 1번 노드와 1번에 붙은 엣지를 함께 지워요 (03 번 실습).","`dict(G.degree())` 는 {노드: 차수} 딕셔너리예요. 순서는 노드를 넣은 순서예요."],"show":"import networkx as nx\n\nG = nx.Graph()\nG.add_edges_from([(1, 2), (2, 3), (3, 1), (3, 4)])\nG.remove_node(1)\nprint(G.number_of_nodes(), G.number_of_edges())\nprint(dict(G.degree()))","answer":"3 2\n{2: 1, 3: 2, 4: 1}","think":["먼저 종이에 그래프를 그려요. 1, 2, 3 은 삼각형이고 4 는 3 에만 붙어 있어요.","1번을 지우면 1번에 붙은 엣지도 같이 지워져요. 남는 엣지를 적어 봐요.","남은 엣지로 노드마다 붙은 선 수를 세요. 딕셔너리는 노드를 넣은 순서대로 찍혀요."],"hints":["처음 엣지는 4개예요. 1번에 붙은 엣지가 몇 개인지 세요","1-2 와 3-1 두 개가 사라져요","남는 엣지는 2-3, 3-4 예요"],"after":"노드를 지우면 엣지도 함께 사라진다는 점은 그래프에서 노드를 걸러낼 때 자주 만나요.","source":"PracticeCode_1 (3).ipynb 셀 24, 28, 30"}]};
