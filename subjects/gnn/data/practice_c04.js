window.GNN_PRACTICE=window.GNN_PRACTICE||{};window.GNN_PRACTICE["c04"]={"id":"c04","track":"c","unit":"wc-4","title":"함수와 모듈","intro":"함수는 자판기예요. 재료(매개변수)를 넣으면 결과(return)가 나와요. 마지막엔 남이 만든 자판기 가게(모듈)를 가져와 써요.","source":"","exercises":[{"id":"c04-01","title":"첫 함수 만들고 부르기","level":"type","run":"browser","goal":"인사말을 출력하는 함수 hello 를 만들고 두 번 불러요.","explain":["함수는 **자판기** 같아요. 한 번 만들어 두면 버튼만 눌러서 몇 번이든 같은 일을 시킬 수 있어요. 이런 묶음을 **함수(Function)** 라고 해요.","`def` 는 \"함수를 만든다(define)\" 는 뜻이에요. `def hello():` 처럼 이름, 괄호, 콜론(:) 순서로 써요.","그 아래 스페이스 4칸 들여쓴 줄이 함수가 할 일(몸통)이에요.","`def` 만 써서는 아무 일도 안 일어나요. `hello()` 처럼 이름 뒤에 괄호를 붙여야 실제로 실행돼요. 이걸 함수를 **부른다(호출)** 고 해요."],"show":"def hello():\n    print(\"안녕하세요\")\n\nhello()\nhello()","given":"","solution":"def hello():\n    print(\"안녕하세요\")\n\nhello()\nhello()","tests":"assert 'hello' in globals() and callable(hello), \"hello 라는 함수가 있어야 해요. def hello(): 로 만들어요\"\nlines = [x.strip() for x in __out__.strip().splitlines() if x.strip()]\nassert len(lines) == 2, f\"안녕하세요 가 두 줄 나와야 해요. 지금은 {len(lines)}줄이에요. hello() 를 두 번 불렀는지 보세요\"\nassert all(x == '안녕하세요' for x in lines), f\"출력이 안녕하세요 와 똑같아야 해요. 지금은 {lines} 예요\"","hints":["def 이름(): 다음 줄은 들여쓰기","함수를 만든 뒤, 들여쓰기 없이 hello() 를 두 번 써요","print(\"안녕하세요\") 줄 앞에 스페이스 4칸"],"after":"GNN 실습 노트북은 할 일을 def 로 묶어 두고 필요할 때 불러요. 3주차 Sample_Code_3 의 def aggregate(G, colors): 도 같은 모양이에요."},{"id":"c04-02","title":"재료 넣기: 매개변수","level":"blank","run":"browser","goal":"이름을 받아서 인사하는 함수 greet 의 빈칸을 채워요.","explain":["`def greet(name):` 의 괄호 안 `name` 은 자판기에 넣는 재료 칸이에요. 이 칸을 **매개변수(Parameter)** 라고 해요.","`greet(\"민수\")` 로 부르면 그 순간 함수 안에서 name 이 \"민수\" 가 돼요.","`greet(\"지아\")` 로 다시 부르면 이번엔 name 이 \"지아\" 예요. 같은 함수가 재료에 따라 다른 일을 해요.","f-string `f\"안녕, {name}!\"` 은 중괄호 안 변수 값을 글자 속에 끼워 넣어요 (c01 에서 배웠어요)."],"given":"def greet(name):\n    print(f\"안녕, {___}!\")\n\ngreet(\"민수\")\ngreet(\"지아\")","solution":"def greet(name):\n    print(f\"안녕, {name}!\")\n\ngreet(\"민수\")\ngreet(\"지아\")","tests":"assert 'greet' in globals() and callable(greet), \"greet 함수가 있어야 해요\"\nlines = [x.strip() for x in __out__.strip().splitlines()]\nassert lines == ['안녕, 민수!', '안녕, 지아!'], f\"출력이 '안녕, 민수!' 와 '안녕, 지아!' 두 줄이어야 해요. 지금은 {lines} 예요. 빈칸에 매개변수 이름 name 을 넣어요\"","hints":["빈칸에는 괄호 안에 적은 재료 칸 이름이 들어가요","def greet(name): 의 name","print(f\"안녕, {name}!\")"],"after":"PracticeCode_1 의 def random_walk_kernel(graph1, graph2, ...) 처럼 GNN 실습 함수는 그래프를 매개변수로 받아요."},{"id":"c04-03","title":"결과 돌려받기: return","level":"type","run":"browser","goal":"두 수를 더한 값을 돌려주는 함수 add 를 만들고, 결과를 변수에 담아요.","explain":["`return 값` 은 자판기에서 음료가 나오는 구멍이에요. 함수가 만든 값을 밖으로 **돌려줘요(Return)**.","`result = add(2, 3)` 은 돌려받은 5 에 result 라는 이름표를 붙여요.","`print` 는 화면에 보여 주기만 하고 값을 돌려주지 않아요. 값을 다시 쓰려면 `return` 이 필요해요.","`return` 을 만나면 함수는 그 자리에서 바로 끝나요."],"show":"def add(a, b):\n    return a + b\n\nresult = add(2, 3)\nprint(result)\nprint(add(10, 20))","given":"","solution":"def add(a, b):\n    return a + b\n\nresult = add(2, 3)\nprint(result)\nprint(add(10, 20))","tests":"assert 'add' in globals() and callable(add), \"add 함수가 있어야 해요\"\nassert add(2, 3) == 5, f\"add(2, 3) 은 5 여야 해요. 지금은 {add(2, 3)} 이에요. return a + b 를 확인해요\"\nassert add(-1, 1) == 0, \"add(-1, 1) 은 0 이어야 해요\"\nassert abs(add(1.5, 2) - 3.5) < 1e-9, \"add(1.5, 2) 는 3.5 여야 해요\"\nassert 'result' in globals() and result == 5, \"result 에 add(2, 3) 의 결과 5 가 담겨야 해요\"\nlines = [x.strip() for x in __out__.strip().splitlines()]\nassert lines == ['5', '30'], f\"출력이 5 와 30 두 줄이어야 해요. 지금은 {lines} 예요\"","hints":["함수 몸통은 return 한 줄이에요","return a + b","result = add(2, 3) 다음 print(result)"],"after":"2주차 PracticeCode_2 의 build_gcn_normalized_adjacency 함수도 계산한 행렬을 return 으로 돌려줘요."},{"id":"c04-04","title":"고치기: None 이 나와요","level":"fix","run":"browser","goal":"평균을 구하는 함수인데, 받은 값 m 이 None 이에요. 함수가 값을 돌려주도록 고쳐요.","explain":["실행하면 `2.0` 은 보이는데 `평균은 None` 이 나와요. **None** 은 \"아무것도 없음\" 이라는 뜻의 값이에요.","함수 안에서 `print` 만 하고 `return` 을 안 하면, 함수는 None 을 돌려줘요.","출력에 None 이 보이면 return 을 빠뜨렸다는 신호예요. 초보가 가장 많이 하는 실수 중 하나예요."],"given":"def mean(xs):\n    total = sum(xs)\n    print(total / len(xs))\n\nm = mean([1, 2, 3])\nprint(\"평균은\", m)","solution":"def mean(xs):\n    total = sum(xs)\n    return total / len(xs)\n\nm = mean([1, 2, 3])\nprint(\"평균은\", m)","tests":"assert 'mean' in globals() and callable(mean), \"mean 함수가 있어야 해요\"\nr = mean([1, 2, 3])\nassert r is not None, \"mean([1, 2, 3]) 의 결과가 None 이에요. 함수 안의 print(...) 를 return ... 으로 바꿔요\"\nassert abs(r - 2.0) < 1e-9, f\"mean([1, 2, 3]) 은 2.0 이어야 해요. 지금은 {r} 이에요\"\nr2 = mean([2, 4])\nassert r2 is not None and abs(r2 - 3.0) < 1e-9, \"mean([2, 4]) 는 3.0 이어야 해요\"\nr3 = mean([10])\nassert r3 is not None and abs(r3 - 10.0) < 1e-9, \"mean([10]) 은 10.0 이어야 해요\"\nassert 'm' in globals() and m is not None and abs(m - 2.0) < 1e-9, \"m 에 2.0 이 담겨야 해요\"","hints":["함수 안에서 값을 밖으로 내보내는 낱말은?","print(total / len(xs)) 줄을 바꿔요","return total / len(xs)"],"after":"GNN 학습 코드에서 손실을 계산하는 함수가 return 을 빼먹으면 loss 가 None 이 되어 학습이 멈춰요."},{"id":"c04-05","title":"안 넣으면 이 값: 기본값","level":"blank","run":"browser","goal":"제곱수를 구하는 함수 power 에서, 지수 n 을 안 넣으면 2 가 되도록 빈칸을 채워요.","explain":["`def power(x, n=2):` 의 `n=2` 는 \"n 을 안 넣으면 2 로 해 줘\" 라는 뜻이에요. 이걸 **기본값(Default)** 이라고 해요.","`power(3)` 은 n 을 안 넣었으니 3 ** 2 = 9, `power(3, 3)` 은 3 ** 3 = 27 이에요.","`power(2, n=5)` 처럼 이름을 붙여 넣을 수도 있어요.","기본값이 있는 매개변수는 기본값이 없는 매개변수보다 뒤에 적어요."],"given":"def power(x, n=___):\n    return x ** n\n\nprint(power(3))\nprint(power(3, 3))","solution":"def power(x, n=2):\n    return x ** n\n\nprint(power(3))\nprint(power(3, 3))","tests":"assert 'power' in globals() and callable(power), \"power 함수가 있어야 해요\"\nassert power(3) == 9, f\"power(3) 은 9 여야 해요. 지금은 {power(3)} 이에요. n 의 기본값을 2 로 해요\"\nassert power(5) == 25, \"power(5) 는 25 여야 해요\"\nassert power(2, 3) == 8, \"power(2, 3) 은 8 이어야 해요. 넣은 값이 기본값보다 먼저예요\"\nassert power(2, n=5) == 32, \"power(2, n=5) 는 32 여야 해요\"","hints":["빈칸은 n 을 안 넣었을 때 쓸 숫자예요","제곱이니까 2","def power(x, n=2):"],"after":"3주차 Sample_Code_3 의 def wl_refine(graphs, num_iterations=3): 에서 num_iterations=3 이 바로 기본값이에요."},{"id":"c04-06","title":"두 값 한꺼번에 돌려받기","level":"type","run":"browser","goal":"리스트의 가장 작은 값과 가장 큰 값을 함께 돌려주는 함수를 만들고, 두 이름표로 나눠 받아요.","explain":["`return min(xs), max(xs)` 처럼 쉼표로 이어 쓰면 두 값이 튜플 하나로 묶여 나와요 (튜플은 c02 에서 배운 괄호 묶음).","`lo, hi = min_max(...)` 는 묶음을 풀어서 첫 값은 lo, 둘째 값은 hi 에 담아요. 이걸 **풀기(Unpacking)** 라고 해요.","왼쪽 이름 개수와 돌려준 값 개수가 같아야 해요. 다르면 ValueError 가 나요.","`min`, `max` 는 파이썬에 원래 있는 함수예요."],"show":"def min_max(xs):\n    return min(xs), max(xs)\n\nlo, hi = min_max([4, 1, 7, 3])\nprint(lo, hi)","given":"","solution":"def min_max(xs):\n    return min(xs), max(xs)\n\nlo, hi = min_max([4, 1, 7, 3])\nprint(lo, hi)","tests":"assert 'min_max' in globals() and callable(min_max), \"min_max 함수가 있어야 해요\"\nassert tuple(min_max([4, 1, 7, 3])) == (1, 7), \"min_max([4, 1, 7, 3]) 은 (1, 7) 을 돌려줘야 해요\"\nassert tuple(min_max([5])) == (5, 5), \"min_max([5]) 는 (5, 5) 여야 해요\"\nassert tuple(min_max([-2, 9, 0])) == (-2, 9), \"min_max([-2, 9, 0]) 은 (-2, 9) 여야 해요\"\nassert 'lo' in globals() and 'hi' in globals(), \"lo, hi = min_max(...) 로 두 이름표에 나눠 받아야 해요\"\nassert lo == 1 and hi == 7, f\"lo 는 1, hi 는 7 이어야 해요. 지금은 lo={lo}, hi={hi} 예요\"\nassert __out__.strip() == '1 7', f\"1 7 이 출력되어야 해요. 지금은 {__out__.strip()} 이에요\"","hints":["return 뒤에 두 값을 쉼표로","받는 쪽도 이름 두 개를 쉼표로","lo, hi = min_max([4, 1, 7, 3])"],"after":"PyTorch 실습의 x, edge_index = data.x, data.edge_index 도 같은 풀기예요."},{"id":"c04-07","title":"고치기: 함수 안 변수는 밖에서 안 보여요","level":"fix","run":"browser","goal":"엣지 수를 세는 함수인데 NameError 가 나요. 함수 안에서 센 값을 밖에서 쓸 수 있게 고쳐요.","explain":["**엣지(Edge)** 는 그래프에서 두 점을 잇는 선이에요. `(0, 1)` 은 0번과 1번을 잇는 선 하나예요.","함수 안에서 만든 변수는 **지역 변수(Local Variable)** 예요. 자판기 안 부품처럼 밖에서는 안 보이고, 함수가 끝나면 사라져요.","에러 마지막 줄 `NameError: name 'n' is not defined` 는 \"n 이라는 이름이 없어요\" 라는 뜻이에요.","밖에서 쓰려면 함수 안에서 `return n` 으로 돌려주고, 밖에서 `n = count_edges(...)` 로 받아요."],"given":"def count_edges(edges):\n    n = len(edges)\n\ncount_edges([(0, 1), (1, 2), (2, 0)])\nprint(n)","solution":"def count_edges(edges):\n    n = len(edges)\n    return n\n\nn = count_edges([(0, 1), (1, 2), (2, 0)])\nprint(n)","tests":"assert 'count_edges' in globals() and callable(count_edges), \"count_edges 함수가 있어야 해요\"\nr = count_edges([(0, 1)])\nassert r is not None, \"count_edges 가 None 을 돌려줘요. 함수 안 마지막 줄에 return n 을 써요\"\nassert r == 1, f\"count_edges([(0, 1)]) 은 1 이어야 해요. 지금은 {r} 이에요\"\nassert count_edges([]) == 0, \"엣지가 없으면 0 이어야 해요\"\nassert count_edges([(0, 1), (1, 2), (2, 3), (3, 0)]) == 4, \"엣지 4개면 4 여야 해요\"\nassert 'n' in globals() and n == 3, \"밖에서 n = count_edges(...) 로 결과를 받아야 해요\"\nassert __out__.strip() == '3', \"3 이 출력되어야 해요\"","hints":["함수 안의 n 을 밖으로 내보내야 해요","함수 몸통 끝에 return n","밖에서 부르는 줄을 n = count_edges(...) 로 바꿔요"],"after":"노트북에서 함수 안 변수를 셀 밖에서 print 하다 NameError 가 나면 이 경우예요."},{"id":"c04-08","title":"함수가 다른 함수를 불러요","level":"blank","run":"browser","goal":"노드 하나의 차수를 구하는 degree 를 이용해서, 모든 노드의 평균 차수를 구하는 함수를 완성해요.","explain":["`adj` 는 인접 리스트예요. 키는 노드(그래프의 점) 번호, 값은 그 노드와 선으로 이어진 이웃 번호 리스트예요.","**차수(Degree)** 는 한 노드에 이어진 이웃 수, 곧 `len(adj[v])` 예요.","`average_degree` 안에서 이미 만든 `degree(adj, v)` 를 불러 써요. 자판기 안에 작은 자판기를 넣는 셈이에요.","작은 함수를 먼저 만들고, 큰 함수가 그걸 부르게 하면 코드가 읽기 쉬워져요."],"given":"adj = {0: [1, 2], 1: [0], 2: [0]}\n\ndef degree(adj, v):\n    return len(adj[v])\n\ndef average_degree(adj):\n    total = 0\n    for v in adj:\n        total = total + ___(adj, v)\n    return total / len(adj)\n\nprint(average_degree(adj))","solution":"adj = {0: [1, 2], 1: [0], 2: [0]}\n\ndef degree(adj, v):\n    return len(adj[v])\n\ndef average_degree(adj):\n    total = 0\n    for v in adj:\n        total = total + degree(adj, v)\n    return total / len(adj)\n\nprint(average_degree(adj))","tests":"assert 'degree' in globals() and 'average_degree' in globals(), \"degree 와 average_degree 함수가 모두 있어야 해요\"\nassert degree({0: [1, 2, 3]}, 0) == 3, \"degree 는 이웃 리스트의 길이를 돌려줘야 해요\"\nr = average_degree({0: [1, 2], 1: [0], 2: [0]})\nassert abs(r - 4 / 3) < 1e-9, f\"평균 차수는 4/3 (약 1.333) 이어야 해요. 지금은 {r} 이에요. 빈칸에 degree 를 넣어요\"\nassert abs(average_degree({0: [1], 1: [0]}) - 1.0) < 1e-9, \"노드 2개가 서로 이어졌으면 평균 차수는 1.0 이에요\"\nassert abs(average_degree({0: [1, 2, 3], 1: [0], 2: [0], 3: [0]}) - 1.5) < 1e-9, \"별 모양 그래프의 평균 차수는 1.5 예요\"","hints":["빈칸은 위에서 만든 함수 이름이에요","노드 v 의 차수를 구하는 함수","total = total + degree(adj, v)"],"after":"3주차 Sample_Code_3 의 wl_refine 은 안에서 aggregate 같은 작은 함수를 불러요."},{"id":"c04-09","title":"직접 짜기: 차수 딕셔너리와 최고 차수 노드","level":"write","run":"browser","goal":"엣지 리스트로 노드마다 차수를 세는 함수와, 차수가 가장 큰 노드를 찾는 함수를 직접 만들어요.","explain":["엣지 리스트는 `[(0, 1), (1, 2)]` 처럼 이어진 두 노드 쌍을 모은 리스트예요.","`for u, v in edges:` 는 쌍 하나를 꺼내 u, v 두 이름표로 풀어요.","처음 보는 노드는 딕셔너리에 없으니 먼저 `deg[u] = 0` 으로 자리를 만들어요.","두 번째 함수는 첫 번째 함수를 불러서 결과를 받아 써요 (바로 앞 실습)."],"given":"# 1. 함수 degree_dict(edges) 를 완성해요\n#    - 빈 딕셔너리 deg 를 만들어요\n#    - edges 의 (u, v) 마다: u 가 deg 에 없으면 deg[u] = 0, v 도 똑같이\n#    - deg[u], deg[v] 를 1씩 늘려요\n#    - 마지막에 deg 를 return 해요\n# 2. 함수 max_degree_node(edges) 를 완성해요\n#    - 안에서 degree_dict(edges) 를 불러 deg 로 받아요\n#    - 차수가 가장 큰 노드를 찾아 return 해요\ndef degree_dict(edges):\n    ...\n\ndef max_degree_node(edges):\n    ...","solution":"def degree_dict(edges):\n    deg = {}\n    for u, v in edges:\n        if u not in deg:\n            deg[u] = 0\n        if v not in deg:\n            deg[v] = 0\n        deg[u] = deg[u] + 1\n        deg[v] = deg[v] + 1\n    return deg\n\ndef max_degree_node(edges):\n    deg = degree_dict(edges)\n    best = None\n    for node in deg:\n        if best is None or deg[node] > deg[best]:\n            best = node\n    return best","tests":"assert 'degree_dict' in globals() and callable(degree_dict), \"degree_dict 함수가 있어야 해요\"\nassert 'max_degree_node' in globals() and callable(max_degree_node), \"max_degree_node 함수가 있어야 해요\"\nd1 = degree_dict([(0, 1), (1, 2), (1, 3)])\nassert d1 is not None, \"degree_dict 가 None 을 돌려줘요. 마지막 줄에 return deg 를 써요\"\nassert d1 == {0: 1, 1: 3, 2: 1, 3: 1}, f\"degree_dict([(0, 1), (1, 2), (1, 3)]) 은 {{0: 1, 1: 3, 2: 1, 3: 1}} 이어야 해요. 지금은 {d1} 이에요\"\nd2 = degree_dict([(\"A\", \"B\"), (\"B\", \"C\"), (\"C\", \"A\"), (\"C\", \"D\")])\nassert d2 == {\"A\": 2, \"B\": 2, \"C\": 3, \"D\": 1}, f\"글자 노드도 세야 해요. 지금은 {d2} 예요\"\nassert degree_dict([]) == {}, \"엣지가 없으면 빈 딕셔너리 {} 를 돌려줘야 해요\"\nm1 = max_degree_node([(0, 1), (1, 2), (1, 3)])\nassert m1 is not None, \"max_degree_node 가 None 을 돌려줘요. 찾은 노드를 return 해요\"\nassert m1 == 1, f\"[(0, 1), (1, 2), (1, 3)] 에서 차수가 가장 큰 노드는 1 이에요. 지금은 {m1} 이에요\"\nm2 = max_degree_node([(\"A\", \"B\"), (\"B\", \"C\"), (\"C\", \"A\"), (\"C\", \"D\")])\nassert m2 == \"C\", f\"차수가 가장 큰 노드는 C 여야 해요. 지금은 {m2} 예요\"","hints":["degree_dict 는 for u, v in edges: 반복 하나로 돼요","max_degree_node 는 best 라는 이름표에 지금까지 1등 노드를 담아 두고, 더 큰 차수를 만나면 바꿔요","if best is None or deg[node] > deg[best]: best = node","마지막에 둘 다 return 을 잊지 마세요"],"after":"1주차 networkx 실습의 G.degree() 가 속에서 하는 일이 바로 이 계산이에요."},{"id":"c04-10","title":"남이 만든 함수 가져오기: import","level":"type","run":"browser","goal":"math 모듈을 가져와 제곱근과 원주율을 써 봐요.","explain":["**모듈(Module)** 은 남이 만들어 둔 함수 모음이에요. 자판기가 잔뜩 있는 가게라고 생각해요.","`import math` 는 math 가게를 통째로 가져와요. 쓸 때는 `math.sqrt(16)` 처럼 가게 이름과 점(.)을 붙여요.","`from math import sqrt` 는 가게에서 sqrt 하나만 꺼내 와요. 그래서 `sqrt(25)` 처럼 이름만 써요.","`math.sqrt` 는 제곱근(루트), `math.pi` 는 원주율 3.14159 예요."],"show":"import math\n\nprint(math.sqrt(16))\nprint(math.pi)\n\nfrom math import sqrt\nprint(sqrt(25))","given":"","solution":"import math\n\nprint(math.sqrt(16))\nprint(math.pi)\n\nfrom math import sqrt\nprint(sqrt(25))","tests":"assert 'math' in globals(), \"import math 로 math 모듈을 가져와야 해요\"\nassert 'sqrt' in globals(), \"from math import sqrt 로 sqrt 를 꺼내 와야 해요\"\nlines = [x.strip() for x in __out__.strip().splitlines()]\nassert len(lines) == 3, f\"출력이 세 줄이어야 해요. 지금은 {len(lines)}줄이에요\"\nassert lines[0] == '4.0', f\"첫 줄은 math.sqrt(16) 의 결과 4.0 이어야 해요. 지금은 {lines[0]} 이에요\"\nassert lines[1].startswith('3.14'), f\"둘째 줄은 math.pi (3.14159...) 여야 해요. 지금은 {lines[1]} 이에요\"\nassert lines[2] == '5.0', f\"셋째 줄은 sqrt(25) 의 결과 5.0 이어야 해요. 지금은 {lines[2]} 이에요\"","hints":["import 줄은 보통 코드 맨 위에 둬요","import math 로 가져오면 math.sqrt 로 써요","from math import sqrt 로 가져오면 sqrt 로 써요"],"after":"모든 GNN 노트북은 첫 셀이 import 줄로 시작해요."},{"id":"c04-11","title":"모듈에 별명 붙이기: as","level":"blank","run":"browser","goal":"statistics 모듈을 st 라는 짧은 별명으로 가져와서 평균을 구해요.","explain":["`import statistics as st` 는 statistics 모듈을 가져오면서 st 라는 **별명** 을 붙여요.","그다음부터는 긴 이름 대신 `st.mean(...)` 처럼 별명으로 써요.","GNN 실습의 `import numpy as np`, `import networkx as nx` 가 바로 이 모양이에요. np, nx 는 모두가 쓰는 약속된 별명이에요.","`st.mean(리스트)` 는 평균을 돌려줘요."],"given":"import statistics ___ st\n\nscores = [70, 80, 90]\navg = ___.mean(scores)\nprint(avg)","solution":"import statistics as st\n\nscores = [70, 80, 90]\navg = st.mean(scores)\nprint(avg)","tests":"assert 'st' in globals(), \"import statistics as st 로 별명 st 를 만들어야 해요\"\nassert getattr(st, '__name__', '') == 'statistics', \"st 는 statistics 모듈의 별명이어야 해요\"\nassert 'avg' in globals() and avg is not None, \"avg 에 평균이 담겨야 해요\"\nassert abs(avg - 80) < 1e-9, f\"avg 는 80 이어야 해요. 지금은 {avg} 예요\"\nassert __out__.strip() == '80', f\"80 이 출력되어야 해요. 지금은 {__out__.strip()} 이에요\"","hints":["별명을 붙이는 낱말은 두 글자예요","첫 빈칸은 as","둘째 빈칸은 별명 st"],"after":"3주차 Sample_Code_3 첫 셀의 import numpy as np, import networkx as nx 를 이제 읽을 수 있어요."},{"id":"c04-12","title":"개수 세기 도구: collections.Counter","level":"type","run":"browser","goal":"색 이름 리스트에서 색마다 몇 번 나왔는지 Counter 로 세어요.","explain":["`from collections import Counter` 는 collections 모듈에서 Counter 하나를 꺼내 와요.","`Counter(리스트)` 는 값마다 몇 번 나왔는지 세어 딕셔너리처럼 담아요. `cnt[\"빨강\"]` 은 3 이에요.","없는 키를 물어보면 에러 대신 0 을 돌려줘요.","`cnt.most_common(1)` 은 가장 많이 나온 값 1개를 `[(값, 개수)]` 모양으로 돌려줘요."],"show":"from collections import Counter\n\ncolors = [\"빨강\", \"파랑\", \"빨강\", \"초록\", \"빨강\"]\ncnt = Counter(colors)\nprint(cnt[\"빨강\"])\nprint(cnt[\"보라\"])\nprint(cnt.most_common(1))","given":"","solution":"from collections import Counter\n\ncolors = [\"빨강\", \"파랑\", \"빨강\", \"초록\", \"빨강\"]\ncnt = Counter(colors)\nprint(cnt[\"빨강\"])\nprint(cnt[\"보라\"])\nprint(cnt.most_common(1))","tests":"assert 'Counter' in globals(), \"from collections import Counter 로 Counter 를 가져와야 해요\"\nassert 'cnt' in globals(), \"cnt = Counter(colors) 로 세어야 해요\"\nassert cnt[\"빨강\"] == 3 and cnt[\"파랑\"] == 1 and cnt[\"초록\"] == 1, \"cnt 는 빨강 3, 파랑 1, 초록 1 이어야 해요\"\nlines = [x.strip() for x in __out__.strip().splitlines()]\nassert lines == ['3', '0', \"[('빨강', 3)]\"], f\"출력이 3, 0, [('빨강', 3)] 세 줄이어야 해요. 지금은 {lines} 예요\"","hints":["첫 줄은 from collections import Counter","cnt = Counter(colors)","가장 많은 것은 cnt.most_common(1)"],"after":"1주차 PracticeCode_1 과 3주차 Sample_Code_3 의 WL 색 정제는 Counter 로 색마다 노드 수를 세요."},{"id":"c04-13","title":"매번 같은 무작위: random.seed","level":"blank","run":"browser","goal":"주사위를 10번 굴린 결과가 두 번 모두 똑같이 나오도록 씨앗(seed)을 맞춰요.","explain":["`random.randint(1, 6)` 은 1 부터 6 사이 정수 하나를 무작위로 뽑아요.","`random.seed(42)` 는 무작위 순서의 출발점(씨앗)을 42 로 고정해요. 씨앗이 같으면 뽑히는 순서도 매번 똑같아요.","`[... for _ in range(10)]` 는 10번 반복해서 리스트를 만들어요. `_` 는 \"이름을 안 쓰는 변수\" 라는 약속이에요.","실험 결과를 다른 사람과 똑같이 다시 만들려고 씨앗을 고정해요."],"given":"import random\n\nrandom.seed(42)\na = [random.randint(1, 6) for _ in range(10)]\n\nrandom.seed(___)\nb = [random.randint(1, 6) for _ in range(10)]\n\nprint(a == b)","solution":"import random\n\nrandom.seed(42)\na = [random.randint(1, 6) for _ in range(10)]\n\nrandom.seed(42)\nb = [random.randint(1, 6) for _ in range(10)]\n\nprint(a == b)","tests":"import random as _r\n_r.seed(42)\n_exp = [_r.randint(1, 6) for _ in range(10)]\nassert 'a' in globals() and 'b' in globals(), \"a 와 b 두 리스트가 있어야 해요\"\nassert a == _exp, \"a 가 씨앗 42 의 결과와 달라요. 첫 번째 random.seed(42) 줄은 그대로 둬요\"\nassert b == a, \"b 가 a 와 달라요. 두 번째 씨앗도 첫 번째와 같은 42 로 해요\"\nassert __out__.strip() == 'True', f\"True 가 출력되어야 해요. 지금은 {__out__.strip()} 이에요\"","hints":["두 번 모두 같은 출발점이어야 같은 순서가 나와요","위에서 쓴 씨앗 숫자를 보세요","random.seed(42)"],"after":"2주차 PracticeCode_2 앞부분의 random.seed(42), np.random.seed(42) 가 이 역할이에요."},{"id":"c04-14","title":"직접 짜기: 무작위 레이블과 다수결","level":"write","run":"browser","goal":"씨앗을 고정해 0 또는 1 레이블을 뽑는 함수와, 가장 많은 레이블과 그 개수를 돌려주는 함수를 직접 만들어요.","explain":["**레이블(Label)** 은 노드에 붙은 정답 이름표예요. 여기서는 0 또는 1 이에요.","`seed=0` 은 기본값이에요. 안 넣으면 씨앗 0 을 써요.","`Counter(labels).most_common(1)` 은 `[(값, 개수)]` 리스트라서 `[0]` 으로 첫 칸을 꺼낸 뒤 풀어요.","`return value, count` 처럼 두 값을 돌려줘요."],"given":"# 1. 맨 위에 import random 과 from collections import Counter 를 적어요\n# 2. 함수 make_labels(n, seed=0) 를 완성해요\n#    - random.seed(seed) 로 씨앗을 고정해요\n#    - random.randint(0, 1) 을 n 번 뽑아 리스트로 만들어 return 해요\n# 3. 함수 majority(labels) 를 완성해요\n#    - Counter(labels).most_common(1)[0] 을 value, count 로 풀어요\n#    - value, count 두 값을 return 해요\ndef make_labels(n, seed=0):\n    ...\n\ndef majority(labels):\n    ...","solution":"import random\nfrom collections import Counter\n\ndef make_labels(n, seed=0):\n    random.seed(seed)\n    return [random.randint(0, 1) for _ in range(n)]\n\ndef majority(labels):\n    value, count = Counter(labels).most_common(1)[0]\n    return value, count","tests":"import random as _r\nassert 'make_labels' in globals() and 'majority' in globals(), \"make_labels 와 majority 함수가 있어야 해요\"\nr = make_labels(10, seed=3)\nassert r is not None, \"make_labels 가 None 을 돌려줘요. 만든 리스트를 return 해요\"\nassert len(r) == 10, f\"make_labels(10, seed=3) 은 길이 10 리스트여야 해요. 지금은 길이 {len(r)} 이에요\"\n_r.seed(3)\n_exp3 = [_r.randint(0, 1) for _ in range(10)]\nassert list(r) == _exp3, \"씨앗 3 의 결과와 달라요. 함수 첫 줄에서 random.seed(seed) 를 하고 random.randint(0, 1) 로 뽑아요\"\nassert make_labels(10, seed=3) == r, \"같은 씨앗이면 몇 번 불러도 같은 리스트가 나와야 해요\"\n_r.seed(0)\n_exp0 = [_r.randint(0, 1) for _ in range(6)]\nassert make_labels(6) == _exp0, \"seed 를 안 넣으면 기본값 0 을 써야 해요. def make_labels(n, seed=0): 을 확인해요\"\nm1 = majority([\"a\", \"b\", \"a\"])\nassert m1 is not None, \"majority 가 None 을 돌려줘요. return value, count 를 써요\"\nassert tuple(m1) == (\"a\", 2), f\"majority(['a', 'b', 'a']) 는 ('a', 2) 여야 해요. 지금은 {m1} 이에요\"\nassert tuple(majority([1, 1, 1, 0])) == (1, 3), \"majority([1, 1, 1, 0]) 은 (1, 3) 이어야 해요\"\nassert tuple(majority([0])) == (0, 1), \"majority([0]) 은 (0, 1) 이어야 해요\"","hints":["import 두 줄을 함수보다 위에 써요","make_labels 는 random.seed(seed) 한 줄 + 리스트 컴프리헨션 return 한 줄","value, count = Counter(labels).most_common(1)[0]","return value, count"],"after":"2주차 레이블 전파 실습은 이렇게 씨앗을 고정하고 노드 레이블을 세며 결과를 확인해요."}]};
