window.GNN_PRACTICE=window.GNN_PRACTICE||{};window.GNN_PRACTICE["c05"]={"id":"c05","track":"c","unit":"wc-5","title":"클래스와 객체 1","intro":"붕어빵 틀(클래스)로 붕어빵(객체)을 찍어 내 봐요. 빈 틀, 속 넣기, self, 메서드 순서로 아주 천천히 가고, 마지막엔 그래프 클래스를 직접 만들어요.","source":"","exercises":[{"id":"c05-01","title":"클래스 없이 노드 담아 보기","level":"type","run":"browser","goal":"노드 두 개를 딕셔너리로 담고, 친구 수를 세는 함수로 출력해요. 무엇이 불편한지 느껴 봐요.","explain":["**노드(Node)** 는 그래프의 점 하나예요. 여기서는 사람 한 명이라고 생각해요.","사람마다 이름과 친구 목록을 같은 모양 딕셔너리로 만들었어요. 사람이 100명이면 이 모양을 100번 똑같이 써야 해요.","키를 `\"freinds\"` 처럼 오타 내도 만들 때는 아무도 알려 주지 않아요.","게다가 데이터(딕셔너리)와 할 일(count_friends 함수)이 따로 흩어져 있어요.","\"같은 모양으로 찍어 내는 틀\" 과 \"그 틀이 할 수 있는 일\" 을 한데 묶는 도구가 **클래스(Class)** 예요. 다음 실습부터 만들어요."],"show":"node_a = {\"name\": \"A\", \"friends\": [\"B\", \"C\"]}\nnode_b = {\"name\": \"B\", \"friends\": [\"A\"]}\n\ndef count_friends(node):\n    return len(node[\"friends\"])\n\nprint(node_a[\"name\"], count_friends(node_a))\nprint(node_b[\"name\"], count_friends(node_b))","given":"","solution":"node_a = {\"name\": \"A\", \"friends\": [\"B\", \"C\"]}\nnode_b = {\"name\": \"B\", \"friends\": [\"A\"]}\n\ndef count_friends(node):\n    return len(node[\"friends\"])\n\nprint(node_a[\"name\"], count_friends(node_a))\nprint(node_b[\"name\"], count_friends(node_b))","tests":"assert 'node_a' in globals() and 'node_b' in globals(), \"node_a, node_b 딕셔너리가 있어야 해요\"\nassert 'count_friends' in globals(), \"count_friends 함수가 있어야 해요\"\nassert count_friends({\"name\": \"Z\", \"friends\": [1, 2, 3]}) == 3, \"count_friends 는 friends 리스트 길이를 돌려줘야 해요\"\nlines = [x.strip() for x in __out__.strip().splitlines()]\nassert lines == ['A 2', 'B 1'], f\"출력이 'A 2' 와 'B 1' 두 줄이어야 해요. 지금은 {lines} 예요\"","hints":["딕셔너리는 {키: 값}","count_friends 는 return len(node[\"friends\"])","print 에 쉼표로 두 값을 넣으면 한 칸 띄워 나와요"],"after":"1주차 networkx 실습의 G.nodes[v] 도 노드마다 이런 딕셔너리를 달고 있어요."},{"id":"c05-02","title":"빈 붕어빵 틀 만들기","level":"type","run":"browser","goal":"Bread 라는 빈 클래스를 만들고, 그 틀로 객체 하나를 찍어 내요.","explain":["클래스는 **붕어빵 틀**, 틀로 찍어 낸 붕어빵 하나하나는 **객체(Object)** 예요.","`class Bread:` 는 Bread 라는 틀을 만든다는 뜻이에요. 끝의 콜론(:) 을 잊지 마세요. 클래스 이름은 대문자로 시작하는 게 약속이에요.","`pass` 는 \"아직 아무것도 안 넣음\" 이라는 자리 표시예요.","`b = Bread()` 는 틀 이름 뒤에 괄호를 붙여 붕어빵 하나를 찍고 b 라는 이름표를 붙여요.","`type(b).__name__` 은 \"b 가 어떤 틀로 찍혔나\" 의 이름이에요."],"show":"class Bread:\n    pass\n\nb = Bread()\nprint(type(b).__name__)","given":"","solution":"class Bread:\n    pass\n\nb = Bread()\nprint(type(b).__name__)","tests":"assert 'Bread' in globals() and isinstance(Bread, type), \"class Bread: 로 Bread 클래스를 만들어야 해요\"\nassert 'b' in globals() and isinstance(b, Bread), \"b 는 Bread() 로 찍어 낸 객체여야 해요. 괄호를 붙였는지 보세요\"\nassert __out__.strip() == 'Bread', f\"Bread 가 출력되어야 해요. 지금은 {__out__.strip()} 이에요\"","hints":["class 이름 뒤에 콜론","안쪽 줄 pass 는 스페이스 4칸 들여쓰기","객체는 Bread() 처럼 괄호를 붙여 만들어요"],"after":"PyTorch 모델도 전부 class 로 만들어요. 2주차 PracticeCode_2 의 class ScratchGCN 이 바로 이 틀이에요."},{"id":"c05-03","title":"같은 틀로 붕어빵 두 개","level":"blank","run":"browser","goal":"Bread 틀로 서로 다른 붕어빵 두 개를 찍어 내요.","explain":["틀 하나로 붕어빵을 몇 개든 찍을 수 있어요. 찍을 때마다 새 붕어빵이 생겨요.","`b1 is b2` 는 \"b1 과 b2 가 완전히 같은 한 개인가\" 를 물어요. 따로 찍었으니 False 예요.","`b1 = Bread` 처럼 괄호를 빼면 붕어빵이 아니라 틀 자체에 이름표를 붙이는 거예요. 꼭 `Bread()`."],"given":"class Bread:\n    pass\n\nb1 = ___()\nb2 = ___()\n\nprint(b1 is b2)","solution":"class Bread:\n    pass\n\nb1 = Bread()\nb2 = Bread()\n\nprint(b1 is b2)","tests":"assert 'Bread' in globals(), \"Bread 클래스가 있어야 해요\"\nassert 'b1' in globals() and isinstance(b1, Bread), \"b1 은 Bread() 처럼 괄호를 붙여 찍은 객체여야 해요\"\nassert 'b2' in globals() and isinstance(b2, Bread), \"b2 도 Bread() 로 찍은 객체여야 해요\"\nassert b1 is not b2, \"b1 과 b2 는 따로 찍은 서로 다른 붕어빵이어야 해요. 각각 Bread() 를 불러요\"\nassert __out__.strip() == 'False', f\"False 가 출력되어야 해요. 지금은 {__out__.strip()} 이에요\"","hints":["빈칸에는 틀 이름이 들어가요","틀 이름은 Bread","b1 = Bread()"],"after":"GNN 실습에서 model = ScratchGCN(...) 을 두 번 부르면 서로 다른 모델 두 개가 생겨요."},{"id":"c05-04","title":"찍을 때 속 넣기: __init__","level":"type","run":"browser","goal":"붕어빵을 찍을 때 속(filling)을 정해 넣고, 그 속을 꺼내 출력해요.","explain":["`__init__` (앞뒤 밑줄 두 개씩) 은 붕어빵을 찍는 순간 자동으로 한 번 실행되는 특별한 함수예요. \"속 넣기\" 담당이고, **생성자(Constructor)** 라고 불러요.","클래스 안의 def 는 첫 칸에 항상 `self` 를 적어요. self 가 무엇인지는 바로 다음 실습에서 자세히 봐요. 지금은 \"꼭 쓰는 첫 칸\" 이라고만 기억해요.","`Bread(\"팥\")` 으로 찍으면 \"팥\" 이 filling 칸에 들어가요. self 칸은 파이썬이 알아서 채워요.","`self.filling = filling` 은 이 붕어빵에 filling 이라는 속을 붙여 두는 줄이에요. 이렇게 객체에 붙은 값을 **속성(Attribute)** 이라고 해요.","`b.filling` 처럼 점(.) 뒤에 이름을 쓰면 속성을 꺼내요."],"show":"class Bread:\n    def __init__(self, filling):\n        self.filling = filling\n\nb = Bread(\"팥\")\nprint(b.filling)","given":"","solution":"class Bread:\n    def __init__(self, filling):\n        self.filling = filling\n\nb = Bread(\"팥\")\nprint(b.filling)","tests":"assert 'Bread' in globals(), \"Bread 클래스가 있어야 해요\"\nassert 'b' in globals() and isinstance(b, Bread), \"b = Bread(\\\"팥\\\") 으로 객체를 만들어야 해요\"\nassert getattr(b, 'filling', None) == '팥', \"b.filling 이 팥 이어야 해요. __init__ 안에 self.filling = filling 을 써요\"\nassert Bread(\"슈크림\").filling == '슈크림', \"Bread(\\\"슈크림\\\").filling 은 슈크림 이어야 해요\"\nassert __out__.strip() == '팥', f\"팥 이 출력되어야 해요. 지금은 {__out__.strip()} 이에요\"","hints":["def __init__(self, filling): 은 class 안이라 4칸 들여쓰기","self.filling = filling 은 8칸 들여쓰기","밑줄은 앞에 두 개, 뒤에 두 개"],"after":"1주차 Solution_1 의 class VoseAlias: 도 def __init__(self, dist): 로 속을 넣으며 시작해요."},{"id":"c05-05","title":"self 는 지금 이 붕어빵 자신","level":"blank","run":"browser","goal":"팥 붕어빵 a 와 슈크림 붕어빵 b 를 찍어서, 둘의 속이 섞이지 않는 걸 확인해요.","explain":["`self` 는 \"지금 찍고 있는 바로 이 붕어빵 자신\" 이에요.","`a = Bread(\"팥\")` 을 실행하면 파이썬이 self 자리에 앞으로 a 가 될 새 붕어빵을 넣어요. 그래서 `self.filling = \"팥\"` 은 `a.filling = \"팥\"` 과 같은 뜻이 돼요.","`b = Bread(\"슈크림\")` 에서는 self 가 b 가 될 붕어빵이에요. 그래서 b 에만 슈크림이 붙어요.","틀(코드)은 하나인데 붕어빵마다 속이 따로 있어요. 이게 self 를 쓰는 이유예요."],"given":"class Bread:\n    def __init__(self, filling):\n        self.___ = filling\n\na = Bread(\"팥\")\nb = Bread(___)\n\nprint(a.filling)\nprint(b.filling)","solution":"class Bread:\n    def __init__(self, filling):\n        self.filling = filling\n\na = Bread(\"팥\")\nb = Bread(\"슈크림\")\n\nprint(a.filling)\nprint(b.filling)","tests":"assert 'Bread' in globals(), \"Bread 클래스가 있어야 해요\"\nassert 'a' in globals() and 'b' in globals(), \"a 와 b 두 객체가 있어야 해요\"\nassert getattr(a, 'filling', None) == '팥', \"a.filling 은 팥 이어야 해요. 첫 빈칸은 속성 이름 filling 이에요\"\nassert getattr(b, 'filling', None) == '슈크림', f\"b.filling 은 슈크림 이어야 해요. 지금은 {getattr(b, 'filling', None)} 이에요. Bread(\\\"슈크림\\\") 으로 찍어요\"\nassert a is not b, \"a 와 b 는 서로 다른 객체여야 해요\"\nc = Bread(\"크림치즈\")\nassert c.filling == '크림치즈' and a.filling == '팥', \"새 붕어빵을 찍어도 a 의 속은 그대로 팥 이어야 해요\"\nlines = [x.strip() for x in __out__.strip().splitlines()]\nassert lines == ['팥', '슈크림'], f\"출력이 팥, 슈크림 두 줄이어야 해요. 지금은 {lines} 예요\"","hints":["첫 빈칸은 print 에서 꺼내는 속성 이름과 같아요","self.filling = filling","둘째 빈칸은 따옴표까지 \"슈크림\""],"after":"GNN 모델도 self.conv1, self.conv2 처럼 self 에 층을 붙여 둬요. 모델마다 자기 층을 따로 가져요."},{"id":"c05-06","title":"고치기: 속이 안 붙었어요","level":"fix","run":"browser","goal":"AttributeError 가 나요. 붕어빵에 속이 제대로 붙도록 한 곳을 고쳐요.","explain":["에러는 마지막 줄부터 읽어요. `AttributeError: 'Bread' object has no attribute 'filling'` 은 \"Bread 객체에 filling 이라는 속성이 없어요\" 라는 뜻이에요.","`filling = filling` 은 함수 안의 지역 변수에 담았을 뿐이에요 (c04 에서 본 지역 변수). `__init__` 이 끝나면 사라져요.","붕어빵 자신에게 붙이려면 앞에 `self.` 을 붙여야 해요."],"given":"class Bread:\n    def __init__(self, filling):\n        filling = filling\n\na = Bread(\"팥\")\nb = Bread(\"슈크림\")\nprint(a.filling, b.filling)","solution":"class Bread:\n    def __init__(self, filling):\n        self.filling = filling\n\na = Bread(\"팥\")\nb = Bread(\"슈크림\")\nprint(a.filling, b.filling)","tests":"assert 'Bread' in globals(), \"Bread 클래스가 있어야 해요\"\nt = Bread(\"녹차\")\nassert hasattr(t, 'filling'), \"Bread 객체에 filling 속성이 없어요. __init__ 안에서 self.filling = filling 으로 붙여요\"\nassert t.filling == '녹차', \"Bread(\\\"녹차\\\").filling 은 녹차 여야 해요\"\nassert a.filling == '팥' and b.filling == '슈크림', \"a 는 팥, b 는 슈크림 이어야 해요\"\nassert __out__.strip() == '팥 슈크림', \"팥 슈크림 이 출력되어야 해요\"","hints":["에러 메시지가 말하는 속성 이름을 찾아요","__init__ 안의 줄 하나만 바꿔요","self.filling = filling"],"after":"PyTorch 모델에서 self. 을 빼고 lin = nn.Linear(...) 라고 쓰면 forward 에서 self.lin 을 못 찾아요. 같은 실수예요."},{"id":"c05-07","title":"직접 짜기: 이름과 특징이 있는 Node","level":"write","run":"browser","goal":"이름(name)과 특징 값(feature)을 가진 Node 클래스를 만들고 노드 두 개를 찍어요.","explain":["**특징(Feature)** 은 노드에 붙은 숫자 정보예요. 예를 들어 사람의 나이나 키 같은 값이에요. GNN 은 이 숫자로 계산해요.","`__init__(self, name, feature)` 처럼 속 넣기 재료를 여러 개 받을 수 있어요.","받은 재료마다 `self.이름 = 재료` 한 줄씩 붙여요."],"given":"# 1. class Node: 를 만들어요\n# 2. def __init__(self, name, feature): 안에서\n#    - self.name 에 name 을 붙여요\n#    - self.feature 에 feature 를 붙여요\n# 3. 클래스 밖에서 노드 두 개를 만들어요\n#    - n1 = Node(\"A\", 0.5)\n#    - n2 = Node(\"B\", 1.2)\nclass Node:\n    ...","solution":"class Node:\n    def __init__(self, name, feature):\n        self.name = name\n        self.feature = feature\n\nn1 = Node(\"A\", 0.5)\nn2 = Node(\"B\", 1.2)","tests":"assert 'Node' in globals() and isinstance(Node, type), \"class Node: 로 Node 클래스를 만들어야 해요\"\ntry:\n    t = Node(\"C\", 3.0)\nexcept TypeError:\n    assert False, 'Node(\"C\", 3.0) 처럼 이름과 특징 두 개를 넣어 만들 수 있어야 해요. def __init__(self, name, feature): 로 적어요'\nassert getattr(t, 'name', None) == 'C', \"name 속성이 없거나 틀려요. self.name = name 을 써요\"\nassert getattr(t, 'feature', None) == 3.0, \"feature 속성이 없거나 틀려요. self.feature = feature 를 써요\"\nassert 'n1' in globals() and 'n2' in globals(), 'n1 = Node(\"A\", 0.5), n2 = Node(\"B\", 1.2) 두 노드를 만들어야 해요'\nassert isinstance(n1, Node) and isinstance(n2, Node), \"n1, n2 는 Node 객체여야 해요\"\nassert n1.name == 'A' and abs(n1.feature - 0.5) < 1e-9, \"n1 은 이름 A, 특징 0.5 여야 해요\"\nassert n2.name == 'B' and abs(n2.feature - 1.2) < 1e-9, \"n2 는 이름 B, 특징 1.2 여야 해요\"\nassert n1.name == 'A', \"다른 노드를 만들어도 n1 의 이름은 그대로 A 여야 해요\"","hints":["... 줄을 지우고 def __init__ 을 적어요","self.name = name, self.feature = feature 두 줄","n1 = Node(\"A\", 0.5) 는 들여쓰기 없이"],"after":"PyG 의 data.x 는 노드마다 이런 특징 값을 모은 표예요."},{"id":"c05-08","title":"붕어빵이 할 수 있는 일: 메서드","level":"type","run":"browser","goal":"Bread 에 인사말을 돌려주는 메서드 greet 를 넣고 불러 봐요.","explain":["클래스 안에 만든 def 를 **메서드(Method)** 라고 해요. 붕어빵이 할 수 있는 일이에요.","메서드도 첫 칸은 `self` 예요. 이번 greet 는 self 를 안 쓰지만 그래도 꼭 적어요.","부를 때는 `b.greet()` 처럼 객체, 점, 메서드 이름, 괄호 순서예요. self 칸은 파이썬이 b 로 채우니 괄호 안은 비워요.","`__init__` 과 greet 는 같은 들여쓰기(4칸)이고, 사이에 빈 줄 하나를 두면 읽기 좋아요."],"show":"class Bread:\n    def __init__(self, filling):\n        self.filling = filling\n\n    def greet(self):\n        return \"안녕, 나는 붕어빵\"\n\nb = Bread(\"팥\")\nprint(b.greet())","given":"","solution":"class Bread:\n    def __init__(self, filling):\n        self.filling = filling\n\n    def greet(self):\n        return \"안녕, 나는 붕어빵\"\n\nb = Bread(\"팥\")\nprint(b.greet())","tests":"assert 'Bread' in globals(), \"Bread 클래스가 있어야 해요\"\nassert hasattr(Bread, 'greet'), \"Bread 안에 def greet(self): 메서드를 만들어야 해요\"\nassert Bread(\"슈크림\").greet() == '안녕, 나는 붕어빵', \"greet() 는 '안녕, 나는 붕어빵' 을 돌려줘야 해요\"\nassert 'b' in globals() and b.filling == '팥', \"b = Bread(\\\"팥\\\") 을 만들어야 해요\"\nassert __out__.strip() == '안녕, 나는 붕어빵', f\"'안녕, 나는 붕어빵' 이 출력되어야 해요. 지금은 {__out__.strip()} 이에요\"","hints":["def greet(self): 는 __init__ 과 같은 4칸 들여쓰기","return 줄은 8칸","부를 때는 b.greet()"],"after":"PyTorch 모델의 forward 도 이런 메서드예요. def forward(self, x): 처럼 첫 칸이 self 예요."},{"id":"c05-09","title":"메서드 안에서 내 속 꺼내기","level":"blank","run":"browser","goal":"붕어빵이 자기 속을 말하는 메서드 describe 를 완성해요. a 와 b 가 서로 다른 말을 해야 해요.","explain":["메서드 안에서 \"이 붕어빵의 속\" 은 `self.filling` 이에요.","`a.describe()` 를 부르면 self 가 a 가 되고, `b.describe()` 를 부르면 self 가 b 가 돼요.","그래서 같은 describe 코드가 붕어빵마다 다른 결과를 돌려줘요.","`filling` 만 쓰면 NameError, `a.filling` 이라고 쓰면 모든 붕어빵이 팥이라고 말해 버려요. 꼭 `self.`."],"given":"class Bread:\n    def __init__(self, filling):\n        self.filling = filling\n\n    def describe(self):\n        return f\"나는 {___.filling} 붕어빵\"\n\na = Bread(\"팥\")\nb = Bread(\"슈크림\")\nprint(a.describe())\nprint(b.describe())","solution":"class Bread:\n    def __init__(self, filling):\n        self.filling = filling\n\n    def describe(self):\n        return f\"나는 {self.filling} 붕어빵\"\n\na = Bread(\"팥\")\nb = Bread(\"슈크림\")\nprint(a.describe())\nprint(b.describe())","tests":"assert 'Bread' in globals() and hasattr(Bread, 'describe'), \"Bread 에 describe 메서드가 있어야 해요\"\nassert Bread(\"팥\").describe() == '나는 팥 붕어빵', \"Bread(\\\"팥\\\").describe() 는 '나는 팥 붕어빵' 이어야 해요\"\nr = Bread(\"피자\").describe()\nassert r == '나는 피자 붕어빵', f\"Bread(\\\"피자\\\").describe() 는 '나는 피자 붕어빵' 이어야 해요. 지금은 {r} 예요. 빈칸에 self 를 넣어요\"\nlines = [x.strip() for x in __out__.strip().splitlines()]\nassert lines == ['나는 팥 붕어빵', '나는 슈크림 붕어빵'], f\"출력이 '나는 팥 붕어빵', '나는 슈크림 붕어빵' 이어야 해요. 지금은 {lines} 예요\"","hints":["빈칸은 \"지금 이 붕어빵 자신\" 을 가리키는 이름","메서드 첫 칸에 적은 이름과 같아요","{self.filling}"],"after":"2주차 GCNLayer 의 forward 안 self.W 같은 줄이 바로 \"이 층 자신의 가중치 꺼내기\" 예요."},{"id":"c05-10","title":"고치기: 메서드 첫 칸 self 가 없어요","level":"fix","run":"browser","goal":"누를 때마다 숫자가 1씩 오르는 딸깍이인데 TypeError 가 나요. 한 곳을 고쳐요.","explain":["`__init__(self)` 처럼 self 만 받을 수도 있어요. 여기서는 속을 정해진 값 0 으로 넣어요.","에러 `click() takes 0 positional arguments but 1 was given` 은 \"0개 받는 함수에 1개가 들어왔다\" 는 뜻이에요.","그 1개는 파이썬이 몰래 넣은 self (c 자신) 예요. `c.click()` 은 속으로 self 칸에 c 를 넣어 부르거든요.","메서드를 만들 때 첫 칸 self 를 빠뜨리는 실수는 정말 흔해요. 이 에러를 보면 self 부터 확인해요."],"given":"class Clicker:\n    def __init__(self):\n        self.count = 0\n\n    def click():\n        self.count = self.count + 1\n\nc = Clicker()\nc.click()\nc.click()\nprint(c.count)","solution":"class Clicker:\n    def __init__(self):\n        self.count = 0\n\n    def click(self):\n        self.count = self.count + 1\n\nc = Clicker()\nc.click()\nc.click()\nprint(c.count)","tests":"assert 'Clicker' in globals(), \"Clicker 클래스가 있어야 해요\"\nc1 = Clicker()\nc2 = Clicker()\ntry:\n    c1.click()\nexcept TypeError:\n    assert False, \"click 을 부르면 TypeError 가 나요. def click(self): 처럼 첫 칸에 self 를 적어요\"\nc1.click()\nc1.click()\nc2.click()\nassert c1.count == 3, f\"c1 을 세 번 눌렀으니 3 이어야 해요. 지금은 {c1.count} 이에요\"\nassert c2.count == 1, f\"c2 는 한 번 눌렀으니 1 이어야 해요. 지금은 {c2.count} 이에요\"\nassert c.count == 2 and __out__.strip() == '2', \"c 를 두 번 눌렀으니 2 가 출력되어야 해요\"","hints":["에러가 난 메서드의 def 줄을 보세요","__init__ 은 첫 칸이 무엇인가요?","def click(self):"],"after":"PyTorch 에서 def forward(x): 라고 self 를 빼면 똑같은 TypeError 가 나요."},{"id":"c05-11","title":"직접 짜기: 이웃을 기억하는 Node","level":"write","run":"browser","goal":"이웃 이름을 모아 두고, 차수(이웃 수)를 돌려주는 Node 클래스를 직접 만들어요.","explain":["**이웃(Neighbor)** 은 선으로 바로 이어진 노드, **차수(Degree)** 는 이웃 수예요.","`self.neighbors = []` 로 노드마다 자기만의 빈 리스트를 만들어요.","메서드 안에서 `self.neighbors.append(other)` 로 이 노드의 리스트에 추가해요.","`degree` 는 `len(self.neighbors)` 를 돌려주면 돼요."],"given":"# 1. class Node: 를 만들어요\n# 2. def __init__(self, name):\n#    - self.name 에 name 을 붙여요\n#    - self.neighbors 에 빈 리스트 [] 를 붙여요\n# 3. def add_neighbor(self, other):\n#    - self.neighbors 에 other 를 append 해요\n# 4. def degree(self):\n#    - 이웃 리스트의 길이를 return 해요\nclass Node:\n    ...","solution":"class Node:\n    def __init__(self, name):\n        self.name = name\n        self.neighbors = []\n\n    def add_neighbor(self, other):\n        self.neighbors.append(other)\n\n    def degree(self):\n        return len(self.neighbors)","tests":"assert 'Node' in globals() and isinstance(Node, type), \"class Node: 로 Node 클래스를 만들어야 해요\"\ntry:\n    t1 = Node(\"A\")\n    t2 = Node(\"B\")\nexcept TypeError:\n    assert False, 'Node(\"A\") 처럼 이름 하나를 넣어 만들 수 있어야 해요. def __init__(self, name): 을 적어요'\nassert getattr(t1, 'name', None) == 'A', \"self.name = name 으로 이름을 붙여요\"\nassert getattr(t1, 'neighbors', None) == [], \"처음 만든 노드의 neighbors 는 빈 리스트 [] 여야 해요\"\nfor m in ['add_neighbor', 'degree']:\n    assert hasattr(t1, m), f\"Node 에 {m} 메서드를 만들어요. 첫 칸은 self 예요\"\nt1.add_neighbor(\"B\")\nt1.add_neighbor(\"C\")\nt2.add_neighbor(\"A\")\nassert t1.neighbors == [\"B\", \"C\"], f\"A 의 이웃은 ['B', 'C'] 여야 해요. 지금은 {t1.neighbors} 예요\"\nassert t2.neighbors == [\"A\"], f\"B 의 이웃은 ['A'] 여야 해요. 지금은 {t2.neighbors} 예요. 노드마다 리스트가 따로 있어야 해요 (__init__ 안에서 self.neighbors = [])\"\nd1 = t1.degree()\nassert d1 is not None, \"degree 가 None 을 돌려줘요. return len(self.neighbors) 를 써요\"\nassert d1 == 2 and t2.degree() == 1, f\"A 의 차수는 2, B 의 차수는 1 이어야 해요. 지금은 {d1}, {t2.degree()} 예요\"\nassert Node(\"Z\").degree() == 0, \"이웃이 없으면 차수는 0 이에요\"","hints":["메서드 세 개: __init__, add_neighbor, degree","self.neighbors = [] 는 __init__ 안에 적어요","add_neighbor 안: self.neighbors.append(other)","degree 안: return len(self.neighbors)"],"after":"GNN 은 노드마다 이웃 목록을 보고 이웃의 정보를 모아요. 그 이웃 목록이 바로 이 neighbors 예요."},{"id":"c05-12","title":"그래프 틀 만들기: add_edge","level":"type","run":"browser","goal":"노드 n 개짜리 그래프 틀을 만들고, 선을 잇는 메서드 add_edge 를 넣어요.","explain":["이번엔 노드 하나가 아니라 **그래프 전체** 를 담는 틀이에요.","`self.adj` 는 인접 리스트 딕셔너리예요. 키는 노드 번호, 값은 이웃 번호 리스트 (c02, c04 에서 본 모양).","`__init__(self, n)` 은 노드 0 부터 n-1 까지 빈 이웃 리스트를 만들어요.","`add_edge(self, u, v)` 는 선 하나를 이어요. 방향이 없는 그래프라 u 쪽에 v, v 쪽에 u 를 둘 다 넣어요."],"show":"class Graph:\n    def __init__(self, n):\n        self.adj = {}\n        for i in range(n):\n            self.adj[i] = []\n\n    def add_edge(self, u, v):\n        self.adj[u].append(v)\n        self.adj[v].append(u)\ng = Graph(3)\ng.add_edge(0, 1)\nprint(g.adj)","given":"","solution":"class Graph:\n    def __init__(self, n):\n        self.adj = {}\n        for i in range(n):\n            self.adj[i] = []\n\n    def add_edge(self, u, v):\n        self.adj[u].append(v)\n        self.adj[v].append(u)\ng = Graph(3)\ng.add_edge(0, 1)\nprint(g.adj)","tests":"assert 'Graph' in globals(), \"Graph 클래스가 있어야 해요\"\nassert 'g' in globals() and g.adj == {0: [1], 1: [0], 2: []}, f\"g.adj 는 {{0: [1], 1: [0], 2: []}} 이어야 해요. 지금은 {g.adj} 예요\"\nh = Graph(4)\nh.add_edge(0, 3)\nh.add_edge(1, 3)\nassert h.adj == {0: [3], 1: [3], 2: [], 3: [0, 1]}, f\"Graph(4) 에 0-3, 1-3 을 이으면 {{0: [3], 1: [3], 2: [], 3: [0, 1]}} 이어야 해요. 지금은 {h.adj} 예요\"\nassert g.adj == {0: [1], 1: [0], 2: []}, \"다른 그래프 h 를 만들어도 g 는 그대로여야 해요\"\nassert __out__.strip() == '{0: [1], 1: [0], 2: []}', \"g.adj 가 출력되어야 해요\"","hints":["for 는 __init__ 안이라 8칸, 그 안쪽 줄은 12칸 들여쓰기","add_edge 는 append 두 줄","g = Graph(3) 은 들여쓰기 없이"],"after":"2주차 레이블 전파와 1주차 networkx 의 G.add_edge(u, v) 가 속에서 이렇게 양쪽에 이웃을 적어요."},{"id":"c05-13","title":"그래프에 neighbors 와 degree 넣기","level":"blank","run":"browser","goal":"Graph 에 이웃 리스트를 돌려주는 neighbors 와 이웃 수를 돌려주는 degree 메서드를 완성해요.","explain":["`neighbors(self, v)` 는 노드 v 의 이웃 리스트, 곧 `self.adj[v]` 를 돌려줘요.","`degree(self, v)` 는 그 리스트의 길이예요.","메서드 안에서 같은 객체의 다른 메서드를 부를 때도 `self.neighbors(v)` 처럼 `self.` 을 붙여요."],"given":"class Graph:\n    def __init__(self, n):\n        self.adj = {}\n        for i in range(n):\n            self.adj[i] = []\n\n    def add_edge(self, u, v):\n        self.adj[u].append(v)\n        self.adj[v].append(u)\n\n    def neighbors(self, v):\n        return self.adj[___]\n\n    def degree(self, v):\n        return ___(self.neighbors(v))\n\ng = Graph(4)\ng.add_edge(0, 1)\ng.add_edge(0, 2)\ng.add_edge(2, 3)\nprint(g.neighbors(0), g.degree(0))","solution":"class Graph:\n    def __init__(self, n):\n        self.adj = {}\n        for i in range(n):\n            self.adj[i] = []\n\n    def add_edge(self, u, v):\n        self.adj[u].append(v)\n        self.adj[v].append(u)\n\n    def neighbors(self, v):\n        return self.adj[v]\n\n    def degree(self, v):\n        return len(self.neighbors(v))\n\ng = Graph(4)\ng.add_edge(0, 1)\ng.add_edge(0, 2)\ng.add_edge(2, 3)\nprint(g.neighbors(0), g.degree(0))","tests":"assert 'Graph' in globals(), \"Graph 클래스가 있어야 해요\"\nassert g.neighbors(0) == [1, 2], f\"g.neighbors(0) 은 [1, 2] 여야 해요. 지금은 {g.neighbors(0)} 이에요. 첫 빈칸은 v 예요\"\nassert g.degree(0) == 2 and g.degree(3) == 1, \"g.degree(0) 은 2, g.degree(3) 은 1 이어야 해요. 둘째 빈칸은 길이를 재는 함수예요\"\nh = Graph(3)\nh.add_edge(0, 1)\nh.add_edge(1, 2)\nassert h.neighbors(2) == [1], \"새 그래프 h 에서 h.neighbors(2) 는 [1] 이어야 해요\"\nassert h.degree(1) == 2 and h.degree(0) == 1, \"h.degree(1) 은 2, h.degree(0) 은 1 이어야 해요\"\nassert __out__.strip() == '[1, 2] 2', f\"'[1, 2] 2' 가 출력되어야 해요. 지금은 {__out__.strip()} 이에요\"","hints":["첫 빈칸은 메서드가 받은 노드 이름","둘째 빈칸은 리스트 길이를 재는 함수","return self.adj[v], return len(self.neighbors(v))"],"after":"networkx 의 G.neighbors(v), G.degree(v) 를 직접 만든 셈이에요."},{"id":"c05-14","title":"직접 짜기: 나만의 Graph 클래스","level":"write","run":"browser","goal":"노드 이름이 숫자든 글자든 되는 Graph 클래스를 처음부터 직접 만들어요.","explain":["이번 그래프는 처음에 노드가 하나도 없어요. `add_edge` 할 때 처음 보는 노드면 자리를 만들어 줘요.","`if u not in self.adj:` 는 \"u 가 아직 딕셔너리에 없으면\" 이라는 뜻이에요.","`self.adj = {}` 는 꼭 `__init__` 안에 써요. 그래야 그래프마다 자기 딕셔너리를 따로 가져요."],"given":"# 1. class Graph: 를 만들어요\n# 2. def __init__(self): self.adj 에 빈 딕셔너리를 붙여요\n# 3. def add_edge(self, u, v):\n#    - u 가 self.adj 에 없으면 self.adj[u] = [] 로 자리를 만들어요. v 도 똑같이\n#    - self.adj[u] 에 v 를, self.adj[v] 에 u 를 append 해요\n# 4. def neighbors(self, v): v 의 이웃 리스트를 return 해요\n# 5. def degree(self, v): v 의 이웃 수를 return 해요\n# 6. def num_nodes(self): 노드 수 (self.adj 의 길이) 를 return 해요\nclass Graph:\n    ...","solution":"class Graph:\n    def __init__(self):\n        self.adj = {}\n\n    def add_edge(self, u, v):\n        if u not in self.adj:\n            self.adj[u] = []\n        if v not in self.adj:\n            self.adj[v] = []\n        self.adj[u].append(v)\n        self.adj[v].append(u)\n\n    def neighbors(self, v):\n        return self.adj[v]\n\n    def degree(self, v):\n        return len(self.adj[v])\n\n    def num_nodes(self):\n        return len(self.adj)","tests":"assert 'Graph' in globals() and isinstance(Graph, type), \"class Graph: 로 Graph 클래스를 만들어야 해요\"\ntry:\n    g1 = Graph()\n    g2 = Graph()\nexcept TypeError:\n    assert False, \"Graph() 처럼 괄호 안을 비우고 만들 수 있어야 해요. def __init__(self): 에는 self 만 적어요\"\nassert hasattr(g1, 'adj') and g1.adj == {}, \"__init__ 안에서 self.adj = {} 로 빈 딕셔너리를 붙여요\"\nfor m in ['add_edge', 'neighbors', 'degree', 'num_nodes']:\n    assert hasattr(g1, m), f\"Graph 에 {m} 메서드를 만들어요. 첫 칸은 self 예요\"\ng1.add_edge(\"A\", \"B\")\ng1.add_edge(\"A\", \"C\")\ng1.add_edge(\"B\", \"C\")\ng2.add_edge(0, 1)\nassert g1.neighbors(\"A\") == [\"B\", \"C\"], f\"g1.neighbors('A') 는 ['B', 'C'] 여야 해요. 지금은 {g1.neighbors('A')} 예요\"\nassert g1.neighbors(\"C\") == [\"A\", \"B\"], \"무방향이라 add_edge 에서 양쪽 모두 append 해야 해요. g1.neighbors('C') 는 ['A', 'B']\"\nr = g1.degree(\"A\")\nassert r is not None, \"degree 가 None 을 돌려줘요. return 을 써요\"\nassert r == 2 and g1.degree(\"B\") == 2, \"g1 에서 A 와 B 의 차수는 2 여야 해요\"\nn1 = g1.num_nodes()\nassert n1 == 3, f\"g1 의 노드 수는 3 이어야 해요. 지금은 {n1} 이에요\"\nassert g2.num_nodes() == 2, f\"g2 의 노드 수는 2 여야 해요. 지금은 {g2.num_nodes()} 예요. self.adj 는 __init__ 안에서 만들어야 그래프마다 따로 생겨요\"\nassert g2.degree(1) == 1 and g2.neighbors(0) == [1], \"g2 에서 0 과 1 은 서로 이웃이어야 해요\"","hints":["메서드 다섯 개: __init__, add_edge, neighbors, degree, num_nodes","add_edge 안에 if u not in self.adj: self.adj[u] = [] 를 먼저 두 번(u, v)","degree 는 return len(self.adj[v])","num_nodes 는 return len(self.adj)"],"after":"PyG 의 Data 객체도 이렇게 그래프를 담은 클래스예요. 다음 단원에서 PyTorch 모델 틀로 넘어가요."}]};
