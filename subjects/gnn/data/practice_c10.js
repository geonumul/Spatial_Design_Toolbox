window.GNN_PRACTICE=window.GNN_PRACTICE||{};window.GNN_PRACTICE["c10"]={"id":"c10","track":"c","unit":"wc-10","title":"nn.Module 로 모델 만들기","intro":"wc-6 에서 순수 파이썬으로 만든 \"Module 흉내\" 를, 이번엔 PyTorch 가 준 진짜 기본 틀 nn.Module 로 만들어요. 층을 속성으로 달고, 파라미터를 세고, 마지막엔 두 층 모델을 혼자 짜요. 전부 Google Colab 에서 열려요.","source":"","exercises":[{"id":"c10-01","title":"PyTorch 가 준 기본 틀 물려받기","level":"type","run":"colab","goal":"wc-6 에서 직접 만든 Layer 부모 틀 대신, PyTorch 가 준 기본 틀 nn.Module 을 물려받아 \"받은 값에 2 곱하기\" 모델을 만들어요.","explain":["이 세트는 Google Colab 에서 열려요. 코드 칸에 치고 실행(Shift+Enter)한 뒤, 아래 채점 칸을 실행하면 돼요.","`import torch.nn as nn` 은 PyTorch 의 신경망 도구 상자(torch.nn)를 `nn` 이라는 짧은 이름으로 불러와요.","`class Double(nn.Module):` 는 **PyTorch 가 준 기본 붕어빵 틀 nn.Module** 을 물려받는다는 뜻이에요. wc-6 의 `class MyLayer(Layer):` 와 같은 모양이에요.","`super().__init__()` 는 \"부모 틀(nn.Module)의 준비 작업을 먼저 해 줘\" 라는 줄이에요. nn.Module 을 물려받으면 `__init__` 첫 줄에 꼭 써요.","`forward` 는 데이터가 들어오면 할 일이에요. 여기서는 받은 값 x 에 2를 곱해 돌려줘요.","`model(텐서)` 처럼 객체를 함수처럼 부르면, wc-6 의 `__call__` 처럼 nn.Module 이 알아서 forward 를 불러 줘요."],"show":"import torch\nimport torch.nn as nn\n\nclass Double(nn.Module):\n    def __init__(self):\n        super().__init__()\n\n    def forward(self, x):\n        return x * 2\n\nmodel = Double()\nout = model(torch.tensor([1.0, 2.0, 3.0]))","given":"","solution":"import torch\nimport torch.nn as nn\n\nclass Double(nn.Module):\n    def __init__(self):\n        super().__init__()\n\n    def forward(self, x):\n        return x * 2\n\nmodel = Double()\nout = model(torch.tensor([1.0, 2.0, 3.0]))","tests":"import torch\nimport torch.nn as nn\nassert 'Double' in globals(), \"Double 클래스를 만들어야 해요. class Double(nn.Module): 로 시작해요\"\nassert isinstance(Double, type) and issubclass(Double, nn.Module), \"class Double(nn.Module): 처럼 괄호 안에 nn.Module 을 넣어 물려받아야 해요\"\nassert 'model' in globals() and isinstance(model, Double), \"model = Double() 로 객체를 만들어야 해요\"\nassert 'out' in globals() and torch.is_tensor(out), \"out = model(torch.tensor([1.0, 2.0, 3.0])) 로 결과를 받아야 해요\"\nassert out.shape == (3,) and torch.allclose(out, torch.tensor([2.0, 4.0, 6.0])), f\"out 은 [2, 4, 6] 이어야 해요. 지금은 {out.tolist()} 이에요\"\nassert torch.allclose(model(torch.tensor([5.0, -1.0])), torch.tensor([10.0, -2.0])), \"forward 는 x * 2 를 return 해야 해요\"","hints":["class 이름 뒤 괄호 안에 부모 틀 nn.Module 을 넣어요","__init__ 의 첫 줄은 super().__init__() 예요","forward 안은 return x * 2 한 줄이에요"],"after":"2주차 PracticeCode_2 의 GCNLayer, ScratchGCN, PyGGCN 이 전부 이 모양 `class 이름(nn.Module):` 으로 시작해요."},{"id":"c10-02","title":"빈칸: 기본 틀과 부모 준비","level":"blank","run":"colab","goal":"받은 값에 1을 더하는 모델 AddOne 을 완성해요. 빈칸 ___ 세 곳을 채워요.","explain":["첫 빈칸은 물려받을 부모 틀 이름이에요. PyTorch 모델의 부모 틀은 항상 같아요.","둘째 빈칸은 `__init__` 첫 줄에 쓰는 \"부모 준비 작업 불러오기\" 예요.","셋째 빈칸은 forward 가 돌려줄 값이에요. 텐서에 `+ 1` 을 하면 모든 칸에 1이 더해져요."],"show":"","given":"import torch\nimport torch.nn as nn\n\nclass AddOne(___):\n    def __init__(self):\n        ___\n\n    def forward(self, x):\n        return ___\n\nmodel = AddOne()\nout = model(torch.tensor([1.0, 5.0]))\nprint(out)","solution":"import torch\nimport torch.nn as nn\n\nclass AddOne(nn.Module):\n    def __init__(self):\n        super().__init__()\n\n    def forward(self, x):\n        return x + 1\n\nmodel = AddOne()\nout = model(torch.tensor([1.0, 5.0]))\nprint(out)","tests":"import torch\nimport torch.nn as nn\nassert 'AddOne' in globals(), \"AddOne 클래스가 있어야 해요\"\nassert isinstance(AddOne, type) and issubclass(AddOne, nn.Module), \"첫 빈칸은 nn.Module 이에요\"\nassert 'out' in globals() and torch.is_tensor(out), \"out = model(...) 결과가 텐서여야 해요\"\nassert torch.allclose(out, torch.tensor([2.0, 6.0])), f\"out 은 [2, 6] 이어야 해요. 지금은 {out.tolist()} 이에요\"\nassert torch.allclose(AddOne()(torch.tensor([0.0, -1.0, 10.0])), torch.tensor([1.0, 0.0, 11.0])), \"forward 는 x + 1 을 돌려줘야 해요\"","hints":["01 실습과 똑같은 틀이에요","부모 틀은 nn.Module, 부모 준비는 super().__init__()","마지막 빈칸은 x + 1"],"after":"수업 노트북의 모든 모델 클래스가 이 세 줄(nn.Module, super().__init__(), forward)을 똑같이 가지고 있어요."},{"id":"c10-03","title":"층을 속성으로 달기: nn.Linear","level":"type","run":"colab","goal":"숫자 3개를 받아 2개로 바꾸는 층(nn.Linear)을 모델 안에 속성으로 달고, 노드 4개짜리 입력을 통과시켜요.","explain":["`nn.Linear(3, 2)` 는 **숫자 3개를 받아 숫자 2개로 바꾸는 층**이에요. 속에 곱할 숫자 표(가중치, weight)와 더할 숫자(bias)가 들어 있고, 학습하면서 이 숫자들이 바뀌어요.","`self.lin = nn.Linear(3, 2)` 는 붕어빵에 속을 넣듯이, 모델 객체에 층을 `lin` 이라는 이름으로 달아 두는 줄이에요.","`forward` 안의 `self.lin(x)` 는 달아 둔 층에 x 를 통과시켜요. 층도 nn.Module 이라 함수처럼 부를 수 있어요.","`torch.ones(4, 3)` 은 1로 채운 4행 3열 표예요. 행 하나가 노드(데이터) 하나, 열이 특징 하나라고 생각해요. 결과 out 은 4행 2열이 돼요.","`torch.manual_seed(0)` 은 층의 무작위 시작 숫자를 매번 똑같이 만드는 줄이에요(wc-4 의 random.seed 와 같은 뜻)."],"show":"import torch\nimport torch.nn as nn\n\nclass OneLayer(nn.Module):\n    def __init__(self):\n        super().__init__()\n        self.lin = nn.Linear(3, 2)\n    def forward(self, x):\n        return self.lin(x)\n\ntorch.manual_seed(0)\nmodel = OneLayer()\nout = model(torch.ones(4, 3))","given":"","solution":"import torch\nimport torch.nn as nn\n\nclass OneLayer(nn.Module):\n    def __init__(self):\n        super().__init__()\n        self.lin = nn.Linear(3, 2)\n    def forward(self, x):\n        return self.lin(x)\n\ntorch.manual_seed(0)\nmodel = OneLayer()\nout = model(torch.ones(4, 3))","tests":"import torch\nimport torch.nn as nn\nassert 'OneLayer' in globals() and 'model' in globals(), \"OneLayer 클래스와 model 객체가 있어야 해요\"\nassert isinstance(getattr(model, 'lin', None), nn.Linear), \"__init__ 안에서 self.lin = nn.Linear(3, 2) 로 층을 달아야 해요\"\nassert (model.lin.in_features, model.lin.out_features) == (3, 2), f\"nn.Linear(3, 2) 여야 해요. 지금은 nn.Linear({model.lin.in_features}, {model.lin.out_features}) 예요\"\nassert 'out' in globals() and torch.is_tensor(out), \"out = model(torch.ones(4, 3)) 이 있어야 해요\"\nassert tuple(out.shape) == (4, 2), f\"out 모양은 (4, 2) 여야 해요. 지금은 {tuple(out.shape)} 예요\"\n_x = torch.randn(5, 3)\nassert torch.allclose(model(_x), model.lin(_x)), \"forward 는 self.lin(x) 를 return 해야 해요\"","hints":["층은 __init__ 안에서 self.이름 = ... 으로 달아요","forward 에서는 self.lin(x) 를 돌려줘요","입력 표는 torch.ones(4, 3) 이에요"],"after":"2주차 GCNLayer 의 `self.W = nn.Linear(in_dim, out_dim, bias=False)` 가 바로 이 \"층을 속성으로 달기\" 예요."},{"id":"c10-04","title":"빈칸: 입력 개수와 출력 개수","level":"blank","run":"colab","goal":"노드 5개가 특징을 4개씩 가지고 있어요. 특징 4개를 3개로 줄이는 모델 Encoder 를 완성해요.","explain":["`nn.Linear(입력 개수, 출력 개수)` 순서예요. 입력 개수는 x 의 **열 수**(특징 수)와 같아야 해요.","`x = torch.ones(5, 4)` 는 5행(노드 5개) 4열(특징 4개)이에요.","forward 는 달아 둔 층에 x 를 통과시킨 결과를 돌려줘요."],"show":"","given":"import torch\nimport torch.nn as nn\n\nclass Encoder(nn.Module):\n    def __init__(self):\n        super().__init__()\n        # 특징 4개를 받아 3개로 바꾸는 층\n        self.lin = nn.Linear(___, ___)\n\n    def forward(self, x):\n        return ___\n\nx = torch.ones(5, 4)\nmodel = Encoder()\nout = model(x)\nprint(out.shape)","solution":"import torch\nimport torch.nn as nn\n\nclass Encoder(nn.Module):\n    def __init__(self):\n        super().__init__()\n        # 특징 4개를 받아 3개로 바꾸는 층\n        self.lin = nn.Linear(4, 3)\n\n    def forward(self, x):\n        return self.lin(x)\n\nx = torch.ones(5, 4)\nmodel = Encoder()\nout = model(x)\nprint(out.shape)","tests":"import torch\nimport torch.nn as nn\nassert 'model' in globals() and isinstance(getattr(model, 'lin', None), nn.Linear), \"self.lin = nn.Linear(...) 이 있어야 해요\"\nassert model.lin.in_features == 4, f\"입력 개수는 x 의 열 수 4 예요. 지금은 {model.lin.in_features} 예요\"\nassert model.lin.out_features == 3, f\"출력 개수는 3 이에요. 지금은 {model.lin.out_features} 예요\"\nassert 'out' in globals() and torch.is_tensor(out), \"forward 가 텐서를 돌려줘야 해요. return self.lin(x)\"\nassert tuple(out.shape) == (5, 3), f\"out 모양은 (5, 3) 이어야 해요. 지금은 {tuple(out.shape)} 예요\"\n_x = torch.randn(2, 4)\nassert torch.allclose(model(_x), model.lin(_x)), \"forward 는 self.lin(x) 를 돌려줘야 해요\"","hints":["x 는 5행 4열이에요. 층이 받는 건 열 수예요","nn.Linear(4, 3)","return self.lin(x)"],"after":"2주차 노트북의 `GCNLayer(in_dim, hidden_dim)` 에서 in_dim 이 노드 특징 수, hidden_dim 이 바꾼 뒤 특징 수예요."},{"id":"c10-05","title":"고치기: super().__init__() 가 빠졌어요","level":"fix","run":"colab","goal":"실행하면 AttributeError 가 나요. 에러 메시지를 읽고 빠진 한 줄을 넣어 고쳐요.","explain":["에러가 나는 건 정상이에요. 메시지 **마지막 줄**부터 읽어요: `cannot assign module before Module.__init__() call` 는 \"부모 준비 작업(Module.__init__)을 부르기 전에 층을 달 수 없어요\" 라는 뜻이에요.","nn.Module 은 `super().__init__()` 에서 층을 넣어 둘 보관함을 만들어요. 이 줄이 없으면 `self.lin = ...` 을 넣을 곳이 없어요.","그래서 `super().__init__()` 는 반드시 `self.lin = ...` **보다 위**, `__init__` 첫 줄에 써요."],"show":"","given":"import torch\nimport torch.nn as nn\n\nclass Tiny(nn.Module):\n    def __init__(self):\n        self.lin = nn.Linear(3, 1)\n\n    def forward(self, x):\n        return self.lin(x)\n\ntorch.manual_seed(0)\nmodel = Tiny()\nout = model(torch.ones(2, 3))\nprint(out)","solution":"import torch\nimport torch.nn as nn\n\nclass Tiny(nn.Module):\n    def __init__(self):\n        super().__init__()\n        self.lin = nn.Linear(3, 1)\n\n    def forward(self, x):\n        return self.lin(x)\n\ntorch.manual_seed(0)\nmodel = Tiny()\nout = model(torch.ones(2, 3))\nprint(out)","tests":"import torch\nimport torch.nn as nn\nassert 'model' in globals(), \"model = Tiny() 까지 실행되어야 해요. super().__init__() 를 __init__ 첫 줄에 넣어 보세요\"\nassert isinstance(getattr(model, 'lin', None), nn.Linear), \"self.lin = nn.Linear(3, 1) 은 그대로 두세요\"\nassert len(list(model.parameters())) == 2, \"층이 모델에 제대로 등록되어야 해요. super().__init__() 가 self.lin 보다 위에 있어야 해요\"\nassert 'out' in globals() and tuple(out.shape) == (2, 1), \"out 모양은 (2, 1) 이어야 해요\"","hints":["에러 메시지에 나오는 Module.__init__() 를 부르는 방법이 무엇이었죠?","__init__ 안, self.lin 줄 위에 한 줄을 넣어요","super().__init__()"],"after":"초보가 가장 많이 하는 실수예요. 2주차 노트북 모든 클래스의 `__init__` 첫 줄이 `super().__init__()` 인 이유예요."},{"id":"c10-06","title":"고치기: model.forward(x) 말고 model(x)","level":"fix","run":"colab","goal":"모델을 `model.forward(x)` 로 직접 불렀어요. 결과 숫자는 같아 보여도 PyTorch 가 해 주는 준비 과정을 건너뛰어요. `model(x)` 로 고쳐요.","explain":["wc-6 에서 `__call__` 이 forward 를 부르게 만들었죠. nn.Module 의 `__call__` 은 forward 를 부르기 **전후에** PyTorch 의 부가 작업(기록 장치 등)도 함께 해요.","그래서 약속은 항상 `model(x)` 예요. `model.forward(x)` 로 부르면 그 부가 작업이 빠져요.","시작 코드의 `calls` 두 줄은 채점용 기록 장치예요. `model(x)` 로 부를 때만 calls 에 1 이 쌓여요. 이해하지 않아도 되니 지우지만 마세요."],"show":"","given":"import torch\nimport torch.nn as nn\n\nclass Small(nn.Module):\n    def __init__(self):\n        super().__init__()\n        self.lin = nn.Linear(2, 2)\n\n    def forward(self, x):\n        return self.lin(x)\n\ntorch.manual_seed(0)\nmodel = Small()\n# 채점용 기록 장치 (지우지 마세요): model(x) 로 부를 때만 calls 에 1 이 쌓여요\ncalls = []\nmodel.register_forward_hook(lambda m, inp, outp: calls.append(1))\n\nx = torch.ones(3, 2)\nout = model.forward(x)\nprint(out.shape, len(calls))","solution":"import torch\nimport torch.nn as nn\n\nclass Small(nn.Module):\n    def __init__(self):\n        super().__init__()\n        self.lin = nn.Linear(2, 2)\n\n    def forward(self, x):\n        return self.lin(x)\n\ntorch.manual_seed(0)\nmodel = Small()\n# 채점용 기록 장치 (지우지 마세요): model(x) 로 부를 때만 calls 에 1 이 쌓여요\ncalls = []\nmodel.register_forward_hook(lambda m, inp, outp: calls.append(1))\n\nx = torch.ones(3, 2)\nout = model(x)\nprint(out.shape, len(calls))","tests":"import torch\nassert 'calls' in globals(), \"calls = [] 기록 장치 줄은 지우지 마세요\"\nassert 'out' in globals() and torch.is_tensor(out), \"out = model(x) 가 있어야 해요\"\nassert tuple(out.shape) == (3, 2), f\"out 모양은 (3, 2) 여야 해요. 지금은 {tuple(out.shape)} 예요\"\nassert len(calls) == 1, f\"model(x) 로 한 번 불러야 기록 장치가 한 번 켜져요. 지금 기록은 {len(calls)}번이에요. model.forward(x) 를 model(x) 로 바꿔 보세요\"","hints":["forward 를 직접 부르는 줄을 찾아요","객체를 함수처럼 부르면 __call__ 이 forward 를 대신 불러요","out = model(x)"],"after":"2주차 노트북도 전부 `model(x, support)`, `self.conv1(x, edge_index)` 처럼 부르고 `.forward(` 는 쓰지 않아요."},{"id":"c10-07","title":"parameters: 학습으로 바뀌는 숫자 세기","level":"type","run":"colab","goal":"모델 안에서 학습으로 바뀌는 숫자 표(파라미터)의 이름과 모양을 출력하고, 숫자가 모두 몇 개인지 세요.","explain":["**파라미터(Parameter)** 는 학습하면서 고쳐지는 숫자들이에요. nn.Linear 안의 weight 와 bias 가 파라미터예요.","`model.named_parameters()` 는 (이름, 숫자 표) 짝을 하나씩 꺼내 줘요. 이름은 `lin.weight`, `lin.bias` 처럼 나와요.","`p.shape` 은 표의 모양, `p.numel()` 은 표 안 숫자 개수예요(number of elements).","`sum(p.numel() for p in model.parameters())` 는 wc-3 의 리스트 컴프리헨션과 같은 모양으로, 모든 표의 숫자 개수를 더해요.","nn.Linear(3, 2) 는 weight 가 2행 3열(6개), bias 가 2개라서 모두 8개예요."],"show":"import torch.nn as nn\n\nclass OneLayer(nn.Module):\n    def __init__(self):\n        super().__init__()\n        self.lin = nn.Linear(3, 2)\n    def forward(self, x):\n        return self.lin(x)\n\nmodel = OneLayer()\nfor name, p in model.named_parameters():\n    print(name, p.shape)\ncount = sum(p.numel() for p in model.parameters())","given":"","solution":"import torch.nn as nn\n\nclass OneLayer(nn.Module):\n    def __init__(self):\n        super().__init__()\n        self.lin = nn.Linear(3, 2)\n    def forward(self, x):\n        return self.lin(x)\n\nmodel = OneLayer()\nfor name, p in model.named_parameters():\n    print(name, p.shape)\ncount = sum(p.numel() for p in model.parameters())","tests":"import torch.nn as nn\nassert 'model' in globals() and isinstance(getattr(model, 'lin', None), nn.Linear), \"model 과 self.lin = nn.Linear(3, 2) 가 있어야 해요\"\nnames = [n for n, _ in model.named_parameters()]\nassert names == ['lin.weight', 'lin.bias'], f\"파라미터 이름은 ['lin.weight', 'lin.bias'] 여야 해요. 지금은 {names} 예요\"\nassert 'count' in globals(), \"count = sum(p.numel() for p in model.parameters()) 줄이 있어야 해요\"\nassert count == 8, f\"count 는 8 이어야 해요(weight 6개 + bias 2개). 지금은 {count} 예요\"","hints":["for 이름, 표 in model.named_parameters(): 로 하나씩 꺼내요","숫자 개수는 p.numel()","count = sum(p.numel() for p in model.parameters())"],"after":"2주차 노트북 셀 60 마지막 줄 `print(f\"Total parameters: {sum(p.numel() for p in custom_model.parameters())}\")` 와 같은 코드예요."},{"id":"c10-08","title":"빈칸: weight 와 bias 의 모양 맞히기","level":"blank","run":"colab","goal":"nn.Linear(4, 3) 의 weight 모양, bias 모양, 전체 숫자 개수를 직접 계산해서 빈칸에 써요.","explain":["헷갈리는 점: nn.Linear(입력, 출력) 의 weight 모양은 **(출력 개수, 입력 개수)** 예요. 순서가 거꾸로예요.","bias 는 출력 하나마다 하나씩 더하는 숫자라서 모양이 (출력 개수,) 예요. 쉼표가 있어야 칸 하나짜리 튜플이에요(wc-2).","전체 숫자 개수 = weight 칸 수(행 x 열) + bias 칸 수."],"show":"","given":"import torch.nn as nn\n\nlin = nn.Linear(4, 3)\n\n# weight 는 (출력 개수, 입력 개수) 모양이에요\nw_shape = (___, ___)\n# bias 는 출력 개수만큼 있어요\nb_shape = (___,)\n# 전체 숫자 개수 = weight 칸 수 + bias 칸 수\nn_params = ___\n\nprint(tuple(lin.weight.shape), tuple(lin.bias.shape))","solution":"import torch.nn as nn\n\nlin = nn.Linear(4, 3)\n\n# weight 는 (출력 개수, 입력 개수) 모양이에요\nw_shape = (3, 4)\n# bias 는 출력 개수만큼 있어요\nb_shape = (3,)\n# 전체 숫자 개수 = weight 칸 수 + bias 칸 수\nn_params = 3 * 4 + 3\n\nprint(tuple(lin.weight.shape), tuple(lin.bias.shape))","tests":"assert 'w_shape' in globals() and 'b_shape' in globals() and 'n_params' in globals(), \"w_shape, b_shape, n_params 세 이름을 지우지 마세요\"\nassert tuple(w_shape) == (3, 4), f\"weight 모양은 (출력 3, 입력 4) = (3, 4) 예요. 지금은 {w_shape} 예요\"\nassert tuple(b_shape) == (3,), f\"bias 모양은 (3,) 이에요. 지금은 {b_shape} 예요\"\nassert n_params == 15, f\"전체 숫자는 3*4 + 3 = 15 개예요. 지금은 {n_params} 예요\"","hints":["출력이 먼저, 입력이 나중이에요","weight (3, 4), bias (3,)","n_params = 3 * 4 + 3"],"after":"2주차 노트북에서 가중치를 직접 만들 때도 `torch.randn(34, 16)` 처럼 모양을 정해요. 모양을 맞히는 연습이 코드 읽기의 절반이에요."},{"id":"c10-09","title":"ReLU: F.relu 와 torch.relu","level":"type","run":"colab","goal":"음수는 0으로, 양수는 그대로 두는 ReLU 를 두 가지 방법으로 써 봐요.","explain":["**ReLU** 는 \"음수면 0, 아니면 그대로\" 하는 함수예요. wc-7 의 `np.maximum(0, x)` 와 같은 일을 텐서에 해요.","`import torch.nn.functional as F` 는 층 객체 없이 바로 쓰는 함수 모음(torch.nn.functional)을 `F` 라는 짧은 이름으로 불러와요.","`F.relu(x)` 와 `torch.relu(x)` 는 결과가 똑같아요. 노트북에 둘 다 나와서 둘 다 알아 두면 좋아요.","층과 층 사이에 ReLU 를 끼우는 이유: 곱하기(선형 층)만 여러 번 하면 결국 곱하기 한 번과 같아져서, 중간에 꺾어 주는 함수가 필요해요."],"show":"import torch\nimport torch.nn.functional as F\n\nx = torch.tensor([-2.0, -0.5, 0.0, 1.5, 3.0])\na = F.relu(x)\nb = torch.relu(x)\nprint(a)\nprint(b)","given":"","solution":"import torch\nimport torch.nn.functional as F\n\nx = torch.tensor([-2.0, -0.5, 0.0, 1.5, 3.0])\na = F.relu(x)\nb = torch.relu(x)\nprint(a)\nprint(b)","tests":"import torch\nassert 'a' in globals() and 'b' in globals(), \"a = F.relu(x), b = torch.relu(x) 두 줄이 있어야 해요\"\n_want = torch.tensor([0.0, 0.0, 0.0, 1.5, 3.0])\nassert torch.is_tensor(a) and torch.allclose(a, _want), f\"a 는 [0, 0, 0, 1.5, 3] 이어야 해요. 지금은 {a}\"\nassert torch.is_tensor(b) and torch.allclose(b, _want), f\"b 는 [0, 0, 0, 1.5, 3] 이어야 해요. 지금은 {b}\"","hints":["F 는 torch.nn.functional 의 짧은 이름이에요","a = F.relu(x)","b = torch.relu(x)"],"after":"2주차 GCNLayer 는 `torch.relu(out)`, PyGGCN 은 `F.relu(h)` 를 써요. 같은 일이에요."},{"id":"c10-10","title":"빈칸: 층 두 개 사이에 ReLU","level":"blank","run":"colab","goal":"특징 4개 → 8개 → 2개로 바꾸는 두 층 모델을 완성해요. 두 층 사이에 ReLU 를 끼워요.","explain":["첫 층이 8개를 내보내니까, 둘째 층은 **8개를 받아야** 해요. 앞 층 출력 개수 = 뒤 층 입력 개수.","`h = F.relu(self.lin1(x))` 는 안쪽부터 읽어요: x 를 lin1 에 통과시키고, 그 결과에 ReLU 를 씌워서 h 라는 이름표를 붙여요.","마지막 층 뒤에는 ReLU 를 씌우지 않아요. 반마다 점수를 음수까지 그대로 내보내야 하거든요."],"show":"","given":"import torch\nimport torch.nn as nn\nimport torch.nn.functional as F\n\nclass TwoLayer(nn.Module):\n    def __init__(self):\n        super().__init__()\n        self.lin1 = nn.Linear(4, 8)\n        self.lin2 = nn.Linear(___, 2)\n\n    def forward(self, x):\n        h = ___(self.lin1(x))\n        return ___\n\ntorch.manual_seed(0)\nmodel = TwoLayer()\nout = model(torch.ones(5, 4))\nprint(out.shape)","solution":"import torch\nimport torch.nn as nn\nimport torch.nn.functional as F\n\nclass TwoLayer(nn.Module):\n    def __init__(self):\n        super().__init__()\n        self.lin1 = nn.Linear(4, 8)\n        self.lin2 = nn.Linear(8, 2)\n\n    def forward(self, x):\n        h = F.relu(self.lin1(x))\n        return self.lin2(h)\n\ntorch.manual_seed(0)\nmodel = TwoLayer()\nout = model(torch.ones(5, 4))\nprint(out.shape)","tests":"import torch\nimport torch.nn as nn\nassert 'model' in globals() and isinstance(getattr(model, 'lin2', None), nn.Linear), \"self.lin2 = nn.Linear(8, 2) 가 있어야 해요\"\nassert model.lin2.in_features == 8, f\"lin2 는 lin1 의 출력 8개를 받아야 해요. 지금은 {model.lin2.in_features} 예요\"\nassert 'out' in globals() and torch.is_tensor(out) and tuple(out.shape) == (5, 2), \"out 모양은 (5, 2) 여야 해요. forward 마지막 줄은 return self.lin2(h)\"\ntorch.manual_seed(3)\n_x = torch.randn(6, 4)\n_want = model.lin2(torch.relu(model.lin1(_x)))\nassert torch.allclose(model(_x), _want, atol=1e-6), \"forward 순서는 lin1 → ReLU → lin2 예요. h = F.relu(self.lin1(x)) 인지 확인해요\"","hints":["앞 층 출력 개수 = 뒤 층 입력 개수","ReLU 함수 이름은 F.relu","return self.lin2(h)"],"after":"2주차 PyGGCN.forward 의 `h = self.conv1(...)`, `h = F.relu(h)`, `logits = self.conv2(...)` 가 같은 흐름이에요."},{"id":"c10-11","title":"nn.Sequential: 층을 순서대로 끼운 줄","level":"type","run":"colab","goal":"클래스를 만들지 않고, 층을 순서대로 끼워 10 실습과 같은 모델을 한 번에 만들어요.","explain":["`nn.Sequential(층1, 층2, 층3)` 은 **층을 순서대로 끼운 줄**이에요. 넣으면 층1 → 층2 → 층3 순서로 통과해요.","`nn.ReLU()` 는 F.relu 의 층 객체 버전이에요. 줄에 끼우려면 괄호를 붙여 객체로 만들어요.","`model[0]` 처럼 번호로 층을 꺼낼 수 있어요(리스트처럼 0부터).","헷갈리는 점: 그래프 모델은 forward 에 x 말고 A_hat 이나 edge_index 도 같이 넣어야 해서, 노트북에서는 Sequential 대신 class 를 써요."],"show":"import torch\nimport torch.nn as nn\n\ntorch.manual_seed(0)\nmodel = nn.Sequential(\n    nn.Linear(4, 8),\n    nn.ReLU(),\n    nn.Linear(8, 2),\n)\nout = model(torch.ones(5, 4))\ncount = sum(p.numel() for p in model.parameters())\nprint(model)","given":"","solution":"import torch\nimport torch.nn as nn\n\ntorch.manual_seed(0)\nmodel = nn.Sequential(\n    nn.Linear(4, 8),\n    nn.ReLU(),\n    nn.Linear(8, 2),\n)\nout = model(torch.ones(5, 4))\ncount = sum(p.numel() for p in model.parameters())\nprint(model)","tests":"import torch\nimport torch.nn as nn\nassert 'model' in globals() and isinstance(model, nn.Sequential), \"model = nn.Sequential(...) 로 만들어야 해요\"\nassert len(model) == 3, f\"층이 3개(Linear, ReLU, Linear) 여야 해요. 지금은 {len(model)}개예요\"\nassert isinstance(model[1], nn.ReLU), \"가운데 층은 nn.ReLU() 예요\"\nassert 'out' in globals() and tuple(out.shape) == (5, 2), \"out 모양은 (5, 2) 여야 해요\"\nassert 'count' in globals() and count == 58, f\"파라미터는 4*8+8 + 8*2+2 = 58 개예요. 지금 count 는 {globals().get('count')} 예요\"","hints":["nn.Sequential( 안에 층을 쉼표로 이어요","ReLU 는 nn.ReLU() 처럼 괄호를 붙여요","count 는 07 실습과 같은 줄이에요"],"after":"3주차 이후 노트북의 MLP(여러 층 신경망) 부분에서 nn.Sequential 을 자주 만나요."},{"id":"c10-12","title":"직접 짜기: MLP 클래스","level":"write","run":"colab","goal":"입력 개수, 중간 개수, 출력 개수를 받아 두 층 모델을 만드는 MLP 클래스를 주석 단계만 보고 직접 짜요.","explain":["10 실습의 TwoLayer 에서 숫자 4, 8, 2 를 매개변수 in_dim, hidden_dim, out_dim 으로 바꾼 것이에요.","`__init__(self, in_dim, hidden_dim, out_dim)` 처럼 self 뒤에 받을 값을 늘어놓아요. 모델을 만들 때 `MLP(4, 8, 2)` 로 넘겨요.","**MLP(Multi-Layer Perceptron)** 는 선형 층과 ReLU 를 번갈아 쌓은 가장 기본 신경망의 이름이에요.","채점은 여러 크기로 모델을 만들어 모양, 파라미터 수, 계산 순서를 확인해요."],"show":"","given":"import torch\nimport torch.nn as nn\nimport torch.nn.functional as F\n\n# 1. nn.Module 을 물려받는 클래스 MLP 를 만들어요\n# 2. __init__(self, in_dim, hidden_dim, out_dim) 첫 줄에서 부모 준비를 불러요\n# 3. self.lin1 = in_dim 개를 hidden_dim 개로 바꾸는 nn.Linear\n# 4. self.lin2 = hidden_dim 개를 out_dim 개로 바꾸는 nn.Linear\n# 5. forward(self, x): x 를 lin1 에 통과시키고 F.relu 를 씌운 뒤, lin2 에 통과시켜 return\nclass MLP(nn.Module):\n    ...\n\ntorch.manual_seed(0)\nmodel = MLP(4, 8, 2)\nout = model(torch.ones(5, 4))\nprint(out.shape)","solution":"import torch\nimport torch.nn as nn\nimport torch.nn.functional as F\n\n# 1. nn.Module 을 물려받는 클래스 MLP 를 만들어요\n# 2. __init__(self, in_dim, hidden_dim, out_dim) 첫 줄에서 부모 준비를 불러요\n# 3. self.lin1 = in_dim 개를 hidden_dim 개로 바꾸는 nn.Linear\n# 4. self.lin2 = hidden_dim 개를 out_dim 개로 바꾸는 nn.Linear\n# 5. forward(self, x): x 를 lin1 에 통과시키고 F.relu 를 씌운 뒤, lin2 에 통과시켜 return\nclass MLP(nn.Module):\n    def __init__(self, in_dim, hidden_dim, out_dim):\n        super().__init__()\n        self.lin1 = nn.Linear(in_dim, hidden_dim)\n        self.lin2 = nn.Linear(hidden_dim, out_dim)\n\n    def forward(self, x):\n        h = F.relu(self.lin1(x))\n        return self.lin2(h)\n\ntorch.manual_seed(0)\nmodel = MLP(4, 8, 2)\nout = model(torch.ones(5, 4))\nprint(out.shape)","tests":"import torch\nimport torch.nn as nn\nassert 'MLP' in globals(), \"MLP 클래스를 만들어야 해요\"\nassert isinstance(MLP, type) and issubclass(MLP, nn.Module), \"class MLP(nn.Module): 로 물려받아야 해요\"\nfor (_i, _h, _o) in [(4, 8, 2), (3, 5, 1), (10, 16, 4)]:\n    _m = MLP(_i, _h, _o)\n    assert isinstance(getattr(_m, 'lin1', None), nn.Linear), \"__init__ 안에 self.lin1 = nn.Linear(in_dim, hidden_dim) 이 있어야 해요\"\n    assert isinstance(getattr(_m, 'lin2', None), nn.Linear), \"__init__ 안에 self.lin2 = nn.Linear(hidden_dim, out_dim) 이 있어야 해요\"\n    assert (_m.lin1.in_features, _m.lin1.out_features) == (_i, _h), f\"MLP({_i}, {_h}, {_o}) 의 lin1 은 nn.Linear({_i}, {_h}) 여야 해요. 지금은 nn.Linear({_m.lin1.in_features}, {_m.lin1.out_features}) 예요\"\n    assert (_m.lin2.in_features, _m.lin2.out_features) == (_h, _o), f\"MLP({_i}, {_h}, {_o}) 의 lin2 는 nn.Linear({_h}, {_o}) 여야 해요. 지금은 nn.Linear({_m.lin2.in_features}, {_m.lin2.out_features}) 예요\"\n    _n = sum(p.numel() for p in _m.parameters())\n    assert _n == _i * _h + _h + _h * _o + _o, f\"MLP({_i}, {_h}, {_o}) 의 파라미터는 {_i * _h + _h + _h * _o + _o}개여야 해요. 지금은 {_n}개예요. 층을 더 만들지 않았는지 봐요\"\n    _x = torch.randn(7, _i)\n    _y = _m(_x)\n    assert torch.is_tensor(_y) and tuple(_y.shape) == (7, _o), f\"입력 (7, {_i}) 를 넣으면 출력은 (7, {_o}) 여야 해요. forward 가 return 하는지 확인해요\"\n    assert torch.allclose(_y, _m.lin2(torch.relu(_m.lin1(_x))), atol=1e-6), \"forward 순서는 lin1 → F.relu → lin2 예요. 마지막 층 뒤에는 ReLU 를 씌우지 않아요\"","hints":["10 실습 TwoLayer 를 떠올려요. 숫자만 매개변수로 바꾸면 돼요","def __init__(self, in_dim, hidden_dim, out_dim): 다음 줄은 super().__init__()","self.lin1 = nn.Linear(in_dim, hidden_dim), self.lin2 = nn.Linear(hidden_dim, out_dim)","forward: h = F.relu(self.lin1(x)) 다음 return self.lin2(h)"],"after":"2주차 `BasicGNN(in_dim, hidden_dim, num_classes)` 와 `ScratchGCN(in_dim, hidden_dim, num_classes)` 가 이 MLP 와 똑같은 뼈대에 그래프 섞기만 더한 거예요."},{"id":"c10-13","title":"직접 짜기: Sequential 로 같은 모델 만드는 함수","level":"write","run":"colab","goal":"12 실습의 MLP 와 같은 모델을 nn.Sequential 로 만들어 돌려주는 함수 make_mlp 를 짜요.","explain":["함수가 모델을 만들어 return 할 수도 있어요. wc-4 의 자판기처럼, 크기를 넣으면 모델이 나와요.","같은 seed 로 만들면 class 로 만든 MLP 와 Sequential 로 만든 모델의 시작 숫자가 똑같아요. 층을 만드는 순서가 같기 때문이에요. 채점도 이걸로 확인해요."],"show":"","given":"import torch\nimport torch.nn as nn\n\n# 1. 함수 make_mlp(in_dim, hidden_dim, out_dim) 를 만들어요\n# 2. nn.Sequential 안에 순서대로 넣어요:\n#    nn.Linear(in_dim, hidden_dim), nn.ReLU(), nn.Linear(hidden_dim, out_dim)\n# 3. 만든 Sequential 을 return 해요\ndef make_mlp(in_dim, hidden_dim, out_dim):\n    ...\n\ntorch.manual_seed(0)\nmodel = make_mlp(4, 8, 2)\nout = model(torch.ones(5, 4))\nprint(model)","solution":"import torch\nimport torch.nn as nn\n\n# 1. 함수 make_mlp(in_dim, hidden_dim, out_dim) 를 만들어요\n# 2. nn.Sequential 안에 순서대로 넣어요:\n#    nn.Linear(in_dim, hidden_dim), nn.ReLU(), nn.Linear(hidden_dim, out_dim)\n# 3. 만든 Sequential 을 return 해요\ndef make_mlp(in_dim, hidden_dim, out_dim):\n    return nn.Sequential(\n        nn.Linear(in_dim, hidden_dim),\n        nn.ReLU(),\n        nn.Linear(hidden_dim, out_dim),\n    )\n\ntorch.manual_seed(0)\nmodel = make_mlp(4, 8, 2)\nout = model(torch.ones(5, 4))\nprint(model)","tests":"import torch\nimport torch.nn as nn\nassert 'make_mlp' in globals() and callable(make_mlp), \"함수 make_mlp 를 만들어야 해요\"\nfor (_i, _h, _o) in [(4, 8, 2), (3, 5, 1)]:\n    torch.manual_seed(1)\n    _m = make_mlp(_i, _h, _o)\n    assert isinstance(_m, nn.Sequential), \"nn.Sequential(...) 을 return 해야 해요. return 을 빠뜨리지 않았나 봐요\"\n    assert len(_m) == 3, f\"층이 3개(Linear, ReLU, Linear) 여야 해요. 지금은 {len(_m)}개예요\"\n    assert isinstance(_m[0], nn.Linear) and isinstance(_m[1], nn.ReLU) and isinstance(_m[2], nn.Linear), \"순서는 nn.Linear, nn.ReLU(), nn.Linear 예요\"\n    assert (_m[0].in_features, _m[0].out_features, _m[2].in_features, _m[2].out_features) == (_i, _h, _h, _o), f\"make_mlp({_i}, {_h}, {_o}) 는 Linear({_i}, {_h}) 와 Linear({_h}, {_o}) 를 가져야 해요\"\n    torch.manual_seed(1)\n    _ref = nn.Sequential(nn.Linear(_i, _h), nn.ReLU(), nn.Linear(_h, _o))\n    _x = torch.randn(4, _i)\n    assert torch.allclose(_m(_x), _ref(_x), atol=1e-6), \"같은 seed 로 만든 기준 모델과 결과가 달라요. 층 순서와 크기를 확인해요\"","hints":["함수 안에서 nn.Sequential(...) 을 만들어 바로 return 해요","숫자 대신 in_dim, hidden_dim, out_dim 을 넣어요","return nn.Sequential(nn.Linear(in_dim, hidden_dim), nn.ReLU(), nn.Linear(hidden_dim, out_dim))"],"after":"이제 모델을 만들 줄 알아요. 다음 c11 에서는 이 모델이 스스로 숫자를 고치게 하는 학습 루프를 짜요."}]};
