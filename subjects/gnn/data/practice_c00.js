window.GNN_PRACTICE=window.GNN_PRACTICE||{};window.GNN_PRACTICE["c00"]={"id":"c00","track":"c","unit":"wc-0","title":"코드 짜기 전에 생각하기","intro":"코드를 치기 전에 입력과 출력, 손 계산, 한국어 단계부터 적어요. 순서 맞추기, 손으로 먼저, 슈도코드 먼저 실습으로 그래프 코드를 혼자 짜 봐요.","source":"","exercises":[{"id":"c00-01","title":"차수 세기 슈도코드 순서 맞추기","level":"order","run":"browser","goal":"엣지 리스트로 노드마다 차수를 세는 한국어 단계가 섞여 있어요. 눌러서 바른 순서로 쌓아요.","explain":["입력은 `edges = [(0, 1), (1, 2), (1, 3)]` 같은 엣지 리스트, 출력은 `{노드: 차수}` 딕셔너리예요.","\"(반복 안)\" 이 붙은 줄은 반복문 안쪽에서 하는 일이에요."],"order":["빈 표 deg 를 만든다","엣지 (u, v) 를 하나씩 본다","(반복 안) u 와 v 각각, 표에 칸이 없으면 0 으로 만든다","(반복 안) 그 칸을 1 늘린다","반복이 끝나면 표 deg 를 돌려준다"],"hints":["끝까지 기억할 표는 언제 만들어야 할까요?","\"엣지마다\" 줄이 반복의 시작이에요. 반복 안 줄은 그 뒤에 와요.","빈 표, 엣지마다, 칸 만들기, 1 늘리기, 보여 주기 순서예요."],"after":"이 순서를 주석으로 먼저 쳐 두면 다음 실습에서 코드가 금방 나와요."},{"id":"c00-02","title":"표가 어떻게 바뀌나 손으로 먼저","level":"predict","run":"browser","goal":"실행하기 전에 출력 세 줄을 손으로 먼저 적어요. 엣지 하나를 볼 때마다 표가 어떻게 바뀌는지 따라가요.","explain":["종이에 `deg` 표를 그리고, 엣지 하나를 볼 때마다 칸 숫자를 고쳐 적어요.","딕셔너리는 처음 넣은 순서대로 출력돼요."],"show":"edges = [(0, 1), (1, 2), (1, 3)]\ndeg = {}\nfor u, v in edges:\n    for x in (u, v):\n        if x not in deg:\n            deg[x] = 0\n        deg[x] += 1\n    print(u, v, deg)","answer":"0 1 {0: 1, 1: 1}\n1 2 {0: 1, 1: 2, 2: 1}\n1 3 {0: 1, 1: 3, 2: 1, 3: 1}","think":["입력은 엣지 세 개예요. 엣지마다 print 가 한 번이라 출력은 세 줄이에요.","(0, 1): 0 칸, 1 칸이 생기고 둘 다 1 이에요.","(1, 2): 1 칸은 2, 2 칸이 생겨 1 이에요.","(1, 3): 1 칸은 3, 3 칸이 생겨 1 이에요. 각 줄 앞에는 u, v 를 먼저 적어요."],"hints":["첫 엣지 (0, 1) 뒤에는 0 칸과 1 칸이 1 이에요.","print 는 바깥 for 안에 있어서 엣지마다 한 번, 모두 세 번 실행돼요.","둘째 줄은 `1 2 {0: 1, 1: 2, 2: 1}` 이에요."],"after":"손 계산과 print 결과를 한 줄씩 맞추는 습관이 디버깅의 절반이에요."},{"id":"c00-03","title":"슈도코드 보고 degree_dict 짜기","level":"write","run":"browser","goal":"왼쪽(위) 슈도코드를 한 줄씩 코드로 옮겨서 함수 `degree_dict(edges)` 를 완성해요. 노드마다 차수를 담은 딕셔너리를 돌려줘요.","explain":["`def degree_dict(edges):` 아래 줄은 전부 스페이스 4칸 들여써요.","print 가 아니라 `return deg` 로 돌려줘야 채점돼요."],"show":"","given":"def degree_dict(edges):\n    # 여기에 슈도코드를 한 줄씩 코드로 옮겨요\n    ...\n\nprint(degree_dict([(0, 1), (1, 2), (1, 3)]))","solution":"def degree_dict(edges):\n    deg = {}\n    for u, v in edges:\n        for x in (u, v):\n            if x not in deg:\n                deg[x] = 0\n            deg[x] += 1\n    return deg\n\nprint(degree_dict([(0, 1), (1, 2), (1, 3)]))","tests":"assert 'degree_dict' in globals(), \"degree_dict 함수를 만들어야 해요\"\nr = degree_dict([(0, 1), (1, 2), (1, 3)])\nassert isinstance(r, dict), f\"딕셔너리를 return 해야 해요. 지금은 {type(r).__name__} 이에요. 마지막에 return deg 를 썼는지 봐요\"\nassert r == {0: 1, 1: 3, 2: 1, 3: 1}, f\"[(0, 1), (1, 2), (1, 3)] 의 차수는 {{0: 1, 1: 3, 2: 1, 3: 1}} 이에요. 지금은 {r} 이에요\"\nr2 = degree_dict([(5, 6)])\nassert r2 == {5: 1, 6: 1}, f\"[(5, 6)] 이면 {{5: 1, 6: 1}} 이어야 해요. 지금은 {r2} 이에요\"\nassert degree_dict([]) == {}, \"엣지가 없으면 빈 딕셔너리 {} 를 돌려줘야 해요\"","pseudo_show":true,"pseudo":"빈 표 deg 를 만든다\n엣지 (u, v) 마다\n    u 와 v 각각\n        표에 칸이 없으면 0 으로 만든다\n        그 칸을 1 늘린다\ndeg 를 돌려준다","think":["입력은 튜플 리스트 `edges`, 출력은 `{노드: 차수}` 딕셔너리예요.","작은 예 `[(0, 1), (1, 2)]` 는 `{0: 1, 1: 2, 2: 1}` 이에요.","반복이 끝나도 남아야 하는 deg 는 반복 전에 만들어요."],"hints":["슈도코드 한 줄을 주석으로 먼저 쳐 두고 그 아래에 코드를 써요.","\"칸이 없으면\" 은 `if x not in deg:` 예요.","반복 두 겹: `for u, v in edges:` 안에 `for x in (u, v):`","마지막 줄은 반복 밖, 함수 안에서 `return deg`"],"after":"1주차 PracticeCode_1 셀 59 도 이 누적 뼈대로 차수를 구해요."},{"id":"c00-04","title":"최대 찾기 슈도코드 순서 맞추기","level":"order","run":"browser","goal":"차수가 가장 큰 노드를 찾는 한국어 단계를 바른 순서로 쌓아요.","explain":["입력은 `{노드: 차수}` 딕셔너리, 출력은 차수가 가장 큰 노드와 그 차수예요."],"order":["지금까지 최고 노드는 없음, 최고 차수는 -1 로 기억한다","노드와 차수를 한 쌍씩 본다","(반복 안) 이번 차수가 지금까지 최고 차수보다 큰지 묻는다","(반복 안, 크면) 최고 노드와 최고 차수를 이번 것으로 바꾼다","반복이 끝나면 최고 노드와 최고 차수를 돌려준다"],"hints":["\"지금까지 최고\" 는 반복이 끝나도 기억해야 해요.","비교는 반복 안에서, 바꾸기는 비교가 참일 때만 해요.","기억할 값 두 개 만들기, 하나씩 보기, 비교, 바꾸기, 돌려주기 순서예요."],"after":"2주차 PracticeCode_2 셀 15 `np.argmax(degrees)` 가 이 일을 한 줄로 해 줘요."},{"id":"c00-05","title":"슈도코드 먼저 적고 max_degree_node 짜기","level":"write","run":"browser","goal":"먼저 한국어로 단계를 적고 모범 슈도코드와 비교해요. 그다음 `max_degree_node(deg)` 를 짜요. 차수가 가장 큰 노드와 그 차수를 `(노드, 차수)` 튜플로 돌려줘요. 같은 차수면 먼저 본 노드를 돌려줘요.","explain":["`return a, b` 는 두 값을 튜플 `(a, b)` 로 돌려줘요."],"show":"","given":"def max_degree_node(deg):\n    # 1단계에서 적은 한국어 순서를 주석으로 옮기고 그 아래를 채워요\n    ...\n\nprint(max_degree_node({0: 1, 1: 3, 2: 1, 3: 1}))","solution":"def max_degree_node(deg):\n    best_node = None\n    best_deg = -1\n    for node, d in deg.items():\n        if d > best_deg:\n            best_node = node\n            best_deg = d\n    return best_node, best_deg\n\nprint(max_degree_node({0: 1, 1: 3, 2: 1, 3: 1}))","tests":"assert 'max_degree_node' in globals(), \"max_degree_node 함수를 만들어야 해요\"\nr = max_degree_node({0: 1, 1: 3, 2: 1, 3: 1})\nassert r == (1, 3), f\"{{0: 1, 1: 3, 2: 1, 3: 1}} 이면 (1, 3) 이어야 해요. 지금은 {r!r} 이에요. return best_node, best_deg 처럼 두 값을 돌려줘요\"\nr2 = max_degree_node({7: 2, 8: 5, 9: 5})\nassert r2 == (8, 5), f\"같은 차수 5 가 둘이면 먼저 본 8 이어야 해요. 지금은 {r2!r} 이에요. >= 대신 > 를 써요\"\nr3 = max_degree_node({4: 0})\nassert r3 == (4, 0), f\"노드 하나에 차수 0 이면 (4, 0) 이어야 해요. 지금은 {r3!r} 이에요. 처음 최고 차수를 -1 로 두었는지 봐요\"","pseudo_first":true,"pseudo":"최고 노드는 없음, 최고 차수는 -1 로 기억한다\n노드와 차수를 한 쌍씩 보면서\n    이번 차수가 최고 차수보다 크면\n        최고 노드와 최고 차수를 바꾼다\n최고 노드와 최고 차수를 돌려준다","think":["입력은 `{노드: 차수}` 딕셔너리, 출력은 `(노드, 차수)` 튜플이에요.","작은 예 `{0: 1, 1: 3, 2: 3}` 은 1 과 2 가 같은 3 이라 먼저 본 1 을 골라 `(1, 3)` 이에요.","끝까지 기억할 값은 최고 노드와 최고 차수 두 개예요.","같은 값일 때 안 바꾸려면 `>` 를 써요. `>=` 면 뒤 노드로 바뀌어요."],"hints":["기억할 값 두 개를 반복 전에 만들어요: `best_node = None`, `best_deg = -1`","`for node, d in deg.items():` 로 한 쌍씩 봐요.","`if d > best_deg:` 일 때만 두 값을 바꿔요. `>=` 를 쓰면 나중 노드로 바뀌어요.","`return best_node, best_deg`"],"after":"같은 차수일 때 무엇을 고를지 정하는 것도 1단계(입력과 출력 정하기)에 들어가요."},{"id":"c00-06","title":"차수 1 인 노드만 모으기","level":"blank","run":"browser","goal":"빈칸을 채워 차수가 1 인 노드(잎)만 리스트로 모으는 `leaves(deg)` 를 완성해요.","explain":["빈칸 `___` 세 개를 채워요."],"show":"","given":"def leaves(deg):\n    result = ___\n    for node, d in deg.items():\n        if ___:\n            ___\n    return result\n\nprint(leaves({0: 1, 1: 3, 2: 1, 3: 1}))","solution":"def leaves(deg):\n    result = []\n    for node, d in deg.items():\n        if d == 1:\n            result.append(node)\n    return result\n\nprint(leaves({0: 1, 1: 3, 2: 1, 3: 1}))","tests":"assert 'leaves' in globals(), \"leaves 함수가 있어야 해요\"\nr = leaves({0: 1, 1: 3, 2: 1, 3: 1})\nassert r == [0, 2, 3], f\"[0, 2, 3] 이어야 해요. 지금은 {r!r} 이에요\"\nr2 = leaves({5: 2, 6: 2})\nassert r2 == [], f\"차수 1 인 노드가 없으면 빈 리스트여야 해요. 지금은 {r2!r} 이에요\"","think":["입력은 `{노드: 차수}`, 출력은 차수가 1 인 노드 번호 리스트예요.","작은 예 `{0: 1, 1: 3, 2: 1}` 은 `[0, 2]` 예요.","조건 찾기 패턴이에요. 빈 리스트로 시작하고 조건이 맞을 때만 담아요."],"pseudo":"빈 리스트 result 를 만든다\n노드와 차수를 한 쌍씩 보면서\n    차수가 1 이면 result 에 노드를 담는다\nresult 를 돌려준다","hints":["조건 찾기는 빈 리스트로 시작해요.","조건은 `d == 1` 이에요. `=` 하나는 넣기라서 오류가 나요.","맞으면 `result.append(node)`"],"after":"조건 찾기 뼈대는 2주차 train_mask 로 일부 노드만 고를 때와 같은 생각이에요."},{"id":"c00-07","title":"슈도코드 먼저 적고 색 세기","level":"write","run":"browser","goal":"먼저 한국어로 단계를 적고 비교한 다음, `count_colors(colors)` 를 짜요. `{노드: 색}` 을 받아 `{색: 노드 수}` 를 돌려줘요.","explain":["키가 글자인 딕셔너리도 숫자 키와 똑같이 써요."],"show":"","given":"def count_colors(colors):\n    # 1단계에서 적은 순서를 주석으로 옮기고 그 아래를 채워요\n    ...\n\nprint(count_colors({0: \"빨강\", 1: \"파랑\", 2: \"빨강\"}))","solution":"def count_colors(colors):\n    count = {}\n    for node, c in colors.items():\n        if c not in count:\n            count[c] = 0\n        count[c] += 1\n    return count\n\nprint(count_colors({0: \"빨강\", 1: \"파랑\", 2: \"빨강\"}))","tests":"assert 'count_colors' in globals(), \"count_colors 함수를 만들어야 해요\"\nr = count_colors({0: \"빨강\", 1: \"파랑\", 2: \"빨강\"})\nassert r == {\"빨강\": 2, \"파랑\": 1}, f\"{{'빨강': 2, '파랑': 1}} 이어야 해요. 지금은 {r!r} 이에요\"\nr2 = count_colors({})\nassert r2 == {}, f\"빈 딕셔너리면 빈 딕셔너리를 돌려줘야 해요. 지금은 {r2!r} 이에요\"\nr3 = count_colors({1: 3, 2: 3, 3: 3})\nassert r3 == {3: 3}, f\"색 3 이 세 번이면 {{3: 3}} 이어야 해요. 지금은 {r3!r} 이에요\"","pseudo_first":true,"pseudo":"빈 표 count 를 만든다\n노드와 색을 한 쌍씩 보면서\n    표에 그 색 칸이 없으면 0 으로 만든다\n    그 색 칸을 1 늘린다\ncount 를 돌려준다","think":["입력은 `{노드: 색}`, 출력은 `{색: 개수}` 예요.","작은 예 `{0: 'a', 1: 'b', 2: 'a'}` 는 `{'a': 2, 'b': 1}` 이에요.","딕셔너리로 세기 패턴이에요. 칸이 없으면 만들고 1 늘려요."],"hints":["차수 세기와 뼈대가 같아요. 세는 대상이 노드가 아니라 색이에요.","`for node, c in colors.items():`","`if c not in count: count[c] = 0` 다음 `count[c] += 1`"],"after":"3주차 WL 색 히스토그램 `Counter(colors.values())` 가 이 함수와 같은 일을 해요."},{"id":"c00-08","title":"이웃 값 합과 평균 손으로 먼저","level":"predict","run":"browser","goal":"실행하기 전에 출력을 손으로 적어요. 노드마다 이웃 값을 더하고 이웃 수로 나눠요.","explain":["출력 한 줄은 `노드 합 평균` 순서예요. 나누기 `/` 결과는 소수라서 `.0` 이 붙어요."],"show":"neighbors = {0: [1, 2], 1: [0], 2: [0]}\nh = {0: 10, 1: 1, 2: 4}\nfor v in neighbors:\n    s = 0\n    for u in neighbors[v]:\n        s += h[u]\n    print(v, s, s / len(neighbors[v]))","answer":"0 5 2.5\n1 10 10.0\n2 10 10.0","think":["노드 0: 이웃 1, 2 의 값 1, 4 를 더해 5, 두 개라 2.5.","노드 1: 이웃 0 의 값 10 하나라 10, 10.0.","노드 2: 이웃 0 의 값 10 하나라 10, 10.0.","s 는 노드마다 0 에서 다시 시작해요. 바깥 for 안에 있어서요."],"hints":["노드 0 의 이웃은 1, 2 예요. 자기 자신 값은 더하지 않아요.","노드 1 의 이웃은 0 하나라서 합도 평균도 10 이에요.","첫 줄은 `0 5 2.5` 예요."],"after":"메시지 패싱의 모으기(aggregate)가 바로 이 계산이에요."},{"id":"c00-09","title":"이웃 평균 슈도코드 순서 맞추기","level":"order","run":"browser","goal":"노드마다 이웃 값의 평균을 새 표에 담는 단계를 바른 순서로 쌓아요.","explain":["\"(바깥 반복 안)\" 은 노드마다, \"(안쪽 반복 안)\" 은 이웃마다 하는 일이에요."],"order":["결과를 담을 빈 표 new_h 를 만든다","노드 v 를 하나씩 본다","(바깥 반복 안) 합 s 를 0 으로 둔다","(안쪽 반복 안) v 의 이웃 u 마다 h[u] 를 s 에 더한다","(바깥 반복 안) s 를 이웃 수로 나눠 new_h[v] 에 담는다","반복이 끝나면 new_h 를 돌려준다"],"hints":["새 표는 모든 노드가 끝날 때까지 남아야 해요.","합 s 는 노드마다 새로 시작해요. 그래서 \"노드마다\" 바로 뒤예요.","새 표, 노드마다, 합 0, 이웃마다 더하기, 나눠 담기, 돌려주기"],"after":"2주차 레이블 전파 `sum(Y[u] for u in neighbors) / len(neighbors)` 가 가운데 세 줄을 한 줄로 쓴 거예요."},{"id":"c00-10","title":"슈도코드 보고 neighbor_mean 짜기","level":"write","run":"browser","goal":"슈도코드를 한 줄씩 옮겨 `neighbor_mean(neighbors, h)` 를 짜요. 노드마다 이웃 값 평균을 담은 새 딕셔너리를 돌려줘요. 이웃이 없는 노드는 0.0 이에요.","explain":["`continue` 는 반복 안의 나머지 줄을 건너뛰고 다음 차례로 가요.","입력 h 를 바로 고치지 말고 새 딕셔너리에 담아요."],"show":"","given":"def neighbor_mean(neighbors, h):\n    # 여기에 슈도코드를 한 줄씩 코드로 옮겨요\n    ...\n\nprint(neighbor_mean({0: [1, 2], 1: [0], 2: [0], 3: []}, {0: 10, 1: 1, 2: 4, 3: 7}))","solution":"def neighbor_mean(neighbors, h):\n    new_h = {}\n    for v in neighbors:\n        if not neighbors[v]:\n            new_h[v] = 0.0\n            continue\n        s = 0\n        for u in neighbors[v]:\n            s += h[u]\n        new_h[v] = s / len(neighbors[v])\n    return new_h\n\nprint(neighbor_mean({0: [1, 2], 1: [0], 2: [0], 3: []}, {0: 10, 1: 1, 2: 4, 3: 7}))","tests":"assert 'neighbor_mean' in globals(), \"neighbor_mean 함수를 만들어야 해요\"\nh = {0: 10, 1: 1, 2: 4, 3: 7}\nr = neighbor_mean({0: [1, 2], 1: [0], 2: [0], 3: []}, h)\nassert isinstance(r, dict), f\"딕셔너리를 return 해야 해요. 지금은 {type(r).__name__} 이에요\"\nassert abs(r.get(0, -1) - 2.5) < 1e-9, f\"노드 0 은 (1 + 4) / 2 = 2.5 여야 해요. 지금은 {r.get(0)} 이에요\"\nassert abs(r.get(1, -1) - 10) < 1e-9 and abs(r.get(2, -1) - 10) < 1e-9, f\"노드 1, 2 는 10.0 이어야 해요. 지금은 {r.get(1)}, {r.get(2)} 이에요\"\nassert r.get(3) == 0.0, f\"이웃이 없는 노드 3 은 0.0 이어야 해요. 지금은 {r.get(3)} 이에요\"\nassert h == {0: 10, 1: 1, 2: 4, 3: 7}, \"입력 h 를 바꾸면 안 돼요. 새 딕셔너리에 담아요\"\nr2 = neighbor_mean({5: [6, 7], 6: [5], 7: [5]}, {5: 0, 6: 3, 7: 5})\nassert abs(r2[5] - 4.0) < 1e-9 and abs(r2[6]) < 1e-9, f\"다른 그래프에서도 맞아야 해요. 지금은 {r2} 이에요\"","pseudo_show":true,"pseudo":"빈 표 new_h 를 만든다\n노드 v 마다\n    이웃이 없으면 new_h[v] 에 0.0 을 담고 다음 노드로 간다\n    합 s 를 0 으로 둔다\n    이웃 u 마다 h[u] 를 s 에 더한다\n    new_h[v] 에 s 나누기 이웃 수를 담는다\nnew_h 를 돌려준다","think":["입력은 이웃 목록 딕셔너리와 노드 값 딕셔너리 h, 출력은 새 딕셔너리예요.","작은 예 `{0: [1], 1: [0], 2: []}`, `h = {0: 2, 1: 6, 2: 9}` 이면 `{0: 6.0, 1: 2.0, 2: 0.0}` 이에요.","이웃이 0 개면 나누기에서 ZeroDivisionError 가 나요. 먼저 걸러요."],"hints":["바깥 반복 `for v in neighbors:`, 안쪽 반복 `for u in neighbors[v]:`","이웃이 없는지는 `if not neighbors[v]:` 또는 `len(neighbors[v]) == 0`","이웃이 없으면 `new_h[v] = 0.0` 하고 `continue` 로 다음 노드로 가요.","나머지는 `new_h[v] = s / len(neighbors[v])`"],"after":"2주차 PracticeCode_2 셀 19 레이블 전파도 `if not neighbors:` 로 이웃 없는 노드를 먼저 걸러요."},{"id":"c00-11","title":"합이 이상해요: 어디서 막혔는지 나누기","level":"fix","run":"browser","goal":"노드마다 이웃 값 합을 구하는 `neighbor_sum` 이 틀린 값을 내요. 손 계산과 비교해서 틀린 줄 하나를 옮겨요.","explain":["먼저 [실행]으로 출력을 보고, 손 계산 `{0: 5, 1: 10, 2: 10}` 과 비교해요.","막히면 `print(v, s)` 를 반복 안에 넣어 봐요."],"show":"","given":"def neighbor_sum(neighbors, h):\n    result = {}\n    s = 0\n    for v in neighbors:\n        for u in neighbors[v]:\n            s += h[u]\n        result[v] = s\n    return result\n\nprint(neighbor_sum({0: [1, 2], 1: [0], 2: [0]}, {0: 10, 1: 1, 2: 4}))","solution":"def neighbor_sum(neighbors, h):\n    result = {}\n    for v in neighbors:\n        s = 0\n        for u in neighbors[v]:\n            s += h[u]\n        result[v] = s\n    return result\n\nprint(neighbor_sum({0: [1, 2], 1: [0], 2: [0]}, {0: 10, 1: 1, 2: 4}))","tests":"assert 'neighbor_sum' in globals(), \"neighbor_sum 함수가 있어야 해요\"\nr = neighbor_sum({0: [1, 2], 1: [0], 2: [0]}, {0: 10, 1: 1, 2: 4})\nassert r == {0: 5, 1: 10, 2: 10}, f\"{{0: 5, 1: 10, 2: 10}} 이어야 해요. 지금은 {r} 이에요. s = 0 이 노드마다 다시 실행되는 자리에 있는지 봐요\"\nr2 = neighbor_sum({3: [4], 4: [3]}, {3: 2, 4: 2})\nassert r2 == {3: 2, 4: 2}, f\"다른 그래프에서 {{3: 2, 4: 2}} 이어야 해요. 지금은 {r2} 이에요\"","think":["결과가 달라요: 7단계, 어디서 막혔는지 나누기예요.","손 계산: 노드 0 은 1 + 4 = 5, 노드 1 은 10, 노드 2 는 10.","코드 출력은 5, 15, 25 예요. 노드 1 부터 앞 노드 합이 섞여 있어요.","앞 합이 남는다는 건 s 가 노드마다 0 으로 돌아가지 않는다는 뜻이에요. s 를 만드는 줄 위치를 봐요."],"pseudo":"결과 표를 만든다\n노드 v 마다\n    합 s 를 0 으로 둔다  (여기가 제자리)\n    이웃 u 마다 h[u] 를 s 에 더한다\n    결과 표에 s 를 담는다\n결과 표를 돌려준다","hints":["노드 1 의 이웃은 0 하나라 합은 10 이어야 해요. 코드는 몇이 나오나요?","합 s 가 노드마다 새로 시작하나요?","`s = 0` 을 `for v in neighbors:` 바로 아래(안쪽)로 옮겨요."],"after":"결과가 다를 때는 반복 안 print 로 몇 번째 반복부터 달라지는지 찾으면 틀린 줄이 보여요."},{"id":"c00-12","title":"슈도코드 먼저: 엣지 리스트로 이웃 목록 만들기","level":"write","run":"browser","goal":"먼저 한국어 단계를 적고 비교한 다음 `adj_list(edges)` 를 짜요. 무방향 엣지 리스트를 받아 `{노드: [이웃, ...]}` 를 돌려줘요. 이웃은 엣지에 나온 순서대로 담아요.","explain":["이번 실습은 앞의 두 패턴(딕셔너리로 세기, 조건 없이 담기)을 합쳐요."],"show":"","given":"def adj_list(edges):\n    # 1단계에서 적은 순서를 주석으로 옮기고 그 아래를 채워요\n    ...\n\nprint(adj_list([(0, 1), (1, 2), (1, 3)]))","solution":"def adj_list(edges):\n    adj = {}\n    for u, v in edges:\n        for a, b in ((u, v), (v, u)):\n            if a not in adj:\n                adj[a] = []\n            adj[a].append(b)\n    return adj\n\nprint(adj_list([(0, 1), (1, 2), (1, 3)]))","tests":"assert 'adj_list' in globals(), \"adj_list 함수를 만들어야 해요\"\nr = adj_list([(0, 1), (1, 2), (1, 3)])\nassert isinstance(r, dict), f\"딕셔너리를 return 해야 해요. 지금은 {type(r).__name__} 이에요\"\nassert r == {0: [1], 1: [0, 2, 3], 2: [1], 3: [1]}, f\"{{0: [1], 1: [0, 2, 3], 2: [1], 3: [1]}} 이어야 해요. 지금은 {r} 이에요. 두 방향 모두 담았는지 봐요\"\nr2 = adj_list([(4, 5)])\nassert r2 == {4: [5], 5: [4]}, f\"[(4, 5)] 면 {{4: [5], 5: [4]}} 이어야 해요. 지금은 {r2} 이에요\"","pseudo_first":true,"pseudo":"빈 표 adj 를 만든다\n엣지 (u, v) 마다\n    (u 에서 v), (v 에서 u) 두 방향 각각\n        출발 노드 칸이 없으면 빈 리스트를 만든다\n        출발 노드 목록에 도착 노드를 담는다\nadj 를 돌려준다","think":["입력은 엣지 튜플 리스트, 출력은 `{노드: 이웃 리스트}` 예요.","작은 예 `[(0, 1), (1, 2)]` 는 `{0: [1], 1: [0, 2], 2: [1]}` 이에요.","딕셔너리로 세기와 같은 뼈대인데, 칸에 숫자 대신 리스트를 두고 append 해요.","무방향이라 한 엣지에 두 번 담아요."],"hints":["차수 세기와 뼈대가 같아요. 0 대신 빈 리스트, 1 늘리기 대신 append 예요.","무방향이라 u 의 목록에 v, v 의 목록에 u 를 둘 다 담아요.","`for a, b in ((u, v), (v, u)):` 로 두 방향을 한 번에 돌 수 있어요.","`if a not in adj: adj[a] = []` 다음 `adj[a].append(b)`"],"after":"이 이웃 목록이 있으면 앞에서 짠 neighbor_mean 을 바로 쓸 수 있어요. 작은 함수들이 이렇게 이어져요."}]};
