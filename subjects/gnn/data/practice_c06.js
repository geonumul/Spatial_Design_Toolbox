window.GNN_PRACTICE=window.GNN_PRACTICE||{};window.GNN_PRACTICE["c06"]={"id":"c06","track":"c","unit":"wc-6","title":"클래스와 객체 2","intro":"기본 틀을 물려받아 조금 바꾸는 법(상속)을 배워요. 마지막엔 순수 파이썬으로 PyTorch nn.Module 흉내를 직접 만들어서, 나중에 모델 코드가 낯설지 않게 해요.","source":"","exercises":[{"id":"c06-01","title":"기본 틀 물려받기: 상속","level":"type","run":"browser","goal":"Animal 틀을 물려받은 Dog 틀을 만들어요. Dog 안에는 아무것도 안 넣어도 인사를 할 수 있어요.","explain":["새 붕어빵 틀을 만들 때 기본 틀을 통째로 물려받고 조금만 바꾸고 싶을 때가 있어요. 이걸 **상속(Inheritance)** 이라고 해요.","`class Dog(Animal):` 의 괄호 안 Animal 이 **부모 클래스**, Dog 가 **자식 클래스** 예요.","Dog 안에는 `pass` 뿐인데도 `Dog(\"바둑이\")` 로 찍고 `d.hello()` 를 부를 수 있어요. `__init__` 과 hello 를 부모에게서 물려받았거든요.","파이썬은 메서드를 찾을 때 먼저 자식 틀을 보고, 없으면 부모 틀을 봐요."],"show":"class Animal:\n    def __init__(self, name):\n        self.name = name\n\n    def hello(self):\n        return f\"나는 {self.name}\"\n\nclass Dog(Animal):\n    pass\n\nd = Dog(\"바둑이\")\nprint(d.hello())","given":"","solution":"class Animal:\n    def __init__(self, name):\n        self.name = name\n\n    def hello(self):\n        return f\"나는 {self.name}\"\n\nclass Dog(Animal):\n    pass\n\nd = Dog(\"바둑이\")\nprint(d.hello())","tests":"assert 'Animal' in globals() and 'Dog' in globals(), \"Animal 과 Dog 클래스가 모두 있어야 해요\"\nassert issubclass(Dog, Animal), \"Dog 는 class Dog(Animal): 처럼 Animal 을 물려받아야 해요\"\nassert Dog(\"초코\").hello() == '나는 초코', \"Dog(\\\"초코\\\").hello() 는 '나는 초코' 여야 해요\"\nassert Animal(\"동물\").hello() == '나는 동물', \"Animal 의 hello 도 그대로 되어야 해요\"\nassert __out__.strip() == '나는 바둑이', f\"'나는 바둑이' 가 출력되어야 해요. 지금은 {__out__.strip()} 이에요\"","hints":["부모 이름은 자식 이름 뒤 괄호 안에","class Dog(Animal):","Dog 안쪽 줄은 pass"],"after":"2주차 PracticeCode_2 의 class GCNLayer(nn.Module): 은 nn.Module 을 물려받은 자식 클래스예요."},{"id":"c06-02","title":"물려받은 메서드 바꾸기: 덮어쓰기","level":"type","run":"browser","goal":"Dog 가 부모의 sound 메서드를 자기 방식(멍멍)으로 바꿔요.","explain":["자식 틀에 부모와 **같은 이름** 의 메서드를 다시 만들면 자식 것이 쓰여요. 이걸 **덮어쓰기(Override)** 라고 해요.","Dog 의 붕어빵은 자식 틀에서 sound 를 먼저 찾으니 멍멍, Animal 의 붕어빵은 원래대로 소리 없음이에요.","부모 틀 코드는 하나도 안 바뀌어요. 자식만 달라져요.","`Animal(\"동물\").sound()` 처럼 찍자마자 바로 메서드를 불러도 돼요."],"show":"class Animal:\n    def __init__(self, name):\n        self.name = name\n\n    def sound(self):\n        return \"소리 없음\"\n\nclass Dog(Animal):\n    def sound(self):\n        return \"멍멍\"\n\nprint(Animal(\"동물\").sound())\nprint(Dog(\"바둑이\").sound())","given":"","solution":"class Animal:\n    def __init__(self, name):\n        self.name = name\n\n    def sound(self):\n        return \"소리 없음\"\n\nclass Dog(Animal):\n    def sound(self):\n        return \"멍멍\"\n\nprint(Animal(\"동물\").sound())\nprint(Dog(\"바둑이\").sound())","tests":"assert 'Animal' in globals() and 'Dog' in globals(), \"Animal 과 Dog 클래스가 있어야 해요\"\nassert issubclass(Dog, Animal), \"Dog 는 Animal 을 물려받아야 해요\"\nassert Dog(\"초코\").sound() == '멍멍', \"Dog 의 sound() 는 멍멍 이어야 해요\"\nassert Animal(\"x\").sound() == '소리 없음', \"Animal 의 sound() 는 그대로 소리 없음 이어야 해요\"\nassert Dog(\"초코\").name == '초코', \"Dog 는 부모의 __init__ 을 물려받아 name 이 있어야 해요\"\nlines = [x.strip() for x in __out__.strip().splitlines()]\nassert lines == ['소리 없음', '멍멍'], f\"출력이 소리 없음, 멍멍 두 줄이어야 해요. 지금은 {lines} 예요\"","hints":["Dog 안에 def sound(self): 를 다시 만들어요","return \"멍멍\"","들여쓰기: def 4칸, return 8칸"],"after":"nn.Module 에도 forward 가 있지만, 우리가 자식 클래스에서 forward 를 덮어써서 모델마다 계산을 정해요."},{"id":"c06-03","title":"부모가 부르는 자식의 메서드","level":"blank","run":"browser","goal":"Animal 을 물려받은 Cat 을 만들고 sound 를 야옹으로 덮어써요. 부모의 intro 가 자식의 sound 를 쓰는 걸 확인해요.","explain":["부모의 `intro` 안에는 `self.sound()` 가 있어요.","`c.intro()` 를 부르면 self 는 c (Cat 붕어빵) 예요. 그래서 `self.sound()` 는 Cat 틀의 sound, 곧 야옹을 불러요.","부모는 \"소리를 내라\" 는 순서만 정하고, 무슨 소리인지는 자식이 정해요. 이 모양이 뒤에서 만들 \"Module 흉내\" 의 핵심이에요."],"given":"class Animal:\n    def __init__(self, name):\n        self.name = name\n\n    def sound(self):\n        return \"소리 없음\"\n\n    def intro(self):\n        return f\"{self.name}: {self.sound()}\"\n\nclass Cat(___):\n    def ___(self):\n        return \"야옹\"\n\nc = Cat(\"나비\")\nprint(c.intro())","solution":"class Animal:\n    def __init__(self, name):\n        self.name = name\n\n    def sound(self):\n        return \"소리 없음\"\n\n    def intro(self):\n        return f\"{self.name}: {self.sound()}\"\n\nclass Cat(Animal):\n    def sound(self):\n        return \"야옹\"\n\nc = Cat(\"나비\")\nprint(c.intro())","tests":"assert 'Cat' in globals() and 'Animal' in globals(), \"Animal 과 Cat 클래스가 있어야 해요\"\nassert issubclass(Cat, Animal), \"Cat 은 class Cat(Animal): 처럼 Animal 을 물려받아야 해요. 첫 빈칸은 부모 이름이에요\"\nassert Cat(\"x\").sound() == '야옹', \"Cat 의 sound() 는 야옹 이어야 해요. 둘째 빈칸은 덮어쓸 메서드 이름 sound 예요\"\nassert Cat(\"치즈\").intro() == '치즈: 야옹', \"Cat(\\\"치즈\\\").intro() 는 '치즈: 야옹' 이어야 해요\"\nassert Animal(\"동물\").intro() == '동물: 소리 없음', \"Animal 의 intro 는 그대로 '동물: 소리 없음' 이어야 해요\"\nassert __out__.strip() == '나비: 야옹', f\"'나비: 야옹' 이 출력되어야 해요. 지금은 {__out__.strip()} 이에요\"","hints":["첫 빈칸은 물려받을 부모 클래스 이름","둘째 빈칸은 부모에 있는 메서드 중 소리를 돌려주는 것","class Cat(Animal): 과 def sound(self):"],"after":"nn.Module 의 속 코드가 self.forward(...) 를 부르면, 우리가 자식 클래스에 만든 forward 가 실행돼요. 똑같은 원리예요."},{"id":"c06-04","title":"부모의 속 넣기도 해 주세요: super().__init__()","level":"type","run":"browser","goal":"Dog 에 품종(breed) 속성을 더하면서, 부모의 이름 넣기도 그대로 쓰게 해요.","explain":["자식이 `__init__` 을 새로 만들면 부모의 `__init__` 을 덮어써요. 그러면 부모가 하던 `self.name = name` 이 안 일어나요.","`super()` 는 \"부모 틀\" 을 가리켜요. `super().__init__(name)` 은 \"부모 틀의 속 넣기도 해 주세요\" 라는 뜻이에요.","보통 자식 `__init__` 의 **첫 줄** 에 써요. 그다음 줄에 자식만의 속성(self.breed)을 붙여요.","부모 `__init__` 이 받는 재료(name)를 괄호 안에 그대로 넘겨요."],"show":"class Animal:\n    def __init__(self, name):\n        self.name = name\n\nclass Dog(Animal):\n    def __init__(self, name, breed):\n        super().__init__(name)\n        self.breed = breed\n\nd = Dog(\"바둑이\", \"진돗개\")\nprint(d.name, d.breed)","given":"","solution":"class Animal:\n    def __init__(self, name):\n        self.name = name\n\nclass Dog(Animal):\n    def __init__(self, name, breed):\n        super().__init__(name)\n        self.breed = breed\n\nd = Dog(\"바둑이\", \"진돗개\")\nprint(d.name, d.breed)","tests":"assert 'Dog' in globals() and 'Animal' in globals(), \"Animal 과 Dog 클래스가 있어야 해요\"\nassert issubclass(Dog, Animal), \"Dog 는 Animal 을 물려받아야 해요\"\nt = Dog(\"초코\", \"푸들\")\nassert getattr(t, 'name', None) == '초코', \"Dog 에 name 이 없어요. 자식 __init__ 첫 줄에 super().__init__(name) 을 써요\"\nassert getattr(t, 'breed', None) == '푸들', \"self.breed = breed 로 품종을 붙여요\"\nassert __out__.strip() == '바둑이 진돗개', f\"'바둑이 진돗개' 가 출력되어야 해요. 지금은 {__out__.strip()} 이에요\"","hints":["super() 뒤에 점, 그다음 __init__","super().__init__(name)","들여쓰기: def 4칸, 안쪽 두 줄 8칸"],"after":"2주차 PracticeCode_2 의 ScratchGCN, GCNLayer 는 모두 __init__ 첫 줄이 super().__init__() 이에요."},{"id":"c06-05","title":"고치기: 이름이 없어졌어요","level":"fix","run":"browser","goal":"Dog 붕어빵이 인사를 못 하고 AttributeError 가 나요. 자식 __init__ 에 빠진 한 줄을 넣어요.","explain":["에러 `AttributeError: 'Dog' object has no attribute 'name'` 은 \"Dog 객체에 name 속성이 없어요\" 라는 뜻이에요.","Dog 의 `__init__` 이 부모 것을 덮어써서, 부모의 `self.name = name` 이 한 번도 실행되지 않았어요.","`super().__init__(name)` 을 자식 `__init__` 첫 줄에 넣으면 해결돼요.","PyTorch 에서 이 줄을 빠뜨리면 `cannot assign module before Module.__init__() call` 에러가 나요. 같은 실수예요."],"given":"class Animal:\n    def __init__(self, name):\n        self.name = name\n\n    def hello(self):\n        return f\"나는 {self.name}\"\n\nclass Dog(Animal):\n    def __init__(self, name, breed):\n        self.breed = breed\n\nd = Dog(\"바둑이\", \"진돗개\")\nprint(d.hello(), d.breed)","solution":"class Animal:\n    def __init__(self, name):\n        self.name = name\n\n    def hello(self):\n        return f\"나는 {self.name}\"\n\nclass Dog(Animal):\n    def __init__(self, name, breed):\n        super().__init__(name)\n        self.breed = breed\n\nd = Dog(\"바둑이\", \"진돗개\")\nprint(d.hello(), d.breed)","tests":"assert 'Dog' in globals() and issubclass(Dog, Animal), \"Dog 는 Animal 을 물려받아야 해요\"\nt = Dog(\"초코\", \"푸들\")\nassert hasattr(t, 'name'), \"Dog 객체에 name 이 없어요. Dog 의 __init__ 첫 줄에 super().__init__(name) 을 넣어요\"\nassert t.name == '초코' and t.breed == '푸들', \"Dog(\\\"초코\\\", \\\"푸들\\\") 은 name 초코, breed 푸들 이어야 해요\"\nassert t.hello() == '나는 초코', \"hello() 는 '나는 초코' 여야 해요\"\nassert __out__.strip() == '나는 바둑이 진돗개', f\"'나는 바둑이 진돗개' 가 출력되어야 해요. 지금은 {__out__.strip()} 이에요\"","hints":["부모의 속 넣기를 불러 주는 줄이 빠졌어요","Dog 의 __init__ 안, self.breed 줄 위","super().__init__(name)"],"after":"nn.Module 을 물려받은 모델에서 super().__init__() 을 빼먹는 건 PyTorch 초보의 1등 실수예요."},{"id":"c06-06","title":"직접 짜기: 사람과 학생","level":"write","run":"browser","goal":"부모 Person 과 자식 Student 를 만들어요. Student 는 super().__init__() 으로 이름을 받고, 자기소개를 덮어써요.","explain":["지금까지 배운 상속, super().__init__(), 덮어쓰기를 한 번에 써요.","자식 `__init__` 은 재료를 두 개(name, school) 받지만, 부모에게는 name 만 넘겨요.","f-string 안에서 `{self.school}`, `{self.name}` 처럼 속성을 끼워 넣어요."],"given":"# 1. 부모 class Person 을 만들어요\n#    - def __init__(self, name): self.name 에 name\n#    - def introduce(self): f\"저는 {self.name}입니다\" 를 return\n# 2. 자식 class Student(Person) 을 만들어요\n#    - def __init__(self, name, school): super().__init__(name) 을 부르고, self.school 에 school\n#    - def introduce(self): 덮어써서 f\"저는 {self.school} 학생 {self.name}입니다\" 를 return\nclass Person:\n    ...\n\nclass Student(Person):\n    ...","solution":"class Person:\n    def __init__(self, name):\n        self.name = name\n\n    def introduce(self):\n        return f\"저는 {self.name}입니다\"\n\nclass Student(Person):\n    def __init__(self, name, school):\n        super().__init__(name)\n        self.school = school\n\n    def introduce(self):\n        return f\"저는 {self.school} 학생 {self.name}입니다\"","tests":"assert 'Person' in globals() and 'Student' in globals(), \"Person 과 Student 클래스가 모두 있어야 해요\"\nassert issubclass(Student, Person), \"Student 는 class Student(Person): 처럼 Person 을 물려받아야 해요\"\ntry:\n    p = Person(\"민수\")\nexcept TypeError:\n    assert False, 'Person(\"민수\") 처럼 이름 하나로 만들 수 있어야 해요. def __init__(self, name): 을 적어요'\nassert hasattr(p, 'introduce'), \"Person 에 introduce 메서드를 만들어요\"\nassert p.introduce() == '저는 민수입니다', f\"Person(\\\"민수\\\").introduce() 는 '저는 민수입니다' 여야 해요. 지금은 {p.introduce()} 예요\"\ntry:\n    s = Student(\"지아\", \"한빛고\")\nexcept TypeError:\n    assert False, 'Student(\"지아\", \"한빛고\") 처럼 이름과 학교로 만들 수 있어야 해요. def __init__(self, name, school): 을 적어요'\nassert hasattr(s, 'name'), \"Student 에 name 이 없어요. Student 의 __init__ 첫 줄에 super().__init__(name) 을 써요\"\nassert s.name == '지아' and getattr(s, 'school', None) == '한빛고', \"Student 는 name 지아, school 한빛고 여야 해요\"\nr = s.introduce()\nassert r == '저는 한빛고 학생 지아입니다', f\"Student 의 introduce() 는 '저는 한빛고 학생 지아입니다' 여야 해요. 지금은 {r} 예요. Student 안에서 introduce 를 덮어써요\"\ns2 = Student(\"도윤\", \"새솔중\")\nassert s2.introduce() == '저는 새솔중 학생 도윤입니다', \"다른 학생도 자기 학교와 이름으로 소개해야 해요\"\nassert Person(\"하린\").introduce() == '저는 하린입니다', \"Person 의 introduce 는 바뀌면 안 돼요\"","hints":["Person 에 메서드 2개, Student 에 메서드 2개","Student 의 __init__: super().__init__(name) 다음 self.school = school","Student 의 introduce: return f\"저는 {self.school} 학생 {self.name}입니다\""],"after":"GNN 모델 클래스 모양 (부모 nn.Module, __init__ 첫 줄 super, 메서드 덮어쓰기) 을 이미 다 써 봤어요."},{"id":"c06-07","title":"파이썬이 대신 부르는 메서드: __len__ 과 __repr__","level":"type","run":"browser","goal":"노드 경로를 담는 Path 클래스에 len() 과 print() 가 먹히게 해요.","explain":["이름 앞뒤에 밑줄이 두 개씩 붙은 메서드는 **특별 메서드** 예요. 우리가 직접 안 불러도 파이썬이 정해진 순간에 대신 불러요. `__init__` 도 그중 하나예요.","`len(p)` 를 쓰면 파이썬이 속으로 `p.__len__()` 을 불러요.","`print(p)` 를 쓰면 파이썬이 `p.__repr__()` 이 돌려준 글자를 보여 줘요.","`__repr__` 이 없으면 `<__main__.Path object at 0x7f...` 처럼 알아보기 힘든 글자가 나와요."],"show":"class Path:\n    def __init__(self, nodes):\n        self.nodes = nodes\n\n    def __len__(self):\n        return len(self.nodes)\n\n    def __repr__(self):\n        return f\"Path({self.nodes})\"\n\np = Path([\"A\", \"B\", \"C\"])\nprint(len(p))\nprint(p)","given":"","solution":"class Path:\n    def __init__(self, nodes):\n        self.nodes = nodes\n\n    def __len__(self):\n        return len(self.nodes)\n\n    def __repr__(self):\n        return f\"Path({self.nodes})\"\n\np = Path([\"A\", \"B\", \"C\"])\nprint(len(p))\nprint(p)","tests":"assert 'Path' in globals(), \"Path 클래스가 있어야 해요\"\nassert len(Path([1, 2])) == 2, \"len(Path([1, 2])) 는 2 여야 해요. __len__ 에서 return len(self.nodes)\"\nassert len(Path([])) == 0, \"빈 경로의 길이는 0 이에요\"\nassert repr(Path([0])) == 'Path([0])', \"Path([0]) 을 출력하면 Path([0]) 이어야 해요. __repr__ 을 확인해요\"\nlines = [x.strip() for x in __out__.strip().splitlines()]\nassert lines == ['3', \"Path(['A', 'B', 'C'])\"], f\"출력이 3 과 Path(['A', 'B', 'C']) 두 줄이어야 해요. 지금은 {lines} 예요\"","hints":["밑줄은 앞 두 개, 뒤 두 개","__len__ 과 __repr__ 둘 다 return 이 있어야 해요","return f\"Path({self.nodes})\""],"after":"PyTorch 에서 print(model) 하면 층 목록이 보기 좋게 나오는 게 nn.Module 의 __repr__ 덕분이에요."},{"id":"c06-08","title":"객체를 함수처럼 부르기: __call__","level":"type","run":"browser","goal":"숫자를 더해 주는 Adder 객체를 만들고, 함수처럼 괄호로 불러요.","explain":["클래스에 `__call__` 메서드가 있으면 객체 이름 뒤에 괄호를 붙여 함수처럼 부를 수 있어요.","`add3(5)` 를 쓰면 파이썬이 속으로 `add3.__call__(5)` 를 불러요. 그래서 x 는 5, self.k 는 3 이에요.","add3 과 add10 은 같은 틀인데 k 속이 달라서 서로 다르게 더해요. 자기 설정을 기억하는 자판기예요.","PyTorch 에서 `model(x)` 로 모델을 부르는 게 바로 이 `__call__` 이에요."],"show":"class Adder:\n    def __init__(self, k):\n        self.k = k\n\n    def __call__(self, x):\n        return x + self.k\n\nadd3 = Adder(3)\nadd10 = Adder(10)\nprint(add3(5))\nprint(add10(5))","given":"","solution":"class Adder:\n    def __init__(self, k):\n        self.k = k\n\n    def __call__(self, x):\n        return x + self.k\n\nadd3 = Adder(3)\nadd10 = Adder(10)\nprint(add3(5))\nprint(add10(5))","tests":"assert 'Adder' in globals(), \"Adder 클래스가 있어야 해요\"\nassert callable(Adder(1)), \"Adder 에 __call__ 메서드가 있어야 객체를 괄호로 부를 수 있어요\"\nassert Adder(3)(5) == 8, \"Adder(3)(5) 는 8 이어야 해요\"\nassert Adder(-1)(1) == 0, \"Adder(-1)(1) 은 0 이어야 해요\"\nlines = [x.strip() for x in __out__.strip().splitlines()]\nassert lines == ['8', '15'], f\"출력이 8 과 15 두 줄이어야 해요. 지금은 {lines} 예요\"","hints":["def __call__(self, x): 는 4칸 들여쓰기","return x + self.k","부를 때는 add3(5)"],"after":"2주차 학습 코드의 out = model(data.x, data.edge_index) 가 이 __call__ 로 동작해요."},{"id":"c06-09","title":"__call__ 이 forward 에게 일 넘기기","level":"blank","run":"browser","goal":"Doubler 객체를 부르면 __call__ 이 직접 계산하지 않고 forward 메서드에게 맡기도록 빈칸을 채워요.","explain":["**forward** 는 \"데이터가 들어오면 할 일\" 을 적는 메서드 이름이에요. 여기서는 두 배 하기예요.","`__call__` 은 `self.forward(x)` 를 불러서 그 결과를 돌려주기만 해요.","속 넣을 것이 없으면 `__init__` 을 안 만들어도 돼요.","왜 굳이 나누냐면, 다음 실습에서 `__call__` 은 부모가, forward 는 자식이 맡게 하려고요."],"given":"class Doubler:\n    def forward(self, x):\n        return x * 2\n\n    def __call__(self, x):\n        return self.___(x)\n\nd = Doubler()\nprint(d(4))\nprint(d.forward(4))","solution":"class Doubler:\n    def forward(self, x):\n        return x * 2\n\n    def __call__(self, x):\n        return self.forward(x)\n\nd = Doubler()\nprint(d(4))\nprint(d.forward(4))","tests":"assert 'Doubler' in globals(), \"Doubler 클래스가 있어야 해요\"\nassert Doubler()(7) == 14, \"Doubler()(7) 은 14 여야 해요. 빈칸에 forward 를 넣어요\"\nclass _Plus100(Doubler):\n    def forward(self, x):\n        return x + 100\nassert _Plus100()(1) == 101, \"__call__ 안에서 직접 계산하지 말고 self.forward(x) 를 불러야 해요\"\nlines = [x.strip() for x in __out__.strip().splitlines()]\nassert lines == ['8', '8'], f\"출력이 8, 8 두 줄이어야 해요. 지금은 {lines} 예요\"","hints":["빈칸은 위에 만든 메서드 이름","데이터가 들어오면 할 일을 적은 메서드","return self.forward(x)"],"after":"nn.Module 의 __call__ 도 속에서 self.forward(...) 를 불러요."},{"id":"c06-10","title":"Module 흉내 1: 부모 Layer 와 자식 Scale","level":"blank","run":"browser","goal":"모든 층의 부모 Layer 를 물려받아 곱하기 층 Scale 을 완성해요.","explain":["**Layer(층)** 는 숫자가 들어오면 계산해서 내보내는 상자예요. 모든 층의 공통 일은 부모 Layer 가 맡아요: 몇 번 불렸나 세기(calls)와 `__call__`.","자식 Scale 은 자기만의 계산 forward (곱하기 k) 만 만들면 돼요.","`s(5)` 를 부르면 부모의 `__call__` 이 돌고, 그 안의 `self.forward(x)` 는 s 가 Scale 이라서 Scale 의 forward 가 불려요 (03 번에서 본 원리).","`super().__init__()` 이 있어야 부모의 `self.calls = 0` 이 생겨요.","이 구조가 PyTorch nn.Module 과 똑같아요: 부모가 `__call__`, 자식이 `__init__` 과 forward."],"given":"class Layer:\n    def __init__(self):\n        self.calls = 0\n\n    def __call__(self, x):\n        self.calls = self.calls + 1\n        return self.forward(x)\n\nclass Scale(___):\n    def __init__(self, k):\n        super().___()\n        self.k = k\n\n    def forward(self, x):\n        return x * self.k\n\ns = Scale(3)\nprint(s(5), s(2))\nprint(s.calls)","solution":"class Layer:\n    def __init__(self):\n        self.calls = 0\n\n    def __call__(self, x):\n        self.calls = self.calls + 1\n        return self.forward(x)\n\nclass Scale(Layer):\n    def __init__(self, k):\n        super().__init__()\n        self.k = k\n\n    def forward(self, x):\n        return x * self.k\n\ns = Scale(3)\nprint(s(5), s(2))\nprint(s.calls)","tests":"assert 'Layer' in globals() and 'Scale' in globals(), \"Layer 와 Scale 클래스가 있어야 해요\"\nassert issubclass(Scale, Layer), \"Scale 은 class Scale(Layer): 처럼 Layer 를 물려받아야 해요\"\nt = Scale(2)\nassert hasattr(t, 'calls') and t.calls == 0, \"Scale 에 calls 가 없어요. __init__ 첫 줄을 super().__init__() 으로 해요\"\nassert t(4) == 8 and abs(t(0.5) - 1.0) < 1e-9, \"Scale(2) 에 4 를 넣으면 8, 0.5 를 넣으면 1.0 이어야 해요\"\nassert t.calls == 2, f\"두 번 불렀으니 calls 는 2 여야 해요. 지금은 {t.calls} 예요\"\nassert Scale(-1)(7) == -7, \"Scale(-1)(7) 은 -7 이어야 해요\"\nlines = [x.strip() for x in __out__.strip().splitlines()]\nassert lines == ['15 6', '2'], f\"출력이 '15 6' 과 '2' 두 줄이어야 해요. 지금은 {lines} 예요\"","hints":["첫 빈칸은 부모 클래스 이름","둘째 빈칸은 부모의 속 넣기 메서드 이름","class Scale(Layer): 와 super().__init__()"],"after":"2주차 GCNLayer(nn.Module) 도 __init__ 에서 super().__init__() 하고 forward 에 계산을 적어요."},{"id":"c06-11","title":"고치기: self 도 return 도 빠졌어요","level":"fix","run":"browser","goal":"숫자에 b 를 더하는 층 AddBias 를 불렀더니 TypeError 가 나요. forward 에 빠진 것이 두 가지 있어요. 하나 고치면 None 이 나오니 끝까지 고쳐요.","explain":["**bias(편향)** 는 계산 결과에 더해 주는 숫자예요. nn.Linear 안에도 있어요.","`TypeError: forward() takes 1 positional argument but 2 were given` 은 \"칸은 1개인데 2개가 들어왔다\" 는 뜻이에요. `self.forward(x)` 는 객체 자신과 x 두 개를 넘기니, 메서드 첫 칸에 `self` 가 있어야 해요 (c05-10 에서 본 실수).","`layer(5)` 는 부모 `__call__` 이 `return self.forward(x)` 를 해요. 그런데 forward 가 아무것도 return 하지 않으면 None 이 그대로 나가요.","`x + self.b` 는 계산만 하고 버려져요. 앞에 `return` 을 붙여야 밖으로 나가요 (c04 에서 본 실수)."],"given":"class Layer:\n    def __init__(self):\n        self.calls = 0\n\n    def __call__(self, x):\n        self.calls = self.calls + 1\n        return self.forward(x)\n\nclass AddBias(Layer):\n    def __init__(self, b):\n        super().__init__()\n        self.b = b\n\n    def forward(x):\n        x + self.b\n\nlayer = AddBias(10)\nprint(layer(5))","solution":"class Layer:\n    def __init__(self):\n        self.calls = 0\n\n    def __call__(self, x):\n        self.calls = self.calls + 1\n        return self.forward(x)\n\nclass AddBias(Layer):\n    def __init__(self, b):\n        super().__init__()\n        self.b = b\n\n    def forward(self, x):\n        return x + self.b\n\nlayer = AddBias(10)\nprint(layer(5))","tests":"assert 'AddBias' in globals() and issubclass(AddBias, Layer), \"AddBias 는 Layer 를 물려받아야 해요\"\ny = AddBias(10)(5)\nassert y is not None, \"AddBias(10)(5) 가 None 이에요. forward 안의 x + self.b 앞에 return 을 붙여요\"\nassert y == 15, f\"AddBias(10)(5) 는 15 여야 해요. 지금은 {y} 예요\"\nt = AddBias(-1)\nassert t(1) == 0 and t(-3) == -4, \"AddBias(-1) 에 1 을 넣으면 0, -3 을 넣으면 -4 여야 해요\"\nassert t.calls == 2, \"층을 부를 때마다 calls 가 늘어야 해요. Layer 코드는 그대로 둬요\"\nassert __out__.strip() == '15', f\"15 가 출력되어야 해요. 지금은 {__out__.strip()} 이에요\"","hints":["에러 메시지 마지막 줄을 읽어요. 메서드 첫 칸에 무엇이 있어야 했죠?","def forward(self, x): 로 고친 뒤 None 이 나오면, None 은 무엇을 빠뜨렸다는 신호였죠?","def forward(self, x): 와 return x + self.b"],"after":"PyTorch 모델 forward 끝에 return 을 빼먹으면 out 이 None 이 되어 loss 계산에서 에러가 나요."},{"id":"c06-12","title":"Module 흉내 2: 층을 차례로 담는 Sequential","level":"blank","run":"browser","goal":"층 여러 개를 리스트로 받아 순서대로 통과시키는 Sequential 을 완성해요.","explain":["Sequential 은 층들을 줄 세워 담는 상자예요. 자기도 Layer 의 자식이라 `model(x)` 로 부를 수 있어요.","forward 에서 층을 하나씩 꺼내 `x = layer(x)` 로 통과시키고, 나온 값을 다시 x 에 담아 다음 층에 넣어요.","모든 층을 지난 마지막 x 를 return 해요.","`__len__` 덕분에 `len(model)` 은 층 수예요.","`Sequential([Scale(2), Scale(10)])` 에 3 을 넣으면 3 곱하기 2 는 6, 6 곱하기 10 은 60 이에요."],"given":"class Layer:\n    def __init__(self):\n        self.calls = 0\n\n    def __call__(self, x):\n        self.calls = self.calls + 1\n        return self.forward(x)\n\nclass Scale(Layer):\n    def __init__(self, k):\n        super().__init__()\n        self.k = k\n\n    def forward(self, x):\n        return x * self.k\n\nclass Sequential(Layer):\n    def __init__(self, layers):\n        super().__init__()\n        self.layers = layers\n\n    def forward(self, x):\n        for layer in self.___:\n            x = layer(x)\n        return ___\n\n    def __len__(self):\n        return len(self.layers)\n\nmodel = Sequential([Scale(2), Scale(10)])\nprint(model(3))\nprint(len(model))","solution":"class Layer:\n    def __init__(self):\n        self.calls = 0\n\n    def __call__(self, x):\n        self.calls = self.calls + 1\n        return self.forward(x)\n\nclass Scale(Layer):\n    def __init__(self, k):\n        super().__init__()\n        self.k = k\n\n    def forward(self, x):\n        return x * self.k\n\nclass Sequential(Layer):\n    def __init__(self, layers):\n        super().__init__()\n        self.layers = layers\n\n    def forward(self, x):\n        for layer in self.layers:\n            x = layer(x)\n        return x\n\n    def __len__(self):\n        return len(self.layers)\n\nmodel = Sequential([Scale(2), Scale(10)])\nprint(model(3))\nprint(len(model))","tests":"assert 'Sequential' in globals() and issubclass(Sequential, Layer), \"Sequential 은 Layer 를 물려받아야 해요\"\nassert Sequential([Scale(3)])(2) == 6, \"Sequential([Scale(3)]) 에 2 를 넣으면 6 이어야 해요. 첫 빈칸은 층 리스트 속성 layers 예요\"\nr = Sequential([Scale(2), Scale(5), Scale(-1)])(1)\nassert r == -10, f\"Scale 2, 5, -1 을 차례로 지나면 1 은 -10 이 되어야 해요. 지금은 {r} 이에요. 마지막에 return x\"\nassert Sequential([])(7) == 7, \"층이 없으면 넣은 값이 그대로 나와야 해요\"\nassert len(Sequential([Scale(1), Scale(1), Scale(1)])) == 3, \"len 은 층 수여야 해요\"\nlines = [x.strip() for x in __out__.strip().splitlines()]\nassert lines == ['60', '2'], f\"출력이 60 과 2 두 줄이어야 해요. 지금은 {lines} 예요\"","hints":["첫 빈칸은 __init__ 에서 붙인 층 리스트 속성 이름","둘째 빈칸은 모든 층을 지난 값","for layer in self.layers: 와 return x"],"after":"PyTorch 의 nn.Sequential(nn.Linear(...), nn.ReLU(), ...) 가 바로 이 상자예요."},{"id":"c06-13","title":"고치기: forward 를 직접 부르면 안 돼요","level":"fix","run":"browser","goal":"모델 결과 60 은 맞는데 층의 calls 가 0 이에요. Sequential 이 층을 부르는 방법을 고쳐요.","explain":["`layer.forward(x)` 로 부르면 계산은 되지만 부모의 `__call__` 을 건너뛰어요. 그래서 `__call__` 이 하던 일 (여기서는 calls 세기) 이 빠져요.","층은 `layer(x)` 처럼 **객체를 불러야** 해요. 그러면 `__call__` 이 돌고, 그 안에서 forward 가 불려요.","PyTorch 의 `__call__` 도 forward 앞뒤로 준비 작업(hook 등)을 해요. 그래서 `model.forward(x)` 대신 `model(x)` 로 부르라고 해요."],"given":"class Layer:\n    def __init__(self):\n        self.calls = 0\n\n    def __call__(self, x):\n        self.calls = self.calls + 1\n        return self.forward(x)\n\nclass Scale(Layer):\n    def __init__(self, k):\n        super().__init__()\n        self.k = k\n\n    def forward(self, x):\n        return x * self.k\n\nclass Sequential(Layer):\n    def __init__(self, layers):\n        super().__init__()\n        self.layers = layers\n\n    def forward(self, x):\n        for layer in self.layers:\n            x = layer.forward(x)\n        return x\n\ns1 = Scale(2)\ns2 = Scale(10)\nmodel = Sequential([s1, s2])\nprint(model(3))\nprint(s1.calls, s2.calls)","solution":"class Layer:\n    def __init__(self):\n        self.calls = 0\n\n    def __call__(self, x):\n        self.calls = self.calls + 1\n        return self.forward(x)\n\nclass Scale(Layer):\n    def __init__(self, k):\n        super().__init__()\n        self.k = k\n\n    def forward(self, x):\n        return x * self.k\n\nclass Sequential(Layer):\n    def __init__(self, layers):\n        super().__init__()\n        self.layers = layers\n\n    def forward(self, x):\n        for layer in self.layers:\n            x = layer(x)\n        return x\n\ns1 = Scale(2)\ns2 = Scale(10)\nmodel = Sequential([s1, s2])\nprint(model(3))\nprint(s1.calls, s2.calls)","tests":"assert 'Sequential' in globals() and issubclass(Sequential, Layer), \"Sequential 은 Layer 를 물려받아야 해요\"\nt1 = Scale(2)\nt2 = Scale(3)\nm = Sequential([t1, t2])\nassert m(1) == 6, \"Scale 2, 3 을 지나면 1 은 6 이어야 해요\"\nassert t1.calls == 1 and t2.calls == 1, f\"각 층의 calls 가 1 이어야 하는데 지금은 {t1.calls}, {t2.calls} 예요. layer.forward(x) 를 layer(x) 로 바꿔요\"\nassert m(2) == 12 and t1.calls == 2, \"두 번째로 불러도 층마다 calls 가 늘어야 해요\"\nassert s1.calls == 1 and s2.calls == 1, \"s1, s2 의 calls 도 1 이어야 해요\"\nlines = [x.strip() for x in __out__.strip().splitlines()]\nassert lines == ['60', '1 1'], f\"출력이 60 과 '1 1' 이어야 해요. 지금은 {lines} 예요\"","hints":["결과 숫자는 맞으니 계산이 아니라 부르는 방법이 문제예요","Sequential 의 forward 안 x = ... 줄","x = layer(x)"],"after":"GNN 모델 안에서 self.conv1(x, edge_index) 처럼 층 객체를 부르고, conv1.forward(...) 로 부르지 않는 이유예요."},{"id":"c06-14","title":"직접 짜기: 순수 파이썬 Module 흉내","level":"write","run":"browser","goal":"부모 Layer, 자식 Scale 과 ReLU, 층 상자 Sequential 을 처음부터 직접 짜서 작은 모델을 만들어요.","explain":["**ReLU** 는 음수를 0 으로 바꾸고 양수는 그대로 두는 층이에요. GNN 에서 층 사이에 끼워 넣어요. `max(0, x)` 한 줄이면 돼요.","ReLU 는 넣을 속이 없어서 `__init__` 을 안 만들어도 돼요. 부모 Layer 의 `__init__` 을 물려받아 calls 가 생겨요.","모델 `Sequential([Scale(2), ReLU(), Scale(3)])` 에 4 를 넣으면 8, 8, 24 순서로 흘러가요. -1 을 넣으면 -2, 0, 0 이에요.","다 짜면 PyTorch 의 nn.Module, forward, model(x), nn.Sequential 을 이미 한 번 만들어 본 거예요."],"given":"# 1. 부모 class Layer\n#    - def __init__(self): self.calls 에 0\n#    - def __call__(self, x): self.calls 를 1 늘리고, self.forward(x) 의 결과를 return\n# 2. 자식 class Scale(Layer)\n#    - def __init__(self, k): super().__init__() 을 부르고 self.k 에 k\n#    - def forward(self, x): x * self.k 를 return\n# 3. 자식 class ReLU(Layer)\n#    - def forward(self, x): max(0, x) 를 return (__init__ 은 안 만들어도 돼요)\n# 4. 자식 class Sequential(Layer)\n#    - def __init__(self, layers): super().__init__() 을 부르고 self.layers 에 layers\n#    - def forward(self, x): 층마다 x = layer(x), 마지막 x 를 return\n#    - def __len__(self): 층 수를 return\n# 5. model = Sequential([Scale(2), ReLU(), Scale(3)]) 을 만들고 print(model(4)) 로 확인해요\nclass Layer:\n    ...","solution":"class Layer:\n    def __init__(self):\n        self.calls = 0\n\n    def __call__(self, x):\n        self.calls = self.calls + 1\n        return self.forward(x)\n\nclass Scale(Layer):\n    def __init__(self, k):\n        super().__init__()\n        self.k = k\n\n    def forward(self, x):\n        return x * self.k\n\nclass ReLU(Layer):\n    def forward(self, x):\n        return max(0, x)\n\nclass Sequential(Layer):\n    def __init__(self, layers):\n        super().__init__()\n        self.layers = layers\n\n    def forward(self, x):\n        for layer in self.layers:\n            x = layer(x)\n        return x\n\n    def __len__(self):\n        return len(self.layers)\n\nmodel = Sequential([Scale(2), ReLU(), Scale(3)])\nprint(model(4))","tests":"for _name in [\"Layer\", \"Scale\", \"ReLU\", \"Sequential\"]:\n    assert _name in globals(), f\"{_name} 클래스를 만들어야 해요\"\nfor _name in [\"Scale\", \"ReLU\", \"Sequential\"]:\n    assert issubclass(globals()[_name], Layer), f\"{_name} 은 class {_name}(Layer): 처럼 Layer 를 물려받아야 해요\"\nclass _Plus1(Layer):\n    def forward(self, x):\n        return x + 1\np = _Plus1()\nassert hasattr(p, 'calls') and p.calls == 0, \"Layer 의 __init__ 에서 self.calls = 0 을 만들어요\"\nassert callable(p), \"Layer 에 __call__ 메서드를 만들어요\"\nr = p(1)\nassert r == 2, f\"Layer 의 __call__ 은 self.forward(x) 의 결과를 return 해야 해요. 지금은 {r} 이 나와요\"\nassert p.calls == 1, \"__call__ 이 불릴 때마다 self.calls 를 1 늘려야 해요\"\ntry:\n    s = Scale(5)\nexcept TypeError:\n    assert False, \"Scale(5) 처럼 k 하나로 만들 수 있어야 해요. def __init__(self, k): 를 적어요\"\nassert hasattr(s, 'calls'), \"Scale 에 calls 가 없어요. Scale 의 __init__ 첫 줄에 super().__init__() 을 불러요\"\nassert s(2) == 10 and s(-1) == -5, \"Scale(5) 에 2 를 넣으면 10, -1 을 넣으면 -5 여야 해요\"\nassert s.calls == 2, \"Scale 을 두 번 불렀으니 calls 는 2 여야 해요\"\nq = ReLU()\nassert q(3) == 3 and q(-2) == 0 and q(0) == 0, \"ReLU 는 양수는 그대로, 음수와 0 은 0 을 돌려줘야 해요\"\na = Scale(2)\nb = ReLU()\nc = Scale(3)\ntry:\n    m = Sequential([a, b, c])\nexcept TypeError:\n    assert False, \"Sequential([층, 층]) 처럼 층 리스트 하나로 만들 수 있어야 해요. def __init__(self, layers): 를 적어요\"\nassert hasattr(m, 'calls'), \"Sequential 에 calls 가 없어요. __init__ 첫 줄에 super().__init__() 을 불러요\"\ny = m(4)\nassert y == 24, f\"모델에 4 를 넣으면 8, 8, 24 로 흘러 24 여야 해요. 지금은 {y} 예요\"\nassert m(-1) == 0, \"모델에 -1 을 넣으면 -2, 0, 0 이라 0 이어야 해요\"\nassert a.calls == 2 and b.calls == 2 and c.calls == 2, \"Sequential 의 forward 에서 층을 layer(x) 로 불러야 층마다 calls 가 늘어요\"\nassert m.calls == 2, \"모델 자신의 calls 도 2 여야 해요\"\nassert hasattr(Sequential, '__len__') and len(m) == 3, \"Sequential 에 __len__ 을 만들어 층 수를 return 해요\"\nassert 'model' in globals() and isinstance(model, Sequential), \"model = Sequential([Scale(2), ReLU(), Scale(3)]) 을 만들어요\"\nassert '24' in __out__, \"print(model(4)) 로 24 를 출력해요\"","hints":["10, 12, 13 번 실습 코드를 떠올려요. 클래스 네 개를 위에서부터 차례로","ReLU 는 class ReLU(Layer): 아래 def forward(self, x): return max(0, x) 뿐이에요","Sequential 의 forward: for layer in self.layers: x = layer(x) 다음 return x","자식 __init__ 첫 줄은 언제나 super().__init__()"],"after":"다음 단원 PyTorch 에서는 Layer 대신 nn.Module, Scale 대신 nn.Linear 를 쓸 뿐 모양이 똑같아요."}]};
